وقارنته بالنسخة السابعة وتوصيات اإلصالح السابقة   dcr-app(8).rar حللت  . والمراجع المرفقة
لإلطالق التشغيلي أو   NO-GO التحديث حقيقي وكبير، لكنه ما زال  : الحكم النهائ
ي . التخاذ قرارات ميدانية آلية
 نتائج التحقق
  النتيجة  الفحص
  ناجحة  690/690 اختبارات المشروع
 
TypeScript  ناجح 
ESLint  صفر أخطاء وتحذيرات  Supabase ناجح عند توفير متغيرات  لإلنتاج  Next.js بنا
ء %78.54 تغطية األسطر
 اختبار قبول مستقل 
  ناجحة  4/9 مختصر
 اختبار قرار مستقل 
  ناجحة فقط  6/19 موسع
 اختبارات قاعدة 
  غير مضمنة  Supabase CLI لم تعمل ألن البيانات
 
E2E   لم يكتمل في البيئة الحالية؛ واالختبارات الموجودة تغطي صفحات عامة فقط  6
اختبارًا ال يكفي للحكم بالجاهزية؛ بعض االختبارات الحالية تتوقع   690 نجاح
  . القديم مؤثرًا PM10 صراحة ً حزمة القواعد القديمة وسلوك بقاء
 ما تم إصالحه فعليًا
  : إصالح تحويالت 
•
o RESTRICT → RESTRICT 
o RESTRICT_SEVERE → RESTRICT 
o STOP_DUST_* → PROTECTIVE_STOP 
o STOP_VISIBILITY_* → PROTECTIVE_STOP  .Open-Meteo المحرك الحي األساسي أصبح يعمل دون طلب  
•
• eventId و sequence و observedAt أصبحت إلزامية في Push.  .ThingsBoard مستقل لحقول timestamp أضيف تخزين 
• .Evaluation Runs و Queue و Scheduler أضيفت هياكل 
• . أضيفت نية التنبيه داخل معاملة حفظ القرار  
• .Credentials V2 وتشفير  Provider Instances أضيفت 
• . في اإلنتاج Mock تم تعطيل 
• . الجديدة فريدة migrations أرقام 
• . تحسنت جذريًا مقارنة بالنسخة السابعة ESLint و TypeScript البناء و 
• موانع اإلطالق الحرجة
  الصحيحة موجودة لكنها غير مفعّلة  PM10 حزمة  .
1يحتوي الحدود الجديدة، لكنه يعيّن الحزمة النشطة  ts-dust-riyadh.2026.3 الملف
  .2026.2 إلى
  : لذلك السلوك الفعلي ما زا
ل
PM10  المطلوب تشغيليًا  السلوك الحالي 
151 ALLOW PRECAUTION 
201 PRECAUTION PRECAUTION 
321 ALLOW_WITH_CONTROLS RESTRICT_ACTIVITY 
340 RESTRICT RESTRICT  مسار اإليقاف  مسار اإليقاف  340
>، وتثبت  250 التحذير عند  : المرجع التنظيمي في  82 تحققت كذلك من الصفحة
 لمدة دقيقة وفترة   PM10 ألكثر من دقيقتين، مع بيانات 340 المخالفة عند تجاوز
  . تسجيل ال تتجاوز دقيقة
، ويسمح بفجوة أربع دقائق  251 النظام الحالي يبدأ التحذير التنظيمي فعليًا عند
 صحيح، لكن استمرارية الدليل   120001ms شرط الزمن  .PM10 داخل سلسلة استمرار

نفسها غير صحيحة؛ يمكن لعينتين متباعدتين أكثر من المسموح إثبات مخالفة   . زائفة
 التحقق الميداني ما زال يتحول إلى مراقبة .
2 : محرك القرار النهائي في
 
FIELD_VERIFICATION_REQUIRED → MONITOR  : بينما المطلو
ب
FIELD_VERIFICATION_REQUIRED → HOLD_FOR_VERIFICATION كما أن سبب القرار المعروض ال يرتبط دائمًا بالمرشح الفائز. قد يكون القرار 
  ، بينما يعرض النظام سببًا أضعف من DVI بسبب PROTECTIVE_STOP النهائي
 
Compliance. هذا يفسد قابلية التدقيق حتى عندما تكون فئة القرار صحيحة .  حداثة البيانات ما زالت جزئية  .
3بوابة األربع دقائق مطبقة فقط تقريبًا على الرياح والهبات واالتجاه والرؤية. 
  : أما

• PM10 
• PM2.5  الحرارة  
• الرطوبة  
•ما زال يستخدم   Evidence Adapter فقد تدخل القرار دون نفس االستبعاد. كما أن
  . دقيقة 20 مقدارها  PM10 نافذة
وعمر أكبر من أربع دقائق ما زال   500 بقيمة  PM10 االختبارات المضادة أثبتت أن
 أو يسمح باستكمال   ALLOW قادرًا على إنتاج إيقاف، وأن فقد الرؤية قد يؤدي إلى
  . نافذة االستئناف من إيقاف سابق
حسب نوع   requiredMetrics ، مع Metric الحل يجب أن يكون مصنف حداثة مركزيًا لك
ل . ويعيد مؤقت االستئناف للصفر HOLD النشاط، وأي نقص في دليل مطلوب ينتج
 
4. ThingsBoard ما زال يعيد تأريخ PM10  القديم كقراءة حديثة  : توجد كتابتان منفصلتان deviceReadingWriter.ts في
 
1. RPC قديمة تستخدم أحدث timestamp عام . 
2. RPC V2 تستخدم timestamps المستقلة الصحيحة .  : مثال فعل
ي
• PM10 = 500  08:00 الساعة .  . 10:00 الحرارة حديثة الساعة  
• . 10:00 في التاريخ عند  PM10 المسار القديم يسجل 
• . حساب االستمرار يقرأ هذا التاريخ القديم 
•ال يفشل   V2 قديم إلى دليل حديث زائف. كما أن فشل  PM10 وبذلك يمكن تحوي
ل . فقط console العملية كلها؛ يُسجل في 
  PM10 واحدة ذرية، ويجب إلغاء الكتابة القديمة أو جعل سجل RPC المطلوب
 
Projection مشتقة من V2 باستخدام وقت PM10 نفسه . 
5. Scheduler  موجود في الكود لكنه غير منشور تشغيليًا 
 vercel.json ما زال يشغل api/alerts/generate /  مرة يوميًا فقط، وال يشغّل كل : دقيقتين
 
• Scheduler Tick 
• Scheduler Worker 
• Provider Pull 
• Alert Outbox Worker 
• Forecast Refresh إذا تعطل العامل    .Lease بعد انتهاء RUNNING ال تستعيد مهمة  Claim دالة كذلك
  . تبقى المهمة عالقة إلى األبد
 يدويًا أوالً ثم   fail ؛ فهو يستدعي Crash ال يحاكي  Recovery االختبار المسم
ى .RETRY يستعيد مهمة 
 
6. Outbox  ليست قابلة لالسترداد وما زال المسار القديم يعمل  : رغم أن نية التنبيه أصبحت ذرية مع القرار، بقيت المشكالت التالي
ة . فعاالً  Lease Recovery ال يملك  RUNNING صف  
• .CLOSE ال توجد نوايا  
• . جزئي يمنع تنبيهين مفتوحين للحالة نفسها  Unique ال يوجد 
•
• final_decision_id ال يُحفظ فعليًا على التنبيه .  .Outbox الناتج في alert_id ال يُحفظ   
•
 .PROCESSED العامل يهمل بعض أخطاء تحديث  •
•  evaluateProject.ts ما زال يستدعي checkDustActivities   القديم بعد . الحفظ
جديدة ومسار مباشر قديم. كما   Outbox : هذا يعني وجود مسارين متنافسين للتنبي
ه، رغم فصل المحرك  Push/Scheduler إلى دورة Open-Meteo يعيد المسار القديم
 . الحي عنه
 P0 ترقية قاعدة البيانات تحتوي أخطاء .
7 : كشفا igrations الفحوص الديناميكية والتحليل النهائي لل
م؛ ما زال االعتماد على  revision باستخدام  CAS ال يوجد 
•
expected_updated_at. 
• migration  القرار أضافت overload  15 وسيطة وتركت نسخة  17 جديدًا بـ   .EXECUTE وسيطة دون سحب واضح، مع مشكلة محتملة في صالحية
 أسقط قفل المشروع وفحص   ingest_device_event_v2 أحدث تعريف لـ 
•
archived_at. بعد إعادة   Event أثبت اختبار قاعدة بيانات أن مشروعًا مؤرشفًا يقبل 
• . تفعيل جهازه
 اإلشارة إلى A في مشروع Current العزل المركب غير مكتمل؛ يمكن لمؤشر 
•
Evaluation من مشروع B.  SET NOT NULL بسبب   Legacy تفشل عند وجود صفوف Credentials V2 ترقية 
• .backfill قبل اكتمال ال
ـ .NULL عن دليلها بجعل المرجع  Evaluation يستطيع فصل  Profile حذف 
•ليست محمية بالكامل من   Outbox و Measurements و Device Events جداول 
• .TRUNCATE التعديل أو
  ThingsBoard قد تسرّب بيانات SSRF حماية .
8 عام آخر مع إعادة استخدام  Origin إلى   Redirect يتم اتباع safeUrl.ts في
 
Body و Headers نفسها .  : بالتالي يمكن أن ينتقل إلى نطاق آخ
ر .Login اسم المستخدم وكلمة المرور في طلب 
•
• JWT   في X-Authorization.  DNS منفصالً عن االتصال الفعلي، ما يترك احتمال DNS كما يبقى فحص
 
rebinding/TOCTOU. مختلف تمامًا للطلبات الحساسة، أو نزع   Origin عبر Redirect المطلوب من
ع . موثوق مع مهلة إجمالية للطلب  Egress Proxy/Resolver األسرار، واستخدام
 قرار االعتماد
  . مقبول : التطوير والعرض الداخلي  
• . ممكنة بحذر : تجربة قراءة فقط مع إشراف بشري 
• . غير مقبول : تنبيهات تلقائية أو إيقاف/استئناف ميداني 
• .NO-GO : سجل امتثال رسمي أو نشر إنتاجي 
•استقبال   RPC ، حداثة كل الحقول، 2026.3 قبل النسخة التاسعة يجب إغالق: تفعيل
 على   migrations ، إصالح Outbox الفعلي، توحيد Cron ، نشر Leases واحدة، استرداد
ثم   .Redirect قاعدة تحتوي بيانات، عزل المشاريع، ومنع تسريب األسرار عبر
تشغيلي   E2E و  Crash Recovery والتزامن و Upgrade DB و Fresh DB تشغيل اختبارات
 . صفحات الدخول فقط not— كامل
 . لم أعدّل األرشيف األصل
ي
 
 
 
 
 
 
 
 
 
 
 
 
// Rule Bundle — RCRC-NCEC-RIYADH-DUST-2026.3  من "دليل اإلصالح الجذري  5.3 حزمة قواعد مجمَّدة وموث َّقة باإلصدار (القسم  /
/ ) عن الحكم operational تفصل النطاق التشغيلي لمرقاب ( — لمنظومة مرقاب")  /
/ واحدة كما كانت في  PM10 ) بدل خلطهما في حدود regulatory التنظيمي الرسمي ( /
/
// rulebook.ts ال تُعدَّل هذه الحزمة بعد نشرها؛ أي تغيير على الحدود يتطلب .  أدناه.  RULE_BUNDLES ...) تُضاف إلى 2026.4 حزمة جديدة ( /
/
//  ============================================================= 
 
export interface Pm10OperationalBounds  { 
  normalMaxInclusive: number ; 
  precautionMaxInclusive: number ; 
  controlsMaxInclusive: number ; 
  severeMaxInclusive: number ; 
} 
 
export interface Pm10RegulatoryBounds  { 
  warningThresholdInclusive: number ; 
  violationThresholdExclusive: number ; 
  violationDurationMsExclusive: number ; 
  suspensionThresholdInclusive: number ; 
  suspensionDurationMsInclusive: number ; 
} 
 
export interface Pm10EvidenceBounds   { 
  liveFreshnessMsInclusive: number ; 
  maxContinuityGapMs: number ; 
  sourceMustBe: 'device ;' 
} 
 
export interface RuleBundle  { 
  id: string ; 
  effectiveFrom: string ; 
  pm10 { : 
    operational: Pm10OperationalBounds ; 
    regulatory: Pm10RegulatoryBounds ; 
    evidence: Pm10EvidenceBounds ; 
   ;} 
} 
 
function deepFreeze<T>(value: T): Readonly<T { > 
  Object.values(value as Record<string, unknown>).forEach((v { >= ) 
    if (v !== null && (typeof v === 'object' || typeof v === 'function') 
&& !Object.isFrozen(v))  { 
      deepFreeze(v) ; 
     } 
   ;)} 
  return Object.freeze(value) ; 
} 
  من الدليل اإليضاحي: النطاق  5.2 القسم  — ) operational حدود التشغيل (  /
/ التشغيلي المعتمد لمرقاب، منفصل عن الحكم التنظيمي الرسمي أدناه.  /
/ ) ALLOW → طبيعي (            150 ≤   /
/
//   150  < x ≤ 250    ( احتراز → MONITOR ) 
//   250  < x ≤ 320   ( ضوابط إلزامية → MONITOR ) 
//   320  < x ≤ 340   ( تقييد شديد → RESTRICT )  )، مؤكَّد PROTECTIVE_STOP → إيقاف احترازي فوري (           340 >     /
/
//                     ( MANDATORY_STOP > بعد استمرار ) دقيقة)   ٢ ثانية (  120 
// 
 مستقلة تماما ً عن األعلى:  — )  regulatory حدود الحكم التنظيمي ( // pm10 >= 250 تحذير تنظيمي:    /
/ pm10 > 340 تجاوز معل َّق فورا ً:     /
/ (دقيقتين) استمرار فعلي   ms 120000 ألكثر من  pm10 > 340 مخالفة مؤكدة:    /
/ متواصلة  ms 000 _ 800 _1 لمدة   pm10 >= 250 دقيقة:  30 بروتوكول تعليق    /
/
// 
// maxContinuityGapMs  (90    هامش منطقي بعد فترة تسجيل معتمدة ال )ثانية تتجاوز
  ليس دليل اتصال مستمر تلقائي؛ األربع دقائق   —) 82 دقيقة (الملحق ب، صفحة  /
/
// ( liveFreshnessMsInclusive  هي أقصى عمر قراءة للقرار الحي، ال فجوة ) استمرار.
 
export const RIYADH_DUST_2026_3: Readonly<RuleBundle> = deepFreeze {( 
  id: 'RCRC-NCEC-RIYADH-DUST-2026.3 ,' 
  effectiveFrom: '2026-08-04T00:00:00+03:00 ,' 
  pm10 { : 
    operational { : 
      normalMaxInclusive: 150 , 
      precautionMaxInclusive: 250 , 
      controlsMaxInclusive: 320 , 
      severeMaxInclusive: 340 , 
     ,} 
    regulatory { : 
      warningThresholdInclusive: 250 , 
      violationThresholdExclusive: 340 , 
      violationDurationMsExclusive: 120_000 , 
      suspensionThresholdInclusive: 250 , 
      suspensionDurationMsInclusive: 1_800_000 , 
     ,} 
    evidence { : 
      liveFreshnessMsInclusive: 240_000 , 
      maxContinuityGapMs: 90_000 , 
      sourceMustBe: 'device ,' 
     ,} 
   ,} 
 ;)} 
  بعد نشرها، فقط  RIYADH_DUST_2026_2/2026_3 ال تُعدَّل   —) Registry السجل ( /
/ تمثيل رمزي للحزمة  RIYADH_DUST_2026_2 ). 5.3 أضِف حزما ً جديدة هنا (القسم  /
/dust-compliance القديمة في   RULEBOOK_VERSION التاريخية ( /
/
engine/rulebook.ts )  محفوظة هنا للتوثيق فقط، ليست الحزمة النشطة حاليا ً.  — /
/
export const RIYADH_DUST_2026_2: Readonly<RuleBundle> = deepFreeze {( 
  id: 'RCRC-NCEC-RIYADH-DUST-2026.2 ,' 
  effectiveFrom: '2026-01-01T00:00:00+03:00 ,' 
  pm10 { : 
    operational { : 
      normalMaxInclusive: 200 , 
      precautionMaxInclusive: 250 , 
      controlsMaxInclusive: 339 , 
      severeMaxInclusive: 340 , 
     ,} 
    regulatory { : 
      warningThresholdInclusive: 251 , 
      violationThresholdExclusive: 340 , 
      violationDurationMsExclusive: 120_000 , 
      suspensionThresholdInclusive: 251 , 
      suspensionDurationMsInclusive: 1_800_000 , 
     ,} 
    evidence { : 
      liveFreshnessMsInclusive: 1_200_000 , 
      maxContinuityGapMs: 90_000 , 
      sourceMustBe: 'device ,' 
     ,} 
   ,} 
 ;)} 
 
export const RULE_BUNDLES   { = 
  ' RCRC-NCEC-RIYADH-DUST-2026.2': RIYADH_DUST_2026_2 , 
  ' RCRC-NCEC-RIYADH-DUST-2026.3': RIYADH_DUST_2026_3 , 
 } as const ; 
 لهذه الجلسة نفسها):   P0 طلب صريح من المستخدم (تراجع عن رفع اإلصدار في  /
/ عودة
 راجع النقاش الذي أدى لهذا القرار قبل   — كنشطة   2026.2 للحزمة القديمة  /
/ تعديل
  قد تُعتمَد  — تبقى معرَّفة أعاله (ال تُحذف   RIYADH_DUST_2026_3 هذا السطر.  /
/ . ACTIVE_RULE_BUNDLE الحقا ً)، لكنها لم تعد  /
/
export const ACTIVE_RULE_BUNDLE = RIYADH_DUST_2026_2 ; 
 
 
 
 
 
 
 
//  ============================================================= 
// Rule Bundle — RCRC-NCEC-RIYADH-DUST-2026.3  من "دليل اإلصالح الجذري  5.3 حزمة قواعد مجمَّدة وموث َّقة باإلصدار (القسم  /
/ ) عن الحكم operational تفصل النطاق التشغيلي لمرقاب ( — لمنظومة مرقاب")  /
/ واحدة كما كانت في  PM10 ) بدل خلطهما في حدود regulatory التنظيمي الرسمي ( /
/
// rulebook.ts ال تُعدَّل هذه الحزمة بعد نشرها؛ أي تغيير على الحدود يتطلب .  أدناه.  RULE_BUNDLES ...) تُضاف إلى 2026.4 حزمة جديدة ( /
/
//  ============================================================= 
 
export interface Pm10OperationalBounds  { 
  normalMaxInclusive: number ; 
  precautionMaxInclusive: number ; 
  controlsMaxInclusive: number ; 
  severeMaxInclusive: number ; 
} 
 
export interface Pm10RegulatoryBounds  { 
  warningThresholdInclusive: number ; 
  violationThresholdExclusive: number ; 
  violationDurationMsExclusive: number ; 
  suspensionThresholdInclusive: number ; 
  suspensionDurationMsInclusive: number ; 
} 
 
export interface Pm10EvidenceBounds   { 
  liveFreshnessMsInclusive: number ; 
  maxContinuityGapMs: number ; 
  sourceMustBe: 'device ;' 
} 
 
export interface RuleBundle  { 
  id: string ; 
  effectiveFrom: string ; 
  pm10 { : 
    operational: Pm10OperationalBounds ; 
    regulatory: Pm10RegulatoryBounds ; 
    evidence: Pm10EvidenceBounds ; 
   ;} 
} 
 
function deepFreeze<T>(value: T): Readonly<T { > 
  Object.values(value as Record<string, unknown>).forEach((v { >= ) 
    if (v !== null && (typeof v === 'object' || typeof v === 'function') 
&& !Object.isFrozen(v))  { 
      deepFreeze(v) ; 
     } 
   ;)} 
  return Object.freeze(value) ; 
} 
  من الدليل اإليضاحي: النطاق  5.2 القسم  — ) operational حدود التشغيل (  /
/ التشغيلي المعتمد لمرقاب، منفصل عن الحكم التنظيمي الرسمي أدناه.  /
/ ) ALLOW → طبيعي (            150 ≤   /
/
//   150  < x ≤ 250    ( احتراز → MONITOR ) 
//   250  < x ≤ 320   ( ضوابط إلزامية → MONITOR ) 
//   320  < x ≤ 340   ( تقييد شديد → RESTRICT )  )، مؤكَّد PROTECTIVE_STOP → إيقاف احترازي فوري (           340 >     /
/
//                     ( MANDATORY_STOP > بعد استمرار ) دقيقة)   ٢ ثانية (  120 
//  مستقلة تماما ً عن األعلى:  — )  regulatory حدود الحكم التنظيمي ( /
/ pm10 >= 250 تحذير تنظيمي:    /
/ pm10 > 340 تجاوز معل َّق فورا ً:     /
/ (دقيقتين) استمرار فعلي   ms 120000 ألكثر من  pm10 > 340 مخالفة مؤكدة:    /
/ متواصلة  ms 000 _ 800 _1 لمدة   pm10 >= 250 دقيقة:  30 بروتوكول تعليق    /
/
// 
// maxContinuityGapMs  (90    هامش منطقي بعد فترة تسجيل معتمدة ال )ثانية تتجاوز
  ليس دليل اتصال مستمر تلقائي؛ األربع دقائق   —) 82 دقيقة (الملحق ب، صفحة  /
/
// ( liveFreshnessMsInclusive  هي أقصى عمر قراءة للقرار الحي، ال فجوة ) استمرار.
 
export const RIYADH_DUST_2026_3: Readonly<RuleBundle> = deepFreeze {( 
  id: 'RCRC-NCEC-RIYADH-DUST-2026.3 ,' 
  effectiveFrom: '2026-08-04T00:00:00+03:00 ,' 
  pm10 { : 
    operational { : 
      normalMaxInclusive: 150 , 
      precautionMaxInclusive: 250 , 
      controlsMaxInclusive: 320 , 
      severeMaxInclusive: 340 , 
     ,} 
    regulatory { : 
      warningThresholdInclusive: 250 , 
      violationThresholdExclusive: 340 , 
      violationDurationMsExclusive: 120_000 , 
      suspensionThresholdInclusive: 250 , 
      suspensionDurationMsInclusive: 1_800_000 , 
     ,} 
    evidence { : 
      liveFreshnessMsInclusive: 240_000 , 
      maxContinuityGapMs: 90_000 , 
      sourceMustBe: 'device ,' 
     ,} 
   ,} 
 ;)} 
  بعد نشرها، فقط  RIYADH_DUST_2026_2/2026_3 ال تُعدَّل   —) Registry السجل ( /
/ تمثيل رمزي للحزمة  RIYADH_DUST_2026_2 ). 5.3 أضِف حزما ً جديدة هنا (القسم  /
/dust-compliance القديمة في   RULEBOOK_VERSION التاريخية ( /
/
engine/rulebook.ts )  محفوظة هنا للتوثيق فقط، ليست الحزمة النشطة حاليا ً.  — /
/
export const RIYADH_DUST_2026_2: Readonly<RuleBundle> = deepFreeze {( 
  id: 'RCRC-NCEC-RIYADH-DUST-2026.2 ,' 
  effectiveFrom: '2026-01-01T00:00:00+03:00 ,' 
  pm10 { : 
    operational { : 
      normalMaxInclusive: 200 , 
      precautionMaxInclusive: 250 , 
      controlsMaxInclusive: 339 , 
      severeMaxInclusive: 340 , 
     ,} 
    regulatory { : 
      warningThresholdInclusive: 251 , 
      violationThresholdExclusive: 340 , 
      violationDurationMsExclusive: 120_000 , 
      suspensionThresholdInclusive: 251 , 
      suspensionDurationMsInclusive: 1_800_000 , 
     ,} 
    evidence { : 
      liveFreshnessMsInclusive: 1_200_000 , 
      maxContinuityGapMs: 90_000 , 
      sourceMustBe: 'device ,' 
     ,} 
   ,} 
 ;)} 
 
export const RULE_BUNDLES   { = 
  ' RCRC-NCEC-RIYADH-DUST-2026.2': RIYADH_DUST_2026_2 , 
  ' RCRC-NCEC-RIYADH-DUST-2026.3': RIYADH_DUST_2026_3 , 
 } as const ; 
 لهذه الجلسة نفسها):   P0 طلب صريح من المستخدم (تراجع عن رفع اإلصدار في  /
/ عودة
 راجع النقاش الذي أدى لهذا القرار قبل   — كنشطة   2026.2 للحزمة القديمة  /
/ تعديل
  قد تُعتمَد  — تبقى معرَّفة أعاله (ال تُحذف   RIYADH_DUST_2026_3 هذا السطر.  /
/ . ACTIVE_RULE_BUNDLE الحقا ً)، لكنها لم تعد  /
/
export const ACTIVE_RULE_BUNDLE = RIYADH_DUST_2026_2 ; 
 
 
 
 
 
 
 
 
 
import { supabaseAdmin } from '@/app/lib/supabaseAdmin ;' 
import type { NormalizedReading } from '@/app/lib/providers/types ;' 
  مُستخرَج من  — منطق الكتابة المشترك ألي قراءة جهاز/محطة  /
/
// app/api/devices/ingest/route.ts  مسار( push ليُستخدَم أيضا ً من مسار )  ). التحقق من app/api/cron/provider-pull/route.ts, pull السحب الدوري (  /
/ ذرّي  RPC ؛ الكتابة الفعلية تُفوَّض بالكامل لـ TypeScript القيم يبقى هنا بـ /
/ / supabase/migrations ، راجع ingest_device_reading_atomic واحد ( /
/
// 202608020004 _ atomic_device_ingest.sql )—  أي تعديل مستقبلي على منطق  الكتابة (ال التحقق) يكون هناك، ال هنا.  /
/
//  من "دليل اإلصالح الجذري لمنظومة مرقاب"): بعد كتابة  8 إضافة (القسم  /
/
// ingest_device_reading_atomic  يغذ ّي    — (يبقى كما هو
device_readings_history / 
// pm10_readings_history/project_devices.last  ،)للمستهلكين القدامى *_ تُستدعى
  — ) بوقت رصد مستقل لكل حقل 202608040002 (  ingest_device_event_v2 أيضا ً  /
/
// device_metric_latest  هو مصدر الحقيقة الجديد لمسار القرار الحي 
// ( resolveFreshProjectDevice  في dustEvaluation.ts  فشل الكتابة الثانية .) ال
 يُسقِط األولى (القراءة القديمة محفوظة فعالً في كل الحاالت)، فقط يُسجَّل.  /
/
 
const MEASUREMENT_FIELDS  [ = 
  ' windSpeedKmh ,' 
  ' windGustKmh ,' 
  ' windDirectionDeg ,' 
  ' pm10 ,' 
  ' pm25 ,' 
  ' visibilityM ,' 
  ' relativeHumidityPercent ,' 
  ' temperatureC ,' 
 ] as const ; 
 
function validateValue(field: (typeof MEASUREMENT_FIELDS)[number], value: 
unknown): string | null  { 
  if (typeof value !== 'number' || !Number.isFinite(value)) return 
`${field}  ;`ً يجب أن يكون رقما 
  if (field === 'windDirectionDeg')   { 
    if (value < 0 || value > 360) return 'windDirectionDeg   يجب أن يكون '; 360 و  0 بين
 
    return null ; 
   } 
  if (field === 'relativeHumidityPercent')  { 
    if (value < 0 || value > 100) return 'relativeHumidityPercent    يجب أن '; 100 و  0 يكون بين
 
    return null ; 
   } 
  if (field === 'temperatureC')  { 
    if (value < -20 || value > 70) return 'temperatureC  يجب أن يكون بين  '; 70و 
20
    return null ; 
   } 
  if (value < 0) return `${field}  ;`ً ال يمكن أن يكون سالبا 
  return null ; 
} 
 
export interface WriteDeviceReadingParams  { 
  deviceId: string ; 
  projectId: string ; 
   // Partial مصدر : push (ingest/route.ts)   قد ال يرسل observedAtIso   ً إطالقا — 
   // writeDeviceReading  تتعامل مع غيابه بنفس منطق األصل (تستخدم وقت وصول  الجديدة يجب أن ترسله دائما ً فعليا ً.  pull // الخادم بدالً منه). مصادر  
 
  reading: Partial<NormalizedReading ;> 
   // idempotency key  —  بنفس دور eventId   في ingest/route.ts للمصادر . pull  `. }observedAtIso{$:}vendorStationId{$:}provider{ // يُبنى عادة كـ`$ 
 
  externalEventId?: string | null ; 
  sequence?: number | null ; 
} 
 
export type WriteDeviceReadingResult = { success: true; duplicate?: 
boolean } | { success: false; error: string } ; 
 
export async function writeDeviceReading(params: 
WriteDeviceReadingParams): Promise<WriteDeviceReadingResult { > 
  const { deviceId, projectId, reading, externalEventId = null, 
sequence: sequenceParam = null } = params ; 
 
  const rawValues: Record<(typeof MEASUREMENT_FIELDS)[number], number | 
undefined { = > 
    windSpeedKmh: reading.windSpeedKmh , 
    windGustKmh: reading.windGustKmh , 
    windDirectionDeg: reading.windDirectionDeg , 
    pm10: reading.pm10 , 
    pm25: reading.pm25 , 
    visibilityM: reading.visibilityM , 
    relativeHumidityPercent: reading.relativeHumidityPercent , 
    temperatureC: reading.temperatureC , 
   ;} 
 
  const measurements: Record<string, number ;}{ = > 
  for (const field of MEASUREMENT_FIELDS)  { 
    const value = rawValues[field] ; 
    if (value === undefined || value === null) continue ; 
    const validationError = validateValue(field, value) ; 
    if (validationError) return { success: false, error: validationError 
}; 
    measurements[field] = value ; 
   } 
 
  if (Object.keys(measurements).length === 0)  { 
    return  { success: false, error  يجب توفير قيمة واحدة على األقل من' : حقول القياس' };
 
   } 
 
  const receivedAt = new Date ;)( 
   // observedAtIso   أصبح إلزاميا ً في مسار devices/ingest/route.ts   عقد( الحدث
  غيابه هنا نظريا ً  — أيضا ً  pull // الكامل)، ويبقى مُرسَالً دائما ً من كل مصادر  
 دفاعي ألي استدعاء ال يمر عبر أي من المسارين). ال   fallback // فقط ( 
 
receivedAt  بخالف السلوك السابق الذي كان يستخدم   — متوفر  observedAtIso // أبدا ً حين  
 
   // receivedAt  .ً دائما 
  const observedAt = reading.observedAtIso ? new 
Date(reading.observedAtIso) : receivedAt ; 
  "عقد الحدث  — من "دليل اإلصالح الجذري لمنظومة مرقاب"  P0-8 / 5.10 // القسم  
  sequence يفرض  Push (devices/ingest/route.ts) ": مسار Push // كامل فقط في   
 
 (موصالت مثل   Provider Pull // رقما ً صحيحا ً غير سالب إلزاميا ً من العميل، لكن   
   // ThingsBoard " ال تملك مفهوم ) sequence ًتعطي فقط   — " أصال observedAtIso   لكل
 بال   RPC إلى الجدول والـ null فارغا ً (كان يمر  sequence_no // حقل. بدل ترك  
  أي
بالمللي   Unix نفسه (طابع  observedAt // رفض)، يُشتَق رقم بديل حتمي من  
  ثانية)
  () ألي Date.getTime يبقى غير سالب دائما ً ( — صراحة ً   sequence // متى لم يُمرَّر   
 موجب)، ويحافظ على ترتيب زمني صحيح بين قراءات    1970 // تاريخ حقيقي بعد  
  نفس
 عبر المقارنة،   late events الحقيقي: كشف  sequence // الجهاز (نفس الغرض من   
  ال
 // فقط الفرادة).  
 
  const sequence = sequenceParam ?? observedAt.getTime ;)( 
  راجع  — واحدة)  SQL ، معاملة RPC // كتابة ذرّية واحدة ( 
 
   // supabase/migrations/202608020004_atomic_device_ingest.sql  3 . يستبدل  _ device_readings ، إدراج  project_devices // استدعاءات شبكة منفصلة (تحديث  
 
   // history إدراج ، pm10_readings_history ) كانت تسمح بنجاح جزئي (فشل صامت  فقط) لو فشل استدعاء الحق بعد نجاح سابق.   console.error // في  
 
  const { data, error } = await 
supabaseAdmin.rpc('ingest_device_reading_atomic { ,' 
    p_project_id: projectId , 
    p_device_id: deviceId , 
    p_external_event_id: externalEventId , 
    p_sequence_no: sequence , 
    p_observed_at: observedAt.toISOString ,)( 
    p_received_at: receivedAt.toISOString ,)( 
    p_measurements: measurements , 
   ;)} 
 
  if (error) return { success: false, error: error.message } ; 
 
   // ingest_device_event_v2 نفس القياسات، لكن بوقت رصد مستقل لكل حقل : — 
   // reading.fields  إن وُجد، من موصالت مثل( ThingsBoard  هو المصدر؛ حقل ) غائب
  ال  push (توافق مع أجهزة  fallback المشترك أعاله كـ observedAt // منه يستخدم  
  واحدا ً للحمولة كلها). فشل هذه الكتابة ال يُسقِط  observedAtIso // تزال ترسل  
  // نجاح الكتابة األولى (القراءة محفوظة تاريخيا ً بغض النظر).  
 
  const measurementsV2: Record<string, { value: number; observedAtIso: 
string } ;}{ = > 
  for (const field of Object.keys(measurements))  { 
    const point = reading.fields?.[field as keyof 
NonNullable<NormalizedReading['fields']>] ; 
    measurementsV2[field]   { = 
      value: measurements[field] , 
      observedAtIso: point?.observedAtIso ?? observedAt.toISOString ,)( 
     ;} 
   } 
 
  const { error: v2Error } = await 
supabaseAdmin.rpc('ingest_device_event_v2 { ,' 
    p_project_id: projectId , 
    p_device_id: deviceId , 
    p_external_event_id: externalEventId , 
    p_sequence_no: sequence , 
    p_received_at: receivedAt.toISOString ,)( 
    p_measurements: measurementsV2 , 
   ;)} 
  if (v2Error)  { 
    console.error('writeDeviceReading: ingest_device_event_v2 failed:', 
v2Error.message) ; 
   } 
 
  return { success: true, duplicate: Boolean(data?.[0]?.is_duplicate) } ; 
} 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
{ 
  " crons [ :" 
     " { path": "/api/alerts/generate", "schedule": "0 3  } "* * * 
   ] 
} 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 --  ===================================================================== -- DCR — 202608040005_claim_evaluation_jobs.sql --  ===================================================================== --  باستخدام   -- العامل يطالب بدفعة  — من "دليل اإلصالح الجذري لمنظومة مرقاب"  10.2 القسم FOR UPDATE SKIP LOCKED  و Lease ال ينشئ ، Jobs   .بنفسه RPC   ال استعالمين منفصلين من التطبيق يفتحان نافذة سباق بين القراءة   -- ذرية( =====================================================================  -- والتحديث).
 
 
create or replace function public.claim_evaluation_jobs ( 
  p_worker_id uuid , 
  p_batch_size integer default 20 , 
  p_lease_seconds integer default 90 
) 
returns setof public.project_evaluation_jobs 
language sql 
security invoker 
set search_path = pg_catalog, public 
as  $$ 
  with candidates as  ( 
    select id 
    from public.project_evaluation_jobs 
    where status in ('PENDING', 'RETRY') 
      and run_after <= clock_timestamp )( 
      and (lease_until is null or lease_until < clock_timestamp()) 
    order by run_after, created_at 
    for update skip locked 
    limit greatest(1, p_batch_size) 
   ) 
  update public.project_evaluation_jobs j 
  set status = 'RUNNING ,' 
      worker_id = p_worker_id , 
      lease_until = clock_timestamp() + make_interval(secs => 
greatest(1, p_lease_seconds)) , 
      attempts = attempts + 1 
  from candidates c 
  where j.id = c.id 
  returning j ;*. 
 ;$$ 
 
revoke all on function public.claim_evaluation_jobs(uuid, integer, 
integer) 
  from public, anon, authenticated ; 
grant execute on function public.claim_evaluation_jobs(uuid, integer, 
integer) to service_role ;  هذا العامل).  Lease مهمة استرجعها عامل آخر بعد انتهاء  -- ما زال يطابق (ال تُنهي  worker_id ذرية، تتحقق من أن    — إنهاء مهمة بنجاح  --
 
create or replace function public.complete_evaluation_job ( 
  p_job_id uuid , 
  p_worker_id uuid 
) 
returns boolean 
language plpgsql 
security invoker 
set search_path = pg_catalog, public 
as  $$ 
declare 
  v_updated boolean ; 
begin 
  update public.project_evaluation_jobs 
  set status = 'SUCCEEDED ,' 
      completed_at = clock_timestamp ,)( 
      lease_until = null , 
      last_error = null 
  where id = p_job_id and worker_id = p_worker_id 
  returning true into v_updated ; 
 
  return coalesce(v_updated, false) ; 
end ; 
 ;$$ 
 
revoke all on function public.complete_evaluation_job(uuid, uuid) from 
public, anon, authenticated ; 
grant execute on function public.complete_evaluation_job(uuid, uuid) to 
service_role ;  ال يزال يجب تسجيل الخطأ ودفع الجدولة قدما ً بأمان).  — العامل فعليا ً  -- هذا  Lease (فشل قد يُسجَّل بعد انتهاء  worker_id نهائيا ً. ال يشترط  DEAD ثم  -- ، p_max_attempts دقائق) حتى   10 أ ُسّي (حد أقصى   Backoff مع  RETRY — فشل مهمة   --
 
create or replace function public.fail_evaluation_job ( 
  p_job_id uuid , 
  p_error text , 
  p_max_attempts integer default 5 
) 
returns void 
language plpgsql 
security invoker 
set search_path = pg_catalog, public 
as  $$ 
declare 
  v_attempts integer ; 
begin 
  select attempts into v_attempts from public.project_evaluation_jobs 
where id = p_job_id ; 
  if not found then 
    return ; 
  end if ; 
 
  if v_attempts >= p_max_attempts then 
    update public.project_evaluation_jobs 
    set status = 'DEAD ,' 
        last_error = p_error , 
        lease_until = null , 
        completed_at = clock_timestamp )( 
    where id = p_job_id ; 
  else 
    update public.project_evaluation_jobs 
    set status = 'RETRY ,' 
        last_error = p_error , 
        lease_until = null , 
        run_after = clock_timestamp() + make_interval(secs => least(600, 
power(2, v_attempts) * 5)) 
    where id = p_job_id ; 
  end if ; 
end ; 
 ;$$ 
 
revoke all on function public.fail_evaluation_job(uuid, text, integer) 
from public, anon, authenticated ; 
grant execute on function public.fail_evaluation_job(uuid, text, integer) 
to service_role ; 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
import { supabaseAdmin } from '@/app/lib/supabaseAdmin ;' 
import   { 
  computeDustResults , 
  computeDustComplianceResults , 
  persistActivityDecisionsAtomic , 
 } from '@/app/lib/dustEvaluation ;' 
import { buildSensitiveReceptor } from '@/app/utils/dust-compliance
engine ;' 
import { checkDustActivities } from '@/app/api/alerts/generate/route ;' 
   — )  DVI + Compliance + FinalDecision منطق إعادة تقييم مشروع كامل ( /
/ مُستخرَج
  ليُستدعى من مصدرين:  app/api/projects/[projectId]/evaluate/route.ts من  /
/ ، طلب requireUserId/verifyProjectOwnership نفسه (بعد   POST /evaluate (أ)   /
/ مباشرة   app/api/cron/provider-pull/route.ts من متصفح مستخدم مسجَّل)، (ب)  /
/ ال جلسة  — (استدعاء داخلي من كود خادم موثوق بعد سحب قراءة جديدة بنجاح   /
/ هنا مصدره صف  projectId ، وال حاجة لها: cron مستخدم متاحة في سياق الـ /
/
// provider_connections  .)الموجود فعالً بقاعدة البيانات، ال مدخل عميل 
//  route.ts هذه مسؤولية المستدعي ( — بال أي تحقق مصادقة/ملكية هنا عمدا ً   /
/ provider_connections.project_id لطلبات المستخدم، أو التحقق الضمني عبر  /
/ من أي مدخل خارجي).  project_id ، الذي ال يقبل أصالً cron للـ /
/
export interface EvaluateProjectResult  { 
  success: boolean ; 
  persisted: number ; 
  failedActivityIds?: string ;][ 
   // failedActivityIds  المحصورة في تعارض CAS  راجع تعليق  —  40001 (كود 
   // persist_activity_decision_atomic .وحده، بال أي فشل حقيقي آخر معها )  ) تحل ّها تلقائيا ً بإعادة cron المستخدم أو الـ refresh // دورة تقييم الحقة (  
  من  current_dust_decisions/current_dust_compliance_decisions // قراءة  
 // جديد؛ ال تستحق نفس درجة الخطورة أو نفس معالجة الفشل الحقيقي في   
 
route.ts . 
  conflictActivityIds?: string ;][ 
  error?: string ; 
} 
  — من "دليل اإلصالح الجذري لمنظومة مرقاب"   10.3 خطأ مكتشَف ومُصل َح (القسم   /
/ ؛ ال تسجل كل شيء evaluateProject "اجعل سبب الحفظ الحقيقي يمر إلى  /
/
// user_refresh كانت :)" evaluateProject  ' تُمرِّر user_refresh ً ثابتا ً دائما ' بصرف النظر عن المستدعي الفعلي    persistActivityDecisionsAtomic لـ /
/
( scheduler فيفقد    — دوري، حدث جهاز، سحب مزوّد، أو طلب مستخدم حقيقي)  /
/
dust_evaluations / 
// dust_compliance_evaluations.triggered_by  أي قيمة تشخيصية حقيقية لتمييز  ' يحافظ user_refresh اختياري (افتراضي '  triggeredBy مصدر كل قرار مخزَّن.   /
/ على التوافق مع كل االستدعاءات القديمة التي لم تُحدِّث بعد).  /
/
export async function evaluateProject ( 
  projectId: string , 
  triggeredBy: string = 'user_refresh ' 
 :) Promise<EvaluateProjectResult { > 
  try  { 
    const { data: project } = await supabaseAdmin 
       . from('projects') 
       . select )'*'( 
       . eq('id', projectId) 
       . maybeSingle ;)( 
    if (!project)  { 
      return  { success: false, persisted: 0, error  'المشروع غير موجود' :
;} 
     }  مراجعة كود خبير خارجي: "المشروع المؤرشف يمكن أن   — // خطأ تشغيلي مكتشَف    
  والتقييم". خط دفاع ثانٍ هنا (باإلضافة الستبعاد  Provider pull // يستمر في    
 
      // provider-pull/route.ts  لالتصاالت التابعة لمشروع مؤرشف من استعالمه  المباشر  push تُستدعى أيضا ً من مسار  evaluateProject — // أصالً)     
 
    ( // devices/ingest/route.ts الذي ال يمر عبر ) provider_connections  // إطالقا ً، فال يستفيد من ذلك االستبعاد؛ فشل آمن هنا بدل االستمرار.    
 
    if (project.archived_at)  { 
      return { success: true, persisted: 0 } ; 
     } 
  : يربط كل دورة تقييم فعلية بمعرّف واحد، Evaluation Run — 11.2 // القسم    
  هو لحظة بداية هذه الدورة تحديدا ً  as_of // بصرف النظر عن مصدر التحفيز.    
  يسمح الحقا ً بإعادة إنتاج "ما كانت الحالة عليه  — // (ال وقت الحفظ الحقا ً)     
  // وقت هذا التقييم بالضبط" حتى لو تغيّرت البيانات بعده.    
 
    const asOf = new Date().toISOString ;)( 
    const { data: runRow } = await supabaseAdmin 
       . from('evaluation_runs') 
       . insert({ project_id: projectId, trigger_type: 
normalizeTriggerType(triggeredBy), as_of: asOf, status: 'RUNNING' }) 
       . select('id') 
       . single ;)( 
    const evaluationRunId = runRow?.id ?? null ; 
 
    const [{ data: dustProfiles }, { data: projectShifts }] = await 
Promise.all [( 
       // archived_at is null —   نشاط مؤرشَف (راجع DELETE   في
app/api/activities / 
       // route.ts  األرشفة حل ّت محل الحذف الفعلي) يجب أال يدخل دورة تقييم ، حية
  // جديدة، رغم بقاء أدلته التاريخية قابلة للقراءة دائما ً في مكان آخر.      
 
       
supabaseAdmin.from('project_dust_profiles').select('*').eq('project_id', 
projectId).is('archived_at', null) , 
      supabaseAdmin.from('project_shifts').select('*').eq('project_id', 
projectId).order('sort_order', { ascending: true }) , 
     ;)] 
    project.shifts = projectShifts ;][ || 
 
    const dustResults = await computeDustResults(dustProfiles || [], 
project, supabaseAdmin) ; 
    if (dustResults.length === 0)  { 
      await completeEvaluationRun(evaluationRunId, { status: 
'SUCCEEDED', activitiesEvaluated: 0, activitiesFailed: 0 }) ; 
      return { success: true, persisted: 0 } ; 
     } 
 
    const { data: sensitiveReceptorRows, error: sensitiveReceptorsError 
} = await supabaseAdmin 
       . from('sensitive_receptors') 
       . select('id, name, receptor_type, lat, lng') ; 
    if (sensitiveReceptorsError)  { 
      await completeEvaluationRun(evaluationRunId, { status: 'FAILED', 
error: sensitiveReceptorsError.message }) ; 
      return { success: false, persisted: 0, error: 
sensitiveReceptorsError.message } ; 
     } 
    const sensitiveReceptors = (sensitiveReceptorRows || 
[]).map(buildSensitiveReceptor) ; 
 
    const dustComplianceResults = await computeDustComplianceResults ( 
      dustProfiles  ,][ || 
      project , 
      dustResults , 
      sensitiveReceptors , 
      supabaseAdmin , 
      true 
     ;) 
 
    const persistResults = await persistActivityDecisionsAtomic ( 
      supabaseAdmin , 
      projectId , 
      dustResults , 
      dustComplianceResults , 
      triggeredBy , 
      triggeredBy , 
      evaluationRunId 
     ;) 
 
    const allFailedActivityIds = persistResults.filter((r) => 
r.failed).map((r) => r.activityId) ; 
  مراجعة كود خبير خارجي: "التنبيهات ال تُنشأ مباشرة   — // خطأ تشغيلي مكتشَف    
  // بعد نجاح حفظ القرار، وقد تخلط قرارا ً قديما ً مع تقييم جديد". قبل هذا،    
 
      // checkDustActivities  مولّد التنبيهات) كان يُستدعى فقط على جدول زمني( generate دقائق من المتصفح عبر  5 يومي، أو كل  Cron // منفصل تماما ً (   
 
mine ) فقد يقرأ التنبيه قرارا ً   — // بمعزل عن لحظة حفظ القرار الفعلية هنا    
  محفوظا ً
  // منذ ساعات بينما تغيّرت الحالة الفعلية للتو. اإلصالح: استدعاء    
 
      // checkDustActivities([projectId])   فورا ً هنا بعد نجاح الحفظ (لو نجح  // نشاط واحد على األقل)، مقيَّدا ً بهذا المشروع فقط (نفس آلية التصفية    
  يقرأ حصريا ً من  — ) generate-mine/route.ts // المستخدَمة أصالً في    
 
      // current_dust_compliance_decisions/final_decisions   المحفوظة للتو (ال من الصفر)، فيغلق الفارق الزمني بين   DVI/Compliance // إعادة حساب    
  "الحفظ"
  دقائق. فشل التنبيه ال  5 // و"التنبيه" ألجزاء من الثانية بدل يوم كامل أو    
  يُسجَّل فقط،  — // يُسقِط نجاح الحفظ نفسه (القرار محفوظ فعالً بغض النظر)     
  . provider-pull/route.ts // نفس مبدأ معالجة الفشل الجزئي في    
 
    const persistedCount = dustResults.length - 
allFailedActivityIds.length ; 
    if (persistedCount > 0)  { 
      await checkDustActivities([projectId]).catch((err { >= ) 
        const message = err instanceof Error ? err.message : String(err) ; 
        console.error(`evaluateProject: checkDustActivities failed for 
project ${projectId}:`, message) ; 
       ;)} 
     } 
 
    if (allFailedActivityIds.length > 0) { 
      const conflictActivityIds = persistResults.filter((r) => r.failed 
&& r.conflict).map((r) => r.activityId) ; 
      const nonConflictFailedActivityIds = persistResults.filter((r) => 
r.failed && !r.conflict).map((r) => r.activityId) ; 
      await completeEvaluationRun(evaluationRunId { , 
        status: nonConflictFailedActivityIds.length > 0 ? 'FAILED' : 
'SUCCEEDED ,' 
        activitiesEvaluated: persistedCount , 
        activitiesFailed: allFailedActivityIds.length , 
        error : 
          nonConflictFailedActivityIds.length > 0  ? 'فشل حفظ سلسلة القرار كاملة لبعض األنشطة'             
  أعد المحاولة',  — لبعض األنشطة  CAS : 'تعارض             
 
       ;)} 
      return  { 
        success: false , 
        persisted: persistedCount , 
        failedActivityIds: nonConflictFailedActivityIds , 
        conflictActivityIds , 
        error : 
          nonConflictFailedActivityIds.length > 0  ? 'فشل حفظ سلسلة القرار كاملة لبعض األنشطة'             
  أعد المحاولة',  — لبعض األنشطة  CAS : 'تعارض             
 
       ;} 
     } 
  : يُحدَّث فقط عند last_successful_project_evaluation_at — 10.3 // القسم    
  / scheduler-tick // نجاح تقييم فعلي كامل (ال مجرد إنشاء المهمة، راجع    
 
      // route.ts   الذي يحدّث last_heartbeat_at  .)وحده evaluation_lag_seconds  . heartbeat الـ  endpoint // يُشتق الحقا ً من الفارق بين اآلن وهذا العمود في    
 
    await supabaseAdmin 
       . from('scheduler_heartbeat') 
       . update({ last_successful_project_evaluation_at: new 
Date().toISOString )( }) 
       . eq('id', true) ; 
 
    await completeEvaluationRun(evaluationRunId { , 
      status: 'SUCCEEDED ,' 
      activitiesEvaluated: dustResults.length , 
      activitiesFailed: 0 , 
     ;)} 
    return { success: true, persisted: dustResults.length } ; 
   } catch (error { ) 
    const message = error instanceof Error ? error.message : 
String(error) ; 
    return { success: false, persisted: 0, error: message } ; 
   } 
} 
 
// trigger_type  في evaluation_runs  مقيَّد بنفس قيم project_evaluation_jobs 
// (SCHEDULED/DEVICE_EVENT/PROVIDER_PULL/USER_REQUEST) — triggeredBy   الممرَّر
  ') user_refresh نص حر أوسع (مثال: ' evaluateProject فعليا ً من مستدعيات  /
/ القديم. هذه الدالة  dust_evaluations.triggered_by ألسباب توافقية مع  /
/ ألي نص  USER_REQUEST تُطابِق النص الحر مع أقرب قيمة مقيَّدة، فشل آمن نحو  /
/ بأكمله لمجرد نص جديد).  evaluation_runs غير معروف (ال نرمي خطأ ً يُسقِط حفظ  /
/
function normalizeTriggerType(triggeredBy: string): 'SCHEDULED' | 
'DEVICE_EVENT' | 'PROVIDER_PULL' | 'USER_REQUEST { ' 
  const value = triggeredBy.toLowerCase ;)( 
  if (value.includes('scheduler') || value.includes('scheduled')) return 
'SCHEDULED ;' 
  if (value.includes('device') || value.includes('ingest')) return 
'DEVICE_EVENT ;' 
  if (value.includes('provider') || value.includes('pull')) return 
'PROVIDER_PULL ;' 
  return 'USER_REQUEST ;' 
} 
 
async function completeEvaluationRun ( 
  evaluationRunId: string | null , 
  outcome: { status: 'SUCCEEDED' | 'FAILED'; activitiesEvaluated?: 
number; activitiesFailed?: number; error?: string } 
 :) Promise<void { > 
  if (!evaluationRunId) return ; 
  try  { 
    await supabaseAdmin 
       . from('evaluation_runs') 
       . update {( 
        status: outcome.status , 
        activities_evaluated: outcome.activitiesEvaluated ?? 0 , 
        activities_failed: outcome.activitiesFailed ?? 0 , 
        error: outcome.error ?? null , 
        completed_at: new Date().toISOString ,)( 
       )} 
       . eq('id', evaluationRunId) ; 
   } catch (err { ) 
    console.error '( completeEvaluationRun  فشل تحديث : evaluation_runs:', 
err ;) 
   } 
} 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
import { lookup } from 'node:dns/promises ;' 
import { isIP } from 'node:net ;' 
  provider_instances خارجي (اآلن يأتي حصرا ً من   base_url ألي   SSRF حماية  /
/ من  15.1 راجع القسم   — المعتمدة من مسؤول النظام، ال من إدخال مستخدم حر  /
/ بال هذا الفحص، أي طلب للمنصة   — "دليل اإلصالح الجذري لمنظومة مرقاب")  /
/ meta-data لخدمة   169.254.169.254 يقدر يستهدف عناوين شبكة داخلية ( /
/ الخاصة) بدل منصة   x/172.16-31.x/192.168.x . 10 ، شبكات localhost السحابية،  /
/ testConnection/listStations/fetchLatestReading حقيقية. يُستدعى عند   /
/ DNS جميعا ً، ال عند الحفظ فقط (رابط قد يكون آمنا ً وقت الحفظ ثم يُغيَّر  /
/ ). DNS rebinding — الحقا ً  /
/
 
const BLOCKED_HOSTNAMES = new Set(['localhost', '0.0.0.0', '::1', 
'[::1]']) ; 
 
function isPrivateOrReservedIPv4(ip: string): boolean  { 
  const parts = ip.split('.').map(Number) ; 
  if (parts.length !== 4 || parts.some((p) => Number.isNaN(p))) return 
true ; 
  const [a, b] = parts ; 
  if (a === 10) return true; // 10.0.0.0/8 
  if (a === 127) return true; // loopback 
  if (a === 169 && b === 254) return true; // link-local  —  يشمل metadata   السحابية
 
  if (a === 172 && b >= 16 && b <= 31) return true; // 172.16.0.0/12 
  if (a === 192 && b === 168) return true; // 192.168.0.0/16 
  if (a === 0) return true; // 0.0.0.0/8 
   // CGNAT  ( 100.64.0.0 /10 ) —  من التقرير اإليضاحي.   18.7 القسم 
  if (a === 100 && b >= 64 && b <= 127) return true ; 
   // Multicast ( 224.0.0.0 /4 و ) Documentation (192.0.2.0/24, 
198.51.100.0/24 ,  Reserved (240.0.0.0/4 .) ) و 24 / 203.0.113.0 //  
 
  if (a >= 224) return true ; 
  if (a === 192 && b === 0) return true ; 
  if (a === 198 && b === 51) return true ; 
  if (a === 203 && b === 0) return true ; 
  return false ; 
} 
 
function isPrivateOrReservedIPv6(ip: string): boolean  { 
  const normalized = ip.toLowerCase ;)( 
  if (normalized === '::1' || normalized === '::') return true ; 
  if (normalized.startsWith('fe80:')) return true; // link-local 
  if (normalized.startsWith('fc') || normalized.startsWith('fd')) return 
true; // unique local (fc00::/7) 
  if (normalized.startsWith('ff')) return true; // multicast (ff00::/8) 
 
   // IPv4-mapped IPv6 (::ffff:a.b.c.d)  —   يُفحَص الجزء IPv4   .ً المُضمَّن أيضا  () new URL ) ممكنة من مصادر خام، لكن ffff:127.0.0.1 // الصيغة المنقوطة (:: 
  كال الشكلين يُفحَصان.   — hex groups (::ffff:7f00:1) // يُطبِّعها لصيغة  
 
  const mappedDotted = normalized.match(/^::ffff:(\d+\.\d+\.\d+\.\d+)$/) ; 
  if (mappedDotted && isPrivateOrReservedIPv4(mappedDotted[1])) return 
true ; 
 
  const mappedHex = normalized.match(/^::ffff:([0-9a-f]{1,4}):([0-9a
f]{1,4})$/) ; 
  if (mappedHex)   { 
    const hi = parseInt(mappedHex[1], 16) ; 
    const lo = parseInt(mappedHex[2], 16) ; 
    const dotted = [(hi >> 8) & 0xff, hi & 0xff, (lo >> 8) & 0xff, lo & 
0xff].join ;)'.'( 
    if (isPrivateOrReservedIPv4(dotted)) return true ; 
   } 
 
  return false ; 
} 
 
function isPrivateOrReservedIP(ip: string): boolean   { 
  const family = isIP(ip) ; 
  if (family === 4) return isPrivateOrReservedIPv4(ip) ; 
  if (family === 6) return isPrivateOrReservedIPv6(ip) ; 
  return true  ليس // ; IP  ًفشل آمن نحو الرفض  — صالحا ً أصال 
} 
 
export class UnsafeExternalUrlError extends Error  { 
  constructor(message: string)   { 
    super(message) ; 
    this.name = 'UnsafeExternalUrlError ;' 
   } 
} 
 يرمي   —  fetch يتحقق من رابط قبل أي استخدام فعلي في   /
/
UnsafeExternalUrlError  فعليا ً (ال يكتفي بفحص الشكل النصي) لمنع اسم نطاق عام   DNS عند الرفض. يحل  /
/ "منع  — 15.1 فقط اآلن (القسم  https .)DNS rebinding( داخلي  IP يُحلّ إلى  /
/
// http  مطلقا ً؛ ال ترسل اسم مستخدم وكلمة مرور عبر http .)" 
export async function assertSafeExternalUrl(rawUrl: string): 
Promise<void { > 
  let parsed: URL ; 
  try  { 
    parsed = new URL(rawUrl) ; 
   } catch  { 
    throw new UnsafeExternalUrlError ;)'رابط المنصة غير صالح'( 
   } 
 
  if (parsed.protocol !== 'https:')   { 
    throw new UnsafeExternalUrlError  رابط المنصة يجب أن يكون'( https   فقط');
 
   } 
 
  if (parsed.username || parsed.password)  { 
    throw new UnsafeExternalUrlError  ال يجوز تضمين اسم مستخدم/كلمة مرور'( داخل الرابط نفسه');
 
   } 
 
  if (parsed.port && parsed.port !== '443')  { 
    throw new UnsafeExternalUrlError فقط');   443 ('المنفذ المسموح 
   } 
 
  const hostname = parsed.hostname.toLowerCase().replace(/^\[|\]$/g, '') ; 
  if (BLOCKED_HOSTNAMES.has(hostname) || hostname.endsWith('.localhost'))  
{ 
    throw new UnsafeExternalUrlError  رابط المنصة يشير لعنوان شبكة داخلي'( غير مسموح');
 
   } 
 
   // hostname   قد يكون IP  بال( ً حرفيا DNS  ) —  .يُفحص مباشرة بال استعالم 
  if (isIP(hostname) !== 0)  { 
    if (isPrivateOrReservedIP(hostname)) { 
      throw new UnsafeExternalUrlError  رابط المنصة يشير لعنوان شبكة'( داخلي غير مسموح');
 
     } 
    return ; 
   } 
 
  let addresses: string ;][ 
  try  { 
    const results = await lookup(hostname, { all: true, verbatim: true 
}) ; 
    addresses = results.map((r) => r.address) ; 
   } catch  { 
    throw new UnsafeExternalUrlError ;)'تعذ ّر تحليل اسم نطاق رابط المنصة'( 
   } 
 
  if (addresses.length === 0 || addresses.some(isPrivateOrReservedIP))   { 
    throw new UnsafeExternalUrlError  رابط المنصة يشير لعنوان شبكة داخلي'( غير مسموح');
 
   } 
} 
  ": ترك TOCTOU/DNS rebinding " — 18.7 / 15.1 خطأ أمني مكتشَف ومُصل َح (القسم   /
/
// fetch   يحل االسم مرة أخرى بعد assertSafeExternalUrl  يبقي فجوة؛ fetch )(  ) تلقائيا ً بال أي فحص على الوجهة الجديدة). redirect أيضا ً يتبع تحويالت ( /
/
// safeFetch  تحل محل fetch المباشر لكل استدعاء )( Connector  :خارجي  — ) قبل كل محاولة اتصال assertSafeExternalUrl . تتحقق من الرابط (نفس 1   /
/  Location ' + فحص كل redirect: 'manual بما فيها كل قفزة تحويل (      /
/ يدويا ً
  بمتابعتها تلقائيا ً بال تحقق).  fetch قبل اتّباعه، بدل السماح لـ      /
/ ). AbortSignal.timeout قصير ( Timeout . تفرض 2   /
/ ) لمنع حلقة تحويل ال 5 . تُحدِّد حدا ً أقصى لعدد التحويالت المتبوعة ( 3   /
/ نهائية.       /
/
const MAX_REDIRECTS = 5 ; 
const DEFAULT_TIMEOUT_MS = 7000 ; 
 
export async function safeFetch ( 
  url: string , 
  init: RequestInit & { timeoutMs?: number }  }{ = 
 :) Promise<Response { > 
  let currentUrl = url ; 
  const { timeoutMs = DEFAULT_TIMEOUT_MS, ...fetchInit } = init ; 
 
  for (let redirectCount = 0; redirectCount <= MAX_REDIRECTS; 
redirectCount++)   { 
    await assertSafeExternalUrl(currentUrl) ; 
 
    const response = await fetch(currentUrl { , 
      ... fetchInit , 
      redirect: 'manual ,' 
      signal: AbortSignal.timeout(timeoutMs) , 
      cache: 'no-store ,' 
     ;)} 
  SSRF صراحة ً (نفس فحص  Location نتحقق من    — تُعامَل كتحويل  3xx // ردود     
  يتبعه تلقائيا ً بال تحقق (كان هذا  fetch // الكامل) قبل اتّباعه، بدل ترك    
 
 : العنوان األول آمن وقت الفحص، لكن التحويل يوجّه TOCTOU // بالضبط ثغرة      // فعليا ً لعنوان داخلي لم يُفحَص إطالقا ً).   
 
    if (response.status >= 300 && response.status < 400)   { 
      const location = response.headers.get('location') ; 
      if (!location)  { 
        throw new UnsafeExternalUrlError استجابة تحويل بال رأس'( Location   صالح');
 
       } 
      currentUrl = new URL(location, currentUrl).toString ;)( 
      continue ; 
     } 
 
    return response ; 
   } 
 
  throw new UnsafeExternalUrlError ;)'عدد التحويالت تجاوز الحد المسموح'( 
} 
 
 
 