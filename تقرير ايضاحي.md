 dcr-app (7 `) `  — # دليل اإلصالح الجذري لمنظومة مرقاب 
     2026 أغسطس  4 **التاريخ:**
  **المخاطب:** المبرمج المسؤول عن النسخة التالية  
 `، مع بيان  NO-GO **النطاق:** إصالح العيوب التي أبقت النسخة السابعة في حالة 
`السبب الجذري، التصميم المستهدف، مواضع التعديل، شكل الكود والبيانات، 
  المهاجرات الجديدة، واختبارات القبول.
 
 ` وعلى الملحق التنظيمي  work/dcr7 > هذا الدليل مبني على السورس الفعلي في `
 . ال يمثل اعتمادًا تنظيميًا من الجهة  82 المرفق، خصوصًا الملحق (ب) في الصفحة
  المختصة.
 
 
 --- 
  . النتيجة المطلوبة من اإلصالح 1 ##
 
  ال نريد فقط أن تصبح االختبارات خضراء. النتيجة المقبولة هي منظومة تحقق اآلتي:
 
  . كل قراءة تحتفظ بوقت رصدها الحقيقي، مستقالً لكل حقل. 
1 . القيمة القديمة تبقى ظاهرة تاريخيًا، لكنها ال تدخل القرار الحي. 
2 وال يعتمد عليه.  Open-Meteo . القرار الحي ال ينتظر 
3
4 ` . RESTRICT `ال يتحول إلى مراقبة، و ` STOP .ال يتحول إلى مراقبة `*_  . نطاقات مرقاب التشغيلية منفصلة عن الحكم التنظيمي الرسمي. 
5يوقف النشاط احترازيًا، وال يصبح مخالفة مؤكدة   340 اللحظي فوق  PM10 . تجاوز 
6 إال بدليل استمرار صالح.
  . القرار والتنبيه ينشآن من اللقطة نفسها ويرتبطان بمعرف واحد. 
7 . المشروع المؤرشف ال يقبل قراءة أو تقييمًا أو قرارًا جديدًا حتى تحت التزامن. 
8 ` متشابه. activity_group_id . ال يمكن خلط نشاطين من مشروعين بسبب ` 
9`، وال تفقد  TRUNCATE ` أو ` DELETE ` أو ` UPDATE . األدلة التاريخية ال تقبل ` 
10 رابطها بالنشاط.
 
11 . Scheduler  مثبت تشغيليًا، وله Queue و Lease و Retry  و Heartbeat  ؛ ليس مجرد
Route  .موجود في الكود   E2E و DB بالترتيب وتنجح في اختبارات  migrations . قاعدة جديدة تطبق جميع  
12 والتزامن.
 
 ` إذا تم إصالح واجهة العرض فقط أو تغيير توقعات  GO الحكم ال يتحول إلى 
` االختبارات لتوافق السلوك الخطأ.
 
 
 --- 
  . األسباب الجذرية المشتركة 2 ##
 
  األخطاء الحالية ليست اثني عشر خطأ ً مستقالً؛ معظمها ناتج من خمس مشكالت معمارية:
 
  انهيار المعاني داخل فئة واحدة  2.1 ###
 
 محرك القرار جمع المراقبة والتقييد والتحقق واإليقاف داخل فرع واحد ينتهي 
 المتقاربة استُخدمت كأنها قرارات تشغيلية   AEI `. السبب أن تصنيفات MONITOR بـ`
 الحسابي ال يساوي قرار الموقع.   AEI متساوية. سقف
 
  فقدان هوية القياس الزمنية  2.2 ###
 
 النظام يخزن قيمًا متعددة في حمولة واحدة ثم يمنحها وقتًا واحدًا. هذا صحيح فقط 
 إذا كانت كل الحساسات أخذت العينة في اللحظة نفسها، وهو غير صحيح في 
 
ThingsBoard   .عند جلب آخر نقطة لكل مفتاح 
  خلط القرار الحي بالتخطيط  2.3 ###
 
 
الدالة نفسها تجلب التوقعات، تبني شبكة الساعات، ثم تبني القرار الحي. لذلك   يتأخر القرار الميداني أو يفشل بسبب مزود توقعات ال يفترض أن يكون جزءًا منه.
 
  حفظ أجزاء الواقعة في معامالت منفصلة 2.4 ###
 
 القرار يُحفظ أوالً، ثم يعاد حساب بعض البيانات إلنشاء التنبيه في مسار آخر. 
  النتيجة: قرار بال تنبيه، أو تنبيه من لقطة مختلفة، أو تنبيه مكرر.
 
  االعتماد على كود التطبيق لضمانات يجب أن تكون في قاعدة البيانات  2.5 ###
 
 عزل المشروع، منع التشغيل بعد األرشفة، الذرية، وعدم تعديل األدلة يجب أن 
 واحد ال يحمي بقية   Route . فحصها في PostgreSQL داخل  RPCs تفرضها قيود و
 المسارات وال سباقات التزامن.
 
 
 --- 
  . المعمارية المستهدفة 3 ##
 
 
``` mermaid 
flowchart TD 
    A  جهاز"[ Push   أو Provider Pull  ]"--  > B"[ Ingest RPC  حدث + قياسات : بأوقاتها"]
 
    B --> C "[ Latest Projection   ]"لكل حقل 
    B --> Q "[ Evaluation Job  ]"دائم 
    S "[ Scheduler   ]"مراقب-- > Q 
    Q --> L "[ Live Evaluator  ]"بال شبكة 
    L --> R["Rulebook + Final Resolver"] 
    R --> T + معاملة: قرار"[ Audit + Alert Outbox ]" 
    T --> N["Alert Worker"] 
    W "[ Forecast Fetcher  ]"مستقل--  > F["Forecast Snapshots"] 
    F --> P "[ Planning Engine  ]"فقط 
 ``` 
  Live أو غيره بين `  Open-Meteo إلى  HTTP قاعدة التصميم: ال توجد أي مكالمة
 
Evaluator + و`قرار ` Outbox .` 
 
 --- 
  PM10 منطق القرار و — # الجزء األول
 
  . إصالح محرك القرار النهائي 4 ##
 
  الخطأ الحالي  4.1 ###
 
  الملف:
 
 
` app/utils/final-decision-engine/engine.ts 247– 207 `، تقريبًا األسطر . 
  `: MONITOR الفئات التالية مجمعة في فرع واحد يعيد 
`
 
``` text 
FIELD_VERIFICATION_REQUIRED 
ALLOW_WITH_CONTROLS 
PRECAUTION 
ALLOW_WITH_MONITORING 
RESTRICT 
RESTRICT_SEVERE 
STOP_DUST_GENERATING_ACTIVITIES 
STOP_VISIBILITY_DEPENDENT_ACTIVITIES 
 ``` 
  لذلك تتحول أوامر التقييد واإليقاف الصريحة إلى السماح بالعمل مع المراقبة.
 
  لماذا تغيير خمسة شروط فقط ليس حالً جذريًا؟  4.2 ###
 
 ` الحالية تسمح بتكرار المشكلة عند إضافة فئة جديدة. كما  if/else ألن سلسلة 
` أن النص واللون وقابلية التجاوز تُشتق أحيانًا من مصدر لم يفز بالقرار الفعلي.
 
 شاملة   TypeScript الحل هو بناء مرشحين مستقلين ثم اختيار األشد، مع خرائط
  تجبر المترجم على إظهار أي فئة جديدة غير مصنفة.
 
  التنفيذ المقترح  4.3 ###
 
 
``` ts 
const OPERATION_RANK  { = 
  ALLOW: 0 , 
  MONITOR: 1 , 
  RESTRICT: 2 , 
  HOLD_FOR_VERIFICATION: 3 , 
  PROTECTIVE_STOP: 4 , 
  MANDATORY_STOP: 5 , 
 } as const satisfies Record<OperationalDecision, number ;> 
 
const DVI_TO_OPERATION  { = 
  ALLOW: 'ALLOW ,' 
  ALLOW_WITH_MONITORING: 'MONITOR ,' 
  RESTRICT: 'RESTRICT ,' 
  RESTRICT_SEVERE: 'RESTRICT ,' 
  STOP_DUST_GENERATING_ACTIVITIES: 'PROTECTIVE_STOP ,' 
  STOP_VISIBILITY_DEPENDENT_ACTIVITIES: 'PROTECTIVE_STOP ,' 
  MANDATORY_STOP: 'MANDATORY_STOP ,' 
 } as const satisfies Record<DviDecisionCategory, OperationalDecision ;> 
 
function complianceCandidate ( 
  compliance: DustComplianceResult | null 
 :) OperationalDecision  { 
  if (!compliance) return 'HOLD_FOR_VERIFICATION ;' 
 
  switch (compliance.decisionCategory)  { 
    case 'ALLOW :' 
      return 'ALLOW ;' 
    case 'PRECAUTION :' 
    case 'ALLOW_WITH_CONTROLS :' 
      return 'MONITOR ;' 
    case 'FIELD_VERIFICATION_REQUIRED :' 
      return 'HOLD_FOR_VERIFICATION ;' 
    case 'RESTRICT_ACTIVITY :' 
      return 'RESTRICT ;' 
    case 'STOP_AFFECTED_ACTIVITY :' 
      return compliance.pendingConfirmation 
        ' ? PROTECTIVE_STOP ' 
        ' : MANDATORY_STOP ;' 
    case 'MANDATORY_STOP :' 
      return 'MANDATORY_STOP ;' 
    default : 
      return assertNever(compliance.decisionCategory) ; 
   } 
} 
 
function strictest(candidates: DecisionCandidate[]): DecisionCandidate   { 
  return candidates.reduce((winner, current >= ) 
    OPERATION_RANK[current.decision] > OPERATION_RANK[winner.decision] 
       ? current 
       : winner 
   ;) 
} 
 ``` 
  المرشحون المطلوبون:
 
 
``` ts 
const candidates: DecisionCandidate [ = ][ 
   { source: 'DVI', decision: DVI_TO_OPERATION[dvi.decisionCategory]  ,} 
   { source: 'COMPLIANCE', decision: complianceCandidate(compliance)  ,} 
   { 
    source: 'EVIDENCE ,' 
    decision: evidenceBlocksDecision 
      ' ? HOLD_FOR_VERIFICATION ' 
      ' : ALLOW ,' 
   ,} 
;] 
 
const winner = strictest(candidates) ; 
 ``` 
  ال تستدل على سبب اإليقاف من نص كود قاعدة  4.4 ###
 
  الكود الحالي يبحث عن:
 
 
` DVI-DUST-ACTIVITY-STOP-004-PM10-ONLY ` 
 فقط. هذا ربط هش بين   PM10 ` لمعرفة أن اإليقاف سببه triggeredRules داخل 
` : DVI إلى نتيجة  Typed محركين. يجب إضافة حقول
 
 
``` ts 
type StopBasis  = 
  ' | NONE ' 
  ' | PM10 ' 
  ' | VISIBILITY ' 
  ' | WIND ' 
  ' | MIXED ;' 
 
type ConfirmationState  = 
  ' | NOT_APPLICABLE ' 
  ' | PENDING ' 
  ' | CONFIRMED ;' 
 
interface DviEvaluationResult  {  // الحقول الحالية...  
 
  stopBasis: StopBasis ; 
  confirmationState: ConfirmationState ; 
} 
 ``` 
 
` للتفسير  reasonCodes هذه الحقول مباشرة. تبقى ` Final Decision ثم يستخدم  والتدقيق، ال للتحكم الخفي في مسار البرنامج.

  اشتقاق الحقول المكررة من القرار الفائز  4.5 ###
 
  ال تسمح للحقول التالية أن تتناقض:
 
 - ` operationalDecision ` - ` mandatoryStop ` - ` pendingConfirmation ` - ` overridable ` - ` level ` - ` decisionLabelAr ` 
  واحدة:  Factory أنشئ
 
 
``` ts 
function buildFinalDecisionFromWinner ( 
  winner: DecisionCandidate , 
  context: FinalDecisionContext 
 :) FinalDecision   { 
  const mandatoryStop = winner.decision === 'MANDATORY_STOP ;' 
  const pendingConfirmation  = 
    context.confirmationState === 'PENDING ;' 
 
  return  { 
     ... // 
    operationalDecision: winner.decision , 
    mandatoryStop , 
    pendingConfirmation , 
    overridable : 
       ! mandatoryStop && winner.sourceAllowsOverride === true , 
    shortReasonAr: winner.reasonAr , 
    decisionLabelAr: labelFor(winner) , 
    level: levelFor(winner) , 
   ;} 
} 
 ``` 
 
` PROTECTIVE_STOP  ال يعني دائمًا أن اإلثبات التنظيمي معل ّق؛ قد يكون إيقافًا ``  confirmationState . لذلك اجعل ` PM10 تشغيليًا بسبب ضعف الرؤية ال يحتاج عداد
  ` من اسم القرار وحده. pendingConfirmation=true مستقالً، وال تشتق `

  االختبارات التي يجب تعديلها  4.6 ###
 
 ` توجد اختبارات تتوقع  app/utils/final-decision-engine/engine.test.ts في 
`` للفئات الخطرة. ال تعدّل اختبار القبول المستقل ليمر؛ عدّل هذه  MONITOR صراحة `
  االختبارات القديمة لتتوقع السلوك الصحيح.
 
 ، ويثبت أن القرار  Compliance و DVI يمر على جميع قيم   Exhaustive أضف اختبارًا 
  النهائي ال يكون أخف من المرشح األشد.
 
 
 --- 
  . فصل نطاقات مرقاب التشغيلية عن الحكم التنظيمي 5 ##
 
  المرجع  5.1 ###
 
 
 ، يثبت اآلتي: 82 الملحق (ب)، الصفحة ` ألكثر من دقيقتين تُسجل مخالفة ويجب  µg/m³  340 قيمة ` PM10 إذا **تجاوز**  - `. µg/m³ 250 مستوى التحذير عند ` -
  لمدة دقيقة واحدة، وفترة تسجيل ال تتجاوز دقيقة.  PM10 بيانات  - دقيقة يُتبع بروتوكول التعليق.  30 إذا استمر التجاوز  - إيقاف األعمال المسببة للغبار.
 
 ` وغيرها فهي سياسة مرقاب التشغيلية المعتمدة للمشروع،  150–0 أما نطاقات `
 . يجب أال يقدّمها النظام على أنها حد  82 وليست نصًا تنظيميًا حرفيًا من الصفحة
  مخالفة صادر من الجهة.
 
  النموذج الصحيح  5.2 ###
 
  الواحدة يجب أن تحتوي مسارين منفصلين:  PM10 نتيجة
 
 
``` ts 
interface Pm10PolicyResult  { 
  operationalBand : 
     ' | NORMAL ' 
     ' | PRECAUTION ' 
     ' | CONTROLS ' 
     ' | SEVERE_RESTRICTION ' 
     ' | PROTECTIVE_STOP ;' 
 
  regulatoryState : 
     ' | BELOW_WARNING_LEVEL ' 
     ' | WARNING_LEVEL ' 
     ' | VIOLATION_PENDING_CONFIRMATION ' 
     ' | VIOLATION_CONFIRMED ;' 
} 
 ``` 
  النطاق التشغيلي المقترح بالقيم المستمرة، وليس باألعداد الصحيحة فقط:
 
 
 | PM10   | نطاق مرقاب التشغيلي | القرار التشغيلي األدنى | 
|---|:---|--- |  ` | ALLOW ` | طبيعي | ` 150 | `<
= ` | MONITOR ` | احتراز | ` 250 && <= 150 | `
> ` مع ضوابط إلزامية | MONITOR ` | ضوابط | ` 320 && <= 250 | `
> ` | RESTRICT ` | تقييد شديد | ` 340 && <= 320 | `
> ` فورًا | PROTECTIVE_STOP ` | تجاوز | ` 340 | `
>
  الحكم التنظيمي يعمل بالتوازي:
 
 - ` pm10 >= 250 .تحذير تنظيمي :` - ` pm10 > 340 .تجاوز معل ّق فورًا :` - ` pm10 > 340 ` 120000 ` مع استمرار صالح ألكثر من ms .دقيقة يجب أن يحمل في حزمة القواعد معامل المقارنة المعتمد   30 بروتوكول  - `: مخالفة مؤكدة `) وأال يُشتق ضمنيًا من أسماء متناقضة. 250 ` أو `> 250 صراحة (`>=

 ` يمكن أن تكون **احترازًا تشغيليًا** وفي الوقت  250 النتيجة المهمة: قراءة 
` نفسه **مستوى تحذير تنظيمي**. ال يوجد تناقض ألن الحقلين مختلفان.
 
  حزمة قواعد غير قابلة للتعديل  5.3 ###
 
 `  dust-engine ` و` dustEvaluation.ts ` و` rulebook.ts ال تترك األرقام موزعة في 
` `. rulesCatalog.ts و`

  أنشئ ملفًا مثل:
 
 
` app/utils/rule-bundles/riyadh-dust-2026.3.ts ` 
 
``` ts 
export const RIYADH_DUST_2026_3 = deepFreeze {( 
  id: 'RCRC-NCEC-RIYADH-DUST-2026.3 ,' 
  effectiveFrom: '2026-08-04T00:00:00+03:00 ,' 
  pm10 { : 
    operational { : 
      normalMaxInclusive: 150 , 
      precautionMaxInclusive: 250 , 
      controlsMaxInclusive: 320 , 
      severeMaxInclusive: 340 , 
     ,} 
    regulatory { : 
      warningThresholdInclusive: 250 , 
      violationThresholdExclusive: 340 , 
      violationDurationMsExclusive: 120_000 , 
      suspensionThresholdInclusive: 250 , 
      suspensionDurationMsInclusive: 1_800_000 , 
     ,} 
    evidence { : 
      liveFreshnessMsInclusive: 240_000 , 
      maxContinuityGapMs: 90_000 , 
      sourceMustBe: 'device ,' 
     ,} 
   ,} 
 ;)} 
 ``` 
 
` maxContinuityGapMs  يجب أن يتوافق مع فترة تسجيل الجهاز المعتمدة. بما أن `ثانية منطقي بعد   90 تطلب فترة ال تتجاوز دقيقة، فإن هامشًا مثل  82 الصفحة 
 االختبار الميداني. ال تعتمد أربع دقائق كفجوة استمرار إذا كانت المحطة يفترض 
 تلقائيًا دليل   أن تسجل كل دقيقة؛ األربع دقائق هي أقصى عمر للقرار، وليست
 اتصال مستمر.
 
  ` بعد نشرها: 2026.2 للحزم، وال تعدّل ` Registry أنشئ
 
 
``` ts 
export const RULE_BUNDLES   { = 
  ' RCRC-NCEC-RIYADH-DUST-2026.2': RIYADH_DUST_2026_2 , 
  ' RCRC-NCEC-RIYADH-DUST-2026.3': RIYADH_DUST_2026_3 , 
 } as const ; 
 ``` 
  خزّن مع القرار:
 
 - ` rule_bundle_version ` - ` rule_bundle_hash ` - ` engine_version ` - ` input_snapshot_hash ` - ` evaluated_at ` 
 ويُولد كتالوج القواعد في الواجهة من الحزمة نفسها؛ ال تنسخ النص والحدود 
  `. rulesCatalog.ts يدويًا في `

 
 --- 
  PM10 . إعادة بناء دليل استمرار 6 ##
 
  الصحيح الموجود حاليًا ويجب االحتفاظ به  6.1 ###
 
  ` توجد أفكار صحيحة: computeSustainedPm10Status في 
` أكثر من دقيقتين ال دقيقتان بالضبط.  - `. 340 ` ال `>= Open-Meteo . - >` 340 عدم تأكيد المخالفة من  - قطع السلسلة عند تغير المصدر.  - المدة بين عينات فعلية، ال بين عينة قديمة والوقت الحالي.  - عينة واحدة ال تثبت االستمرار.  - استخدام وقت الرصد ال وقت الوصول.  -
 
  األخطاء الباقية  6.2 ###
 القيمة الحالية وسلسلة االستمرار يمكن أن تأتي من استعالمين أو توقيتين   -
 ` في حاسب السلسلة ال تطابق دائمًا العتبة التشغيلية في  250 العتبة ` - مختلفين.
 
Rulebook . -  .الدالة تستخدم `  - القراءة القديمة قد تبني حالة إيقاف مؤقت قبل أن تُسقطها طبقة جودة الدليل Date.now .بعض الصفوف تُجلب على مستوى المشروع ثم تُفلتر في  - ()` ضمنيًا، ما يضعف إعادة إنتاج القرار التاريخي JavaScript . 
  كائن دليل واحد  6.3 ###
 
 
``` ts 
interface Pm10EvidenceWindow  { 
  asOfIso: string ; 
  current { : 
    valueUgM3: number ; 
    observedAtIso: string ; 
    deviceId: string ; 
    eventId: string ; 
    source: 'device ;' 
   | } null ; 
  operationalBand: Pm10OperationalBand | null ; 
  currentFresh: boolean ; 
  homogeneousDevice: boolean ; 
  sampleCount: number ; 
  durationAbove340Ms: number ; 
  durationAtOrAbove250Ms: number ; 
  violationConfirmed: boolean ; 
  suspensionConfirmed: boolean ; 
  reasonCodes: string ;][ 
} 
 ``` 
  يُبنى هذا الكائن من استعالم واحد مقيد بـ:
 
 
``` sql 
where project_id = :project_id 
  and device_id = :device_id 
  and source = 'device ' 
  and observed_at >= :window_start 
order by observed_at desc 
 ``` 
  ` وحده. activity_group_id وال يقبل 
`
  قواعد اإلثبات:
 
 
``` ts 
violationConfirmed  = 
  current.valueUgM3 > 340   && 
  currentFresh  && 
  homogeneousDevice  && 
  sampleCount >= 2  && 
  durationAbove340Ms > 120_000 ; 
 ``` 
 ` تصبح مؤكدة إذا استوفت  ms 120001 ` بالضبط تبقى معلقة. عند ` ms 120000 عند 
` بقية الشروط.
 
  إذا انتهت حداثة القراءة:
 إذا كان القرار السابق إيقافًا، ال يبدأ االستئناف حتى تصل قراءات حديثة آمنة   - `. HOLD_FOR_VERIFICATION القرار يصبح ` - `. ALLOW ال تتحول إلى ` - ال تبقى مخالفة حية مؤكدة من دون عينة حديثة.  -
  وتكتمل مدة االستقرار المطلوبة.
 
 
 --- 
  عقد القياس والحداثة   — # الجزء الثاني
 
  . فصل لقطة العرض عن لقطة القرار 7 ##
 
  الخطأ الحالي  7.1 ###
 
 
` FreshDeviceReading ` يحتوي أوقاتًا مستقلة، لكن ` buildDustInput  ال يمرر إال `` يأخذ كل القيم  buildDeviceMergedReading . ثم ` PM10 الوقت العام ووقت 
 ` للعرض فقط، في حين  DEVICE_READING_FRESHNESS_MINUTES=20 القديمة كما هي. `
  المطلوب للقرار أربع دقائق لكل حقل.
 
  نموذج القياس  7.2 ###
 
 
``` ts 
type MetricStatus  = 
  ' | FRESH ' 
  ' | STALE ' 
  ' | MISSING ' 
  ' | FUTURE_TIMESTAMP ' 
  ' | INVALID ;' 
 
interface MetricObservation  { 
  value: number | null ; 
  observedAtIso: string | null ; 
  receivedAtIso: string | null ; 
  ageMs: number | null ; 
  status: MetricStatus ; 
  source: 'device' | 'manual' | 'forecast ;' 
  qualityFlags: string ;][ 
} 
 
interface DeviceSnapshot  { 
  deviceId: string ; 
  projectId: string ; 
  asOfIso: string ; 
  metrics: Record<Metric, MetricObservation ;> 
} 
 ``` 
 
``` ts 
const LIVE_FRESHNESS_MS = 4 * 60_000 ; 
 
function classifyMetric ( 
  value: number | null , 
  observedAtIso: string | null , 
  receivedAtIso: string | null , 
  asOfMs: number 
 :) MetricObservation  { 
  if (value === null || !observedAtIso)  { 
    return { value, observedAtIso, receivedAtIso, ageMs: null , 
      status: 'MISSING', source: 'device', qualityFlags ;} ][ : 
   } 
 
  const observedMs = Date.parse(observedAtIso) ; 
  if (!Number.isFinite(observedMs))   { 
    return { value, observedAtIso, receivedAtIso, ageMs: null , 
      status: 'INVALID', source: 'device', qualityFlags: ['INVALID_TIME']  
;} 
   } 
 
  const ageMs = asOfMs - observedMs ; 
  if (ageMs < 0)   { 
    return { value, observedAtIso, receivedAtIso, ageMs , 
      status: 'FUTURE_TIMESTAMP', source: 'device', qualityFlags: 
['CLOCK_SKEW']  ;} 
   } 
 
  return  { 
    value , 
    observedAtIso , 
    receivedAtIso , 
    ageMs , 
    status: ageMs <= LIVE_FRESHNESS_MS ? 'FRESH' : 'STALE ,' 
    source: 'device ,' 
    qualityFlags ,][ : 
   ;} 
} 
 
function decisionValue(m: MetricObservation): number | null  { 
  return m.status === 'FRESH' ? m.value : null ; 
} 
 ``` 
 `  decisionValue(m) ` والعمر. المحرك يقرأ ` status ` مع ` m.value الواجهة تعرض 
` فقط.
 
  قواعد مهمة  7.3 ###
 
 
- PM10   .الحرارة الحديثة ال تجعل  - حديث ال يجعل الرياح القديمة حديثة PM10  .انتهاء حداثة البيانات ال يزيل قرار إيقاف سابق تلقائيًا؛ يتحول إلى   - الرؤية القديمة المرتفعة ال تثبت أن العمل آمن اآلن.  - الرؤية القديمة المنخفضة ال تثبت أن الخطر ما زال قائمًا اآلن.  - القديم حديثًا Hold  اتجاه الرياح ال يدخل تحليل االنتشار إال إذا كان توجيه الشمال الحقيقي   - حتى إثبات االستئناف ببيانات حديثة.
 ` واجعل اتجاه الرياح  qualityFlags ` إلى ` TRUE_NORTH_UNVERIFIED موثق ًا؛ أضف `
  غير مؤهل للتحليل المكاني عند وجوده.
 
  وحده PM10 جودة الدليل ال تعتمد على  7.4 ###
 
 
` deriveEvidenceQuality ` في ` final-decision-engine/adapters.ts  يفحص أساسًا `دقيقة. يجب استبداله بتقييم يعتمد على الحقول التي   20 وبنافذة   PM10 وقت
  استخدمها القرار فعالً:
 
 
``` ts 
interface EvidenceAssessment  { 
  overall: 'OK' | 'PARTIAL' | 'STALE' | 'UNAVAILABLE ;' 
  requiredMetrics: Metric ;][ 
  freshMetrics: Metric ;][ 
  staleMetrics: Metric ;][ 
  missingMetrics: Metric ;][ 
  reasonCodes: string ;][ 
} 
 ``` 
 إذا كان النشاط يعتمد على الرؤية، فالرؤية حقل حرج. إذا كانت قاعدة الرياح 
  هي سبب القرار، فالرياح حقل حرج. ال تستخدم قائمة ثابتة واحدة لكل األنشطة.
 
 
 --- 
  ThingsBoard و Push . إصالح عقد 8 ##
 
  شكل الحدث الجديد  8.1 ###
 
 
``` ts 
interface NormalizedMetricPoint  { 
  value: number ; 
  observedAtIso: string ; 
  sourcePointId: string ; 
  unit: string ; 
} 
 
interface NormalizedDeviceEventV2  { 
  schemaVersion: 2 ; 
  eventId: string ; 
  sequence: number ; 
  fields: Partial<Record<Metric, NormalizedMetricPoint ;>> 
} 
 ``` 
  مثال:
 
 
``` json 
{ 
  " schemaVersion": 2 , 
  " eventId": "station-4-98122 ," 
  " sequence": 98122 , 
  " fields { :" 
    " pm10 { :" 
       " value": 341.7 , 
       " observedAtIso": "2026-08-04T06:10:00.000Z ," 
       " sourcePointId": "station-4:pm10:1754287800000 ," 
       " unit": "ug/m3 " 
     ,} 
    " temperatureC { :" 
       " value": 43.2 , 
       " observedAtIso": "2026-08-04T06:10:55.000Z ," 
       " sourcePointId": "station-4:temperature:1754287855000 ," 
       " unit": "C " 
     } 
   } 
} 
 ``` 
 
 ### 8.2 ThingsBoard 
 ` العام. احتفظ بنقطة مستقلة  latestTs ` احذف ` thingsboardConnector.ts في 
` لكل مفتاح:
 
 
``` ts 
reading.fields[metric]  { = 
  value: latest.value , 
  observedAtIso: new Date(latest.ts).toISOString ,)( 
  sourcePointId : 
    ` thingsboard:${vendorStationId}:${tbKey}:${latest.ts} ,` 
  unit: unitFor(metric) , 
;} 
 ``` 
 . النقطة بال وقت موثوق تسجل  timestamp ()` بديالً عند غياب  new Date ال تستخدم 
` كغير صالحة أو تُرفض، وال تدخل القرار.
 
  نموذج قاعدة البيانات المقترح  8.3 ###
 
  القديمة:  migrations جديدة، ال تعدّل  migration نفّذ ذلك في
 
 
``` sql 
create table public.device_events  ( 
  id uuid primary key default gen_random_uuid ,)( 
  project_id uuid not null , 
  device_id uuid not null , 
  external_event_id text not null , 
  sequence_no bigint not null check (sequence_no >= 0) , 
  payload_hash text not null , 
  source text not null , 
  received_at timestamptz not null default clock_timestamp ,)( 
  unique (device_id, external_event_id) , 
  foreign key (project_id, device_id) 
    references public.project_devices(project_id, id) 
    on delete restrict 
;) 
 
create table public.device_measurements  ( 
  id uuid primary key default gen_random_uuid ,)( 
  event_id uuid not null references public.device_events(id) on delete 
restrict , 
  project_id uuid not null , 
  device_id uuid not null , 
  metric text not null , 
  value numeric not null , 
  unit text not null , 
  observed_at timestamptz not null , 
  received_at timestamptz not null , 
  source_point_id text not null , 
  quality_status text not null default 'VALID ,' 
  unique (device_id, metric, source_point_id) , 
  check (metric in  ( 
    ' windSpeedKmh','windGustKmh','windDirectionDeg ,' 
    ' pm10','pm25','visibilityM ,' 
    ' relativeHumidityPercent','temperatureC ' 
   )) 
;) 
 
create table public.device_metric_latest ( 
  project_id uuid not null , 
  device_id uuid not null , 
  metric text not null , 
  value numeric not null , 
  observed_at timestamptz not null , 
  received_at timestamptz not null , 
  source_measurement_id uuid not null 
    references public.device_measurements(id) on delete restrict , 
  primary key (project_id, device_id, metric) 
;) 
 ``` 
 
` device_metric_latest  هو ` Projection   سريع قابل إلعادة البناء، أما
` device_events/device_measurements .فهما الدليل التاريخي ` 
  `: eventId عند تكرار 
``؛ ال تعتبره  EVENT_ID_REUSE_WITH_DIFFERENT_PAYLOAD إذا اختلف: ارفض بـ` - `. duplicate=true ` مطابق ًا: ` payload_hash إذا كان ` -
 
Retry  .صحيحًا 
  الحدث المتأخر:
  ` وال يطلق قرارًا حيًا. historicalOnly=true يُوسم ` - دقيقة.   40 ال يُرفض لمجرد أن عمره  - ` إذا كان أقدم. device_metric_latest ال يستبدل `  - يُحفظ تاريخيًا.  -
 
  الحدث المستقبلي فوق هامش الساعة المسموح يُرفض أو يعزل كحدث غير صالح.
 
  دورية التسجيل  8.4 ###
 
 ` يقول إن دورة اإلرسال التصميمية دقيقتان.  devices/ingest/route.ts تعليق `
 يطلب بيانات دقيقة واحدة وفترة تسجيل ال تتجاوز   82 الملحق (ب) في الصفحة
  دقيقة. يجب فصل أمرين:
  **دورية القياس/التسجيل:** ال تتجاوز دقيقة وفق المرفق.  -
 
:** شبكة أمان إلعادة التقييم، ويمكن أن تكون كل دقيقتين  Scheduler **دورية  
- عند وصول كل قراءة.  Event-driven إذا بقي التقييم
 
 إذا بقي الجهاز يرسل كل دقيقتين، فيجب توثيق استثناء رسمي قبل تقديم النظام 
  أسرع ال يصنع عينات لم يقسها الجهاز.  Scheduler كسجل امتثال؛ وجود
 
 
 --- 
  التشغيل والتوقعات والتنبيهات   — # الجزء الثالث
 
  Planning عن  Live . فصل 9 ##
 
  موضع الخلط الحالي  9.1 ###
 
 
` computeDustResults :يستدعي ` 
 - ` evaluateDustVisibilityWindow ` - ` ثم evaluateDustVisibilityWorkDayHourly ` 
 ثانية في أسوأ    20 قرابة  Open-Meteo حتى في التقييم الحي. المسار قد ينتظر
  محاوالت الشبكة الحالية.
 
  الخدمات الجديدة  9.2 ###
 
 
``` ts 
evaluateLiveOperationalDecision ( 
  activity , 
  deviceSnapshot , 
  previousState , 
  asOf 
) 
 
evaluatePlanningForecast ( 
  activity , 
  forecastSnapshot , 
  asOf 
) 
 ``` 
  األولى:
 
 - Pure   أو قريبة من Pure . - ` بال fetch .` -  `تعتمد لقطة جهاز واحدة و asOf .تحفظ قرار ` - ` واحدًا LIVE_OPERATIONAL .` 
  الثانية:
  فشلها ال يؤخر أو يغير القرار الحي.  - ال تنتج مخالفة تنظيمية مؤكدة.  - ` فقط. PLANNING تنتج ` - محفوظًا مسبق ًا.  Forecast تقرأ   -
 
  تعديالت الملفات:
 
 - ` app/lib/dustEvaluation.ts ` تقسيم :` computeDustResults  إلى ` Live   . Planning 
و
- ` app/utils/dust-engine/engine.ts إضافة دالة :` Live   ال تستورد
` weather.ts .` - ` app/lib/evaluateProject.ts تشغيل :` Live  .فقط وعدم استدعاء توقعات - ` app/utils/dust-engine/weather.ts استخدامه في :` Forecast Worker   .فقط - ` app/api/alerts/generate/route.ts .منع أي إعادة حساب للقرار الحي :` 
 ` يرمي فور استدعائه، وشغّل  global.fetch على ` Mock : ضع  CI اختبار حاسم في
 
` evaluateLiveOperationalDecision  يجب أن ينجح االختبار ويثبت أن عدد .` ` يساوي صفرًا. fetch مكالمات `

 
 --- 
 
 ## 10  . Scheduler   دائم وقابل لالسترداد 
  لماذا الجدول الحالي غير كافٍ؟  10.1 ###
 
 
` scheduler_locks(project_id,time_bucket) يمنع تكرارًا داخل ` Bucket :لكنه ،  أو طلب المستخدم.  Push مع  Scheduler ال يمنع تداخل  - يعالج كل المشاريع في طلب واحد تسلسليًا.  - يتضخم بال تنظيف.  - . Lease ال يملك  - ال يعيد المحاولة بعد فشل التقييم.  - يُستدعى فعليًا.  Route ال يثبت أن   -
 
 
 ### 10.2 Queue 
 
``` sql 
create table public.project_evaluation_jobs  ( 
  id uuid primary key default gen_random_uuid ,)( 
  project_id uuid not null references public.projects(id) on delete 
restrict , 
  dedupe_key text not null , 
  trigger_type text not null check (trigger_type in   ( 
    ' SCHEDULED','DEVICE_EVENT','PROVIDER_PULL','USER_REQUEST ' 
   ,)) 
  status text not null default 'PENDING' check (status in  ( 
    ' PENDING','RUNNING','RETRY','SUCCEEDED','DEAD ' 
   ,)) 
  run_after timestamptz not null default clock_timestamp ,)( 
  attempts integer not null default 0 , 
  worker_id uuid , 
  lease_until timestamptz , 
  last_error text , 
  created_at timestamptz not null default clock_timestamp ,)( 
  completed_at timestamptz , 
  unique (project_id, dedupe_key) 
;) 
 ``` 
  FOR UPDATE SKIP فقط. العامل يطالب بدفعة باستخدام `  Jobs ينشئ  Scheduler ال
ـ
LOCKED و ` Lease : 
 
``` sql 
with candidates as  ( 
  select id 
  from public.project_evaluation_jobs 
  where status in ('PENDING','RETRY') 
    and run_after <= clock_timestamp )( 
    and (lease_until is null or lease_until < clock_timestamp()) 
  order by run_after, created_at 
  for update skip locked 
  limit 20 
) 
update public.project_evaluation_jobs j 
set status = 'RUNNING ,' 
    worker_id = gen_random_uuid ,)( 
    lease_until = clock_timestamp() + interval '90 seconds ,' 
    attempts = attempts + 1 
from candidates c 
where j.id = c.id 
returning j ;*. 
 ``` 
  بعد الفشل:
 
 - ` RETRY مع ` Backoff . - ` بعد حد المحاوالت DEAD .وتنبيه تشغيلي ` - Lease  .منتهية تسمح لعامل آخر باسترداد المهمة 
  اإلثبات التشغيلي  10.3 ###
 
  ومقاييس:  Heartbeat أضف
 
 
``` text 
scheduler_last_heartbeat_at 
jobs_due 
jobs_running 
jobs_dead 
evaluation_lag_seconds 
last_successful_project_evaluation_at 
 ``` 
 أكثر من ثالث دقائق يظهر إنذار تشغيلي. ال تقبل عبارة   Heartbeat إذا غاب
  "الخدمة الخارجية ستستدعي الرابط" من دون إعداد منشور واختبار ومراقبة.
 
 `; ال تسجل كل شيء  evaluateProject اجعل سبب الحفظ الحقيقي يمر إلى 
`
` user_refresh .` 
 
 --- 
  . القرار والتنبيه في لقطة واحدة 11 ##
 
  الخطأ الحالي  11.1 ###
 
 `. هذه  checkDustActivities ` دالة ` evaluateProject بعد حفظ القرار، يستدعي 
`، ثم تمزج ذلك مع آخر قرار مخزن. كما أن  DVI الدالة تعيد جلب الجهاز وتشغيل
 
` alertExists `و ` insertAlert ` منفصلتان، وإنشاء ` alerts  ` ` منفصالن. alert_state_events و`

 
 ### 11.2 Evaluation Run 
 
``` sql 
create table public.evaluation_runs   ( 
  id uuid primary key default gen_random_uuid ,)( 
  project_id uuid not null references public.projects(id) on delete 
restrict , 
  evaluation_job_id uuid references public.project_evaluation_jobs(id) , 
  trigger_type text not null , 
  as_of timestamptz not null , 
  input_snapshot_hash text not null , 
  status text not null , 
  started_at timestamptz not null default clock_timestamp ,)( 
  completed_at timestamptz 
;) 
 ``` 
  `: final_decisions أضف إلى 
`
 - ` evaluation_run_id ` - ` dvi_evaluation_id ` - ` compliance_evaluation_id ` - ` required_actions jsonb ` - ` input_snapshot_hash ` - ` rule_bundle_hash ` 
 
 ### 11.3 Outbox 
 
``` sql 
create table public.decision_alert_outbox  ( 
  id uuid primary key default gen_random_uuid ,)( 
  evaluation_run_id uuid not null references public.evaluation_runs(id) , 
  final_decision_id uuid not null references public.final_decisions(id) , 
  project_id uuid not null references public.projects(id) , 
  activity_id uuid not null references public.project_dust_profiles(id) , 
  kind text not null , 
  action text not null check (action in ('OPEN','CLOSE','UPDATE')) , 
  payload jsonb not null , 
  status text not null default 'PENDING ,' 
  attempts integer not null default 0 , 
  available_at timestamptz not null default clock_timestamp ,)( 
  locked_until timestamptz , 
  processed_at timestamptz , 
  last_error text , 
  unique (final_decision_id, kind, action) 
;) 
 ``` 
  `: persist_activity_decision_atomic داخل 
`
  . DVI . إدراج 
1 . Compliance . إدراج 
2 `. final_decision_id وإرجاع ` Final Decision . إدراج 
3 . Outbox في   Alert Intents . إدراج 
4
5 . Commit  .واحد 
 تتراجع المعاملة؛ ال يوجد قرار جديد بال نية تنبيه مرتبطة به.   Outbox إذا فشل
  ويعاد.  Outbox الحق ًا ال يلغي القرار؛ يبقى صف   Email أو  Push فشل إرسال
 
  Pure مولّد التنبيه يصبح  11.4 ###
 
 
``` ts 
function deriveAlertIntents ( 
  finalDecision: FinalDecision , 
  previousFinalDecision: FinalDecision | null 
 :) AlertIntent { ][  . DVI ، ال إعادة حساب  Open-Meteo ، ال  fetch ، ال DB // ال   
 
} 
 ``` 
 ` في خدمة تذكيرات تخطيطية مستقلة،  BEFORE_2H/BEFORE_1H/BEFORE_START يبقى 
` وال يختلط بتنبيهات القرار الحي.
 
  حي يجب أن يحمل:  Alert كل
 
 - ` opened_by_final_decision_id ` - ` evaluation_run_id ` - ` input_snapshot_hash ` 
 ` مرة أخرى؛ التسجيل يحدث فقط عند حدث  onsite_pm10 وال يسجل مولّد التنبيهات 
` قياس حقيقي.
 
  إنشاء التنبيه وانتقال حالته ذريان أيضًا  11.5 ###
 
  `؛ فهذا سباق. أضف insertAlert ` ثم ` alertExists ال ينفذ `  Outbox عامل
 
` dedupe_key بــ ` Unique Index  واربط التنبيه بالقرار النهائي، ثم استخدم ،
RPC  واحدة 
` create_alert_atomic ` تنفذ إدراج ` alerts ` وحدث الحالة األول ` NEW  في ` المعاملة
  ` الموجود بدل إنشاء نسخة ثانية. alert_id نفسها. إعادة المفتاح نفسه تعيد 
`
  صف  RPC ` من المتصفح. يجب أن تقفل  previous_state ال يقبل مسار تغيير الحالة `
  التنبيه وتقرأ حالته الحقيقية ثم تكتب الحدث:
 
 
``` sql 
select state into v_previous_state 
from public.alert_current_state 
where alert_id = p_alert_id 
for update ; 
 
insert into public.alert_state_events  ( 
  alert_id, previous_state, new_state, actor_user_id 
 ) values  ( 
  p_alert_id, v_previous_state, p_new_state, auth.uid )( 
;) 
 ``` 
  وبذلك ينتج طلبان متزامنان سلسلة صحيحة بدل انتقالين يدّعي كالهما أنه بدأ من
 =...` المباشر؛  UPDATE alerts SET state الحالة القديمة نفسها. يجب أن يفشل 
` حالة
  التنبيه تُشتق من سجل األحداث فقط.
 
 
 --- 
  قاعدة البيانات والعزل واألمان   — # الجزء الرابع
 
  . عزل النشاط داخل المشروع 12 ##
 
  الخطأ الحالي  12.1 ###
 
 
مركبًا، لكن بقيت استعالمات تبحث   Current تم جعل المفتاح في جدولي  ` وحده في: activity_group_id بـ`

 - ` dustEvaluation.ts .قرب قراءة القرار السابق ` - ` fetchLatestFinalDecisions .` - ` fetchLatestStoredCompliance .` 
  مركبة تربط النشاط بالمشروع.  FK كما أن بقية جداول الدليل ال تملك جميعها
 
  اإلصالح التطبيقي  12.2 ###
 
  غيّر التواقيع:
 
 
``` ts 
fetchLatestFinalDecisions ( 
  supabase , 
  projectId , 
  activityGroupIds 
) 
 
fetchLatestStoredCompliance ( 
  supabase , 
  projectId , 
  activityGroupIds 
) 
 ``` 
  وكل استعالم يجب أن يحتوي:
 
 
``` ts 
. eq('project_id', projectId) 
. in('activity_group_id', activityGroupIds) 
 ``` 
  ,...>` عبر مشاريع متعددة. استخدم: Map<activityGroupId ال تستخدم 
`
 
``` ts 
type ActivityKey = `${string}:${string} ;` 
const activityKey = (projectId: string, groupId: string): ActivityKey  >= 
  $` {projectId}:${groupId} ;` 
 ``` 
  DB اإلصالح في  12.3 ###
 
 ` هو جدول هوية المجموعة؛ المجموعة قد تضم  project_dust_profiles ال تجعل 
` أكثر من
  صف نشاط. أنشئ كيانًا مستقالً بهوية مركبة، ثم اجعل كل األدلة تشير إليه:
 
 
``` sql 
create table public.activity_groups   ( 
  project_id uuid not null , 
  id text not null , 
  archived_at timestamptz , 
  archived_by uuid references auth.users(id) on delete set null , 
  created_at timestamptz not null default clock_timestamp ,)( 
  primary key (project_id, id) , 
  foreign key (project_id) 
    references public.projects(id) on delete restrict , 
  check (length(btrim(id)) between 1 and 200) 
;) 
 
update public.project_dust_profiles 
set activity_group_id = 'dust-' || id::text 
where activity_group_id is null or btrim(activity_group_id)  ;'' = 
 
alter table public.project_dust_profiles 
  alter column activity_group_id set not null ; 
 
insert into public.activity_groups (project_id, id) 
select distinct project_id, activity_group_id 
from public.project_dust_profiles 
on conflict do nothing ; 
 ``` 
  قبل إضافة القيود، أدخل كذلك هويات المجموعات التاريخية الموجودة في
 
` dust_evaluations `و ` dust_compliance_evaluations `و ` final_decisions  ` وجدولي
 
` current بعد ذلك أضف .`*_ FK  :مركبة من كل جدول إلى 
 
``` text 
activity_groups(project_id, id) 
 ``` 
 `، افحص التعارضات، ثم  NOT VALID ابدأ القيود على البيانات القائمة بصيغة `
  نفّذ
 
` VALIDATE CONSTRAINT في ` migration  ` تالية. ال تستخدم UNIQUE(project_id , 
activity_group_id ` على `) project_dust_profiles  إذا كان تصميم المجموعة ` يسمح
  بأكثر من نشاط؛ استخدم بدالً منه:
 
 
``` sql 
alter table public.project_dust_profiles 
  add constraint project_dust_profiles_project_group_id_uk 
  unique (project_id, activity_group_id, id) ; 
 ``` 
 بالتقييم الصحيح بمفتاح مركب يثبت أن   pointer كما يجب ربط 
 
` latest_evaluation_id يتبع المشروع والنشاط نفسيهما، ال مجرد ` UUID  .موجود 
 الصحيح، ثم **استبدل**   Tenant أضف أيضًا مفاتيح مركبة تربط كل تابع بمالك
  القيود
  المنفردة القديمة وال تتركها بالتوازي:
 
  | العالقة | المفتاح المطلوب |
 
|---|--- | 
 | Profile → Device | `(project_id, device_id)` → 
`project_devices(project_id,id) | ` 
 | Profile → Shift | `(project_id, shift_id)` → 
`project_shifts(project_id,id) | ` 
 | Provider Connection → Device | `(project_id, device_id)` → 
`project_devices(project_id,id) | ` 
 | Measurement History → Device | `(project_id, device_id)` → 
`project_devices(project_id,id) | ` 
 | Current DVI → Evaluation | 
`(project_id,activity_group_id,latest_evaluation_id) | ` 
 | Current Compliance → Evaluation  | المفتاح المركب النظير | 
 | Final Decision → Profile | 
`(project_id,activity_group_id,dust_profile_id) | ` 
 | Decision Record → Final Decision | `(project_id,final_decision_id) | ` 
  `. ON DELETE RESTRICT جميع عالقات األدلة تستخدم 
`
 ` حرة من  activity_group_id ` قيمة ` api/dust-profiles األفضل أال يقبل `
/ العميل.
 الخادمية معرف المجموعة   RPC يرسل العميل صفوف األنشطة المطلوبة، ثم تنشئ
  وتكتب
  المجموعة والصفوف كلها في معاملة واحدة.
 
 '`. يجب أال  activity_group_id='same-id اختبار القبول: أنشئ مشروعين لهما 
` قرار المشروع اآلخر.   Current Pointer أو تنبيه أو   API يقرأ أي
 
  CAS ` كنسخة updated_at ال تستخدم ` 12.4 ###
 
  Timestamp اإلصالح الحالي لسباق أول إدراج أفضل من النسخة السابقة، لكن مقارنة
 ،  Current ` إلى جدولي revision bigint not null default 0 تبقى هشة. أضف 
` RPC واجعل
  ` تستقبل النسخة المتوقعة: persist_activity_decision_atomic_v2 الجديدة 
`
 
``` sql 
update public.current_dust_decisions 
set latest_evaluation_id = v_dvi_evaluation_id , 
    decision = v_decision , 
    revision = revision + 1 , 
    updated_at = clock_timestamp )( 
where project_id = p_project_id 
  and activity_group_id = p_activity_group_id 
  and revision = p_expected_revision ; 
 
get diagnostics v_affected_rows = row_count ; 
if v_affected_rows <> 1 then 
  raise exception using 
    errcode = '40001', message = 'DVI CAS conflict ;' 
end if ; 
 ``` 
  و  DVI الطلبان المتزامنان بالنسخة نفسها: واحد فقط ينجح، واآلخر يتراجع بكامل
 
Compliance  و Final Decision و Outbox بعد انتقال التطبيق إلى . V2    اسحب
` EXECUTE `  ؛ وجود مسارين قابلين للكتابة يبطل الضمان الجديد. V1 من
 
 
 --- 
  . األرشفة الذرية 13 ##
 
  الخطأ الحالي  13.1 ###
 
 ألن   Push المشروع المؤرشف يستبعد من التقييم المعتاد، لكنه ما زال يقبل
 
` requireDeviceApiKey `و ` ingest_device_reading_atomic  .يفحصان الجهاز فقط ` كما أن األرشفة ال تعطل جميع األجهزة واالتصاالت في معاملة واحدة.
 
  قاعدة القفل  13.2 ###
 
  كل العمليات التالية يجب أن تأخذ قفل المشروع نفسه أوالً:
 
 
``` sql 
perform pg_advisory_xact_lock ( 
  hashtextextended('project:' || p_project_id::text, 0) 
;) 
 ``` 
  : RPCs الترتيب في جميع
 
  . قفل المشروع. 
1 `. archived_at . التحقق من ` 
2 . قفل الجهاز أو النشاط. 
3 . تنفيذ الكتابة. 
4
  . archive/ingest/evaluate وسباق   Deadlocks هذا الترتيب الموحد يمنع
 
 
 ### 13.3 RPC  األرشفة 
 
``` sql 
create function public.archive_project_atomic ( 
  p_project_id uuid , 
  p_actor_id uuid 
 ) returns void 
language plpgsql 
security definer 
set search_path = pg_catalog, public 
as  $$ 
begin 
  perform pg_advisory_xact_lock ( 
    hashtextextended('project:' || p_project_id::text, 0) 
   ;) 
 
  update public.projects 
  set archived_at = clock_timestamp(), archived_by = p_actor_id 
  where id = p_project_id and archived_at is null ; 
 
  if not found then 
    raise exception 'PROJECT_NOT_FOUND_OR_ARCHIVED ;' 
  end if ; 
 
  update public.project_dust_profiles 
  set archived_at = coalesce(archived_at, clock_timestamp()) , 
      archived_by = coalesce(archived_by, p_actor_id) 
  where project_id = p_project_id ; 
 
  update public.project_devices 
  set is_active = false , 
      revoked_at = coalesce(revoked_at, clock_timestamp()) 
  where project_id = p_project_id ; 
 
  update public.provider_connections 
  set is_active = false 
  where project_id = p_project_id ; 
 
  update public.project_evaluation_jobs 
  set status = 'DEAD', last_error = 'PROJECT_ARCHIVED ' 
  where project_id = p_project_id 
    and status in ('PENDING','RETRY','RUNNING') ; 
 
  insert into public.admin_audit_log  ;)...( 
end ; 
 ;$$ 
 ``` 
 `  persist_activity_decision_atomic ` و` ingest_device_event_v2 ويجب أن يفحص 
`` داخل المعاملة، ال في  Jobs `projects.archived_at is null الـ  Claim و

TypeScript  .فقط 
 ` ويستبعد المشروع المؤرشف  projects ` ليضم ` requireDeviceApiKey كذلك عدّل 
` قبل
 ، لكن اعتبر هذا تحسينًا مبكرًا فقط؛ الحارس الحاسم يبقى داخل  RPC الوصول إلى
 
DB .  ` كافيًا للكتابة: افصل بين verifyProjectOwnership وال تجعل 
`
` verifyProjectReadable `و ` verifyProjectWritable والثانية تشترط ،` 
` archived_at is null .` 
  عند األرشفة يجب في المعاملة نفسها:
  . Audit Event كتابة  - المعلقة.  Jobs إلغاء  - أرشفة األنشطة.  - تعطيل اتصاالت المزودين.  - تعطيل األجهزة وإبطال مفاتيحها.  -
 
 يمنع إعادة تفعيل جهاز أو اتصال إذا كان المشروع مؤرشفًا، ألن    Trigger أضف
  مسارًا
  حالة واضحة مثل  API األرشفة. وأعد من  RPC إداريًا آخر قد يتجاوز
 
` PROJECT_ARCHIVED  مع ` HTTP `410 ` ال ،` success:true .مع صفر نتائج ` 
  ، ثم Commit األرشفة بال  T1 اختبار السباق يجب أن يستخدم جلستين حقيقيتين: تبدأ
 وال يزيد   T2 تفشل  T1 في   Commit االستقبال وتتأكد أنها تنتظر؛ بعد   T2 تبدأ
 
History  . كرر االختبار نفسه مع حفظ القرار. Latest Projection وال تتغير
 
 
 --- 
 
 ## 14  . Append-only  حقيقي 
  األخطاء الحالية  14.1 ###
 
 - Triggers  ` تمنع UPDATE/DELETE ` وال تمنع ` TRUNCATE .` - ` دالة forbid_evidence_mutation ` تسمح بتصفير ` dust_profile_id .` - ` بعض عالقات الدليل تستخدم ON DELETE SET NULL ` أو ` CASCADE .` - ` alerts تحمل ` Core + State  في صف قابل للتغيير، ثم يحاول Trigger    آخر مزامنة الحالة.
 
  التصحيح  14.2 ###
 
 `، ألن النشاط والمشروع  ON DELETE RESTRICT األدلة ال تُفصل عن النشاط. استخدم 
` يُؤرشفان أصالً وال يُحذفان.
 
  : TRUNCATE منفصالً للـ  Trigger أنشئ
 
 
``` sql 
create or replace function public.forbid_evidence_truncate )( 
returns trigger language plpgsql as   $$ 
begin 
  raise exception 'evidence tables cannot be truncated ;' 
end ; 
 ;$$ 
 
create trigger final_decisions_no_truncate 
before truncate on public.final_decisions 
for each statement execute function public.forbid_evidence_truncate ;)( 
 ``` 
  طبق ذلك على:
 
 - ` device_events ` - ` device_measurements ` - ` pm10_readings_history ` - ` dust_evaluations ` - ` dust_compliance_evaluations ` - ` final_decisions ` - ` decision_alert_outbox بعد المعالجة إذا كانت جزءًا من الدليل ` - ` alert_state_events ` - ` admin_audit_log ` 
  واسحب الصالحيات:
 
 
``` sql 
revoke update, delete, truncate 
on public.final_decisions 
from public, anon, authenticated, service_role ; 
 
grant select, insert 
on public.final_decisions 
to service_role ; 
 ``` 
  . RLS `؛ ال تمر عبر TRUNCATE لمنع `  RLS ال تعتمد على 
 
  : Current State عن  Alert Core األفضل فصل
 
 - ` alerts .سجل الحدث األساسي غير قابل للتعديل :` - ` alert_state_events .انتقاالت غير قابلة للتعديل :` - ` alert_current_state`: Projection   قابل إلعادة البناء، أو View   ألحدث
Event . 
 ` أو  replacement_decision_id التصحيح ال يعدل القرار القديم؛ ينشئ `
 
Correction Event   .جديدًا 
 
 ### 14.3 RLS ` ال تعزل service_role ` 
  دليالً على العزل في المسارات الخادمية؛  RLS ال تعتبر نجاح سياسات
 
` service_role  يتجاوز ` RLS :لذلك يجب أن يتوافر الشرطان معًا . 
  ` صراحة. project_id . كل استعالم خادمي يحمل ` 
1 تمنع الربط بين مشروعين حتى عند وجود خطأ برمجي.  DB . القيود المركبة في 
2
 `  UPDATE/DELETE/TRUNCATE `، واسحب ` anon/authenticated المباشر من ` DML اسحب
  حتى
  مسماة فقط. أغلق   RPCs ` على جداول األدلة، واجعل اإلدراج عبر  service_role من 
`
Default Privileges  حتى ال يفتح جدول أو Function   :جديد تلقائيًا 
 
``` sql 
revoke create on schema public from public, anon, authenticated ; 
revoke execute on all functions in schema public 
  from public, anon, authenticated ; 
 
alter default privileges for role postgres in schema public 
  revoke all on tables from anon, authenticated ; 
 
alter default privileges for role postgres in schema public 
  revoke execute on functions from public, anon, authenticated ; 
 ``` 
  ` يجب أن يثبت: SECURITY DEFINER كل 
`
 
``` sql 
set search_path = pg_catalog, public 
 ``` 
 ` ومنح التنفيذ للدور الضروري باالسم. اختبر  REVOKE EXECUTE FROM PUBLIC ثم 
` ذلك
  SET ROLE ثم ` Transaction تجريبيين داخل  Function و Table بإنشاء 
 
authenticated ؛`  غير مكتمل.  Default Privileges إذا امتلك الدور صالحية تلقائية فإغالق 
 
 
 --- 
 
 ## 15  . ThingsBoard: SSRF  وبيانات االعتماد 
  ال تسمح بعنوان عشوائي في اإلنتاج  15.1 ###
 
 يكتبها المستخدم.   Credentials ` جزءًا من base_url الحل األقوى هو أال تكون 
` أنشئ
  معتمدة يديره مسؤول النظام:  Instances سجل
 
 
``` sql 
create table public.provider_instances  ( 
  id uuid primary key default gen_random_uuid ,)( 
  provider text not null , 
  origin text not null unique , 
  hostname text not null , 
  port integer not null default 443 check (port = 443) , 
  is_approved boolean not null default false , 
  approved_by uuid references auth.users(id) , 
  approved_at timestamptz , 
  is_active boolean not null default true , 
  check (origin like 'https://%') 
;) 
 
alter table public.provider_connections 
  add column provider_instance_id uuid 
  references public.provider_instances(id) on delete restrict ; 
 ``` 
  حر. إذا كان البد من دعم  URL معتمدًا بدل إرسال  Instance يختار المستخدم
 
ThingsBoard   :مستضاف ذاتيًا 
 - ` https .منع  - ` فقط Username/Password  داخل URL . 
  Private/Link-local/Loopback/Multicast/Documentation/Reserved حظر عناوين  - ` قبل اتباعه. Location '` ورفض التحويالت، أو فحص كل ` Admin . - ` redirect: 'manual فقط إال بموافقة  443 المنفذ  
- أو الصيغة الرسمية المعتمدة ثم استخدم  UUID ` كـ vendorStationId تحقق ` - لمسارات االختبار وجلب المحطات.  Instance موزع لكل مستخدم و Rate Limit - قصير.  Timeout - متوقع.  Content-Type حد أقصى لحجم االستجابة و - . IPv6 و IPv4 في
 
  ` encodeURIComponent .حد أقصى لعدد المحطات والنقاط في الرد، وال تجمع  - ` عند بناء المسار Payload   غير محدود فيفي    DNS يثبت الوجهة؛ فحص  Firewall أو  Egress Proxy األفضل تمرير الخروج عبر  - الذاكرة.
  . TOCTOU/DNS rebinding ` يحل االسم مرة أخرى يبقي فجوة fetch التطبيق ثم ترك `

  ` مطلق ًا. http ال ترسل اسم مستخدم وكلمة مرور عبر 
`
  التشفير  15.2 ###
 
  قديمًا يجب أال يبقى سلوك إنتاج دائمًا.  Plaintext االختبار الذي يقبل
 
  المطلوب:
 
  ` ووقت الترحيل. key_version منفصالً و`  Ciphertext V2 . أضف عمود  
1 ؛ ال تشفّر القيم النصية AES-256-GCM/Envelope Encryption كامالً بـ  JSON . شفّر  
2 مكشوفة.  Arrays أو   Objects وتترك   
   connection_id + project_id + device_id + provider يحتوي على `  AAD . اجعل 
3
+ 
   key_version حتى يفشل نقل ،` Ciphertext  .إلى صف آخر 
4 . Backfill   .لكل السجالت القديمة  وتجربة فك عينة آمنة.  Plaintext . تقرير يثبت صفر سجل  
5 :*`. enc:v2 لشكل ` Constraint ` وأضف V2 `NOT NULL . اجعل 
6 نهائيًا.  Plaintext الذي يقبل   Fallback القديم وأزل   JSONB . احذف عمود 
7 التي سبق تخزينها غير مشفرة.  Credentials . دوّر  
8 . Logs في  Ciphertext أو  JWT . ال تسجل األسرار أو 
9
  مثال مرحلة االنتقال األولى:
 
 
``` sql 
alter table public.provider_connections 
  add column credentials_ciphertext text , 
  add column credentials_key_version integer , 
  add column credentials_migrated_at timestamptz ; 
 
alter table public.provider_connections 
  add constraint provider_credentials_v2_shape 
  check  ( 
    credentials_ciphertext is null or  ( 
      credentials_ciphertext like 'enc:v2 '%: 
      and credentials_key_version is not null 
     ) 
   ) not valid ; 
 ``` 
  اختياريًا. النشر مرحلتان: إصدار يستطيع قراءة القديم   Backfill ال تجعل سكربت
  ال يقرأ القديم إطالق ًا. اختبر تبديل   Cutover أثناء الترحيل، ثم  V2 وكتابة 
 
Ciphertext  بين صفين، تعديل Auth Tag  وتدوير المفتاح؛ يجب أن تفشل ، المحاولتان
  األوليان ويعمل المفتاحان فقط أثناء نافذة التدوير.
 
 بدل مفتاح وحيد   Envelope Encryption و  Secret Manager/KMS إن أمكن، استخدم
  دائم في متغير بيئة.
 
 
 --- 
  واالختبارات والتسليم  migrations  — # الجزء الخامس
 
  migrations . سياسة 16 ##
 
  مطبقة سابق ًا.  migration . ال تعدّل أي 
1 . أنشئ أرقامًا جديدة فريدة فقط. 
2 ` عندما يكون الحجم كبيرًا. SET NOT NULL عن `  Backfill . افصل 
3` عند الحاجة لتقليل  VALIDATE CONSTRAINT ` ثم ` NOT VALID . استخدم قيود ` 
4 القفل.
  . اختبر مسارين: 
5 إلى آخرها.  migration قاعدة فارغة من أول  -  
  . Upgrade نسخة مماثلة لإلنتاج ثم  -  
  ` بين النتيجتين. pg_dump --schema-only . قارن ` 
6 . Health endpoint ` في app_commit ` و` migration_version . احفظ ` 
7
 إلى أي   PostgreSQL التاريخي نفسه يفشل قبل أن يصل  Baseline إذا كان ال
ـ
Migration  تستطيع إصالح تركيب جديد؛ لم تُنفذ أصالً.  Forward Migration تصحيحية، فال توجد 
  عندها يكون الحل الصحيح مسارين واضحين:
  Forward وتستمر بـ  Immutable القديمة تبقى  migrations البيئات القائمة:  -
 
  Migrations  .التركيبات الجديدة:  - الجديدة فقط Baseline/Snapshot  جديد معتمد ناتج من Schema   النهائية، مع
  يثبت التطابق.   Schema Diff األصلية واختبار  migrations سجل يربطه بسلسلة  
 
  ، وال تعدّل ملفًا سبق تطبيقه إلرضاء اختبار Baseline ال تنسخ قاعدة إنتاج وتسمّيها
 من   CI معزوالً في  PostgreSQL ` أو supabase db reset القاعدة الفارغة. نفّذ `
  الصفر
  إلثبات المسار الجديد.
 
  الجديدة المقترح:   migrations ترتيب
 
 
1 _...` . rule_bundle_audit_columns.sql ` 
2 _...` . activity_groups_backfill.sql ` 
3 _...` . activity_composite_fks_not_valid.sql ` 
4 _...` . activity_composite_fks_validate.sql ` 
5 _...` . device_events_v2.sql ` 
6 _...` . current_decision_revision_and_persist_v2.sql ` 
7 _...` . evaluation_jobs_and_runs.sql ` 
8 _...` . decision_alert_outbox.sql ` 
9 _...` . archive_project_atomic.sql ` 
10 _...` . append_only_and_acl_complete.sql ` 
11 _...` . provider_instances.sql ` 
12 _...` . provider_credentials_v2_transition.sql ` 
13 _...` . provider_credentials_v2_cutover.sql ` 
   Migration ال تجمع كل شيء في ملف ضخم غير قابل للمراجعة، لكن يجب أن تكون كل
  ذرية ومتوافقة مع إصدار التطبيق الذي سينشر معها.
 
 
 --- 
 
 . ترتيب التنفيذ للمبرمج 17 ## 
  معنى القرار  —  P0-A ### المرحلة 
 
  `. Rule Bundle `2026.3 . إنشاء 
1 . Regulatory State عن  Operational Band . فصل  
2 . Stop Basis/Confirmation . إصالح مصفوفة القرار النهائي وحقول  
3 . تحديث االختبارات الخاطئة. 
4
  عقد البيانات  —  P0-B ### المرحلة 
 
  ووقت مستقل لكل قياس.  Event V2 . إضافة 
1 . ThingsBoard . إصالح 
2 قرار.   Snapshot عرض و   Snapshot . بناء 
3 . تطبيق حداثة أربع دقائق لكل حقل حرج. 
4 . PM10 Evidence Window . إعادة بناء 
5
  خط التشغيل  —  P0-C ### المرحلة 
 
  . Planning عن   Live . فصل  
1 . Live في   Network Call . منع أي 
2 . Heartbeat و Worker و  Evaluation Jobs . إضافة 
3 . Evaluation Run . حفظ  
4
  الذرية والعزل  —  P0-D ### المرحلة 
 
  . Final Decision بـ Alert وربط  Outbox . إضافة 
1 ` لكل استعالم نشاط. project_id . إضافة ` 
2 المركبة.  FKs . إضافة 
3 . أرشفة ذرية وقفل موحد. 
4 لألدلة. Truncate . منع كل تعديل أو حذف أو  
5
  األمن والجودة   — P1 ### المرحلة 
 
 
1 . Allowlist/Egress  لحماية SSRF .  . Credentials . ترحيل وتدوير 
2 . DB ` داخل القرار و any ، خصوصًا ` ESLint . تنظيف 
3
4 . CI  و DB/E2E/Concurrency .  . Scheduler و  Provider و  Queue . مراقبة 
5
 صغيرة بهذا   PRs واحدًا يغير كل الملفات بال فصل. يفضل  Pull Request ال تفتح 
  كلها.  P0 الترتيب، مع إبقاء النظام غير قابل للنشر حتى اكتمال
 
 
 --- 
  . مصفوفة اختبارات القبول 18 ##
 
  القرار النهائي  18.1 ###
 
  | الحالة | المتوقع |
 
|---|--- | 
 | DVI `RESTRICT` + Compliance `ALLOW` | `RESTRICT | ` 
 | DVI `RESTRICT_SEVERE` | `RESTRICT | ` 
 | DVI `STOP_DUST_GENERATING_ACTIVITIES ` | غير إلزامي ` PROTECTIVE_STOP | ` 
 | DVI `STOP_VISIBILITY_DEPENDENT_ACTIVITIES` | `PROTECTIVE_STOP | ` 
 | Compliance `FIELD_VERIFICATION_REQUIRED` | `HOLD_FOR_VERIFICATION` + 
`NOT_DETERMINABLE | ` 
 | DVI Stop + Compliance Precaution  يفوز | DVI  | ويظهر سببه 
 | DVI Restrict + Evidence Hold   يفوز | Hold  |  قديم | يستمر قرار الرؤية بحسب دليلها المستقل |  PM10 | رؤية خطرة مؤكدة +
 
 | PM10   | وحده قديم Hold ال ، Stop  حي وال Allow  | 
  PM10 حدود  18.2 ###
 
  | القيمة | المتوقع التشغيلي |
 
|---|:--- | 
` | 0 ` ,` 150 | ` Normal/Allow  | 
` | 150.01 ` ,` 151 ` ,` 249.99 ` ,` 250 | ` Precaution/Monitor  | 
` | 250.01 ` ,` 251 ` ,` 320 | ` Controls/Monitor  | 
` | 320.01 ` ,` 321 ` ,` 340 | ` Severe/Restrict  |  |  Protective Stop + Pending ` بال استمرار | 340.01 | 
` |  Pending ` | ما زال ms 120000 ` عند ` 340.01 | 
` Mandatory Stop + Confirmed ` بدليل صالح | ms 120001 ` عند ` 340.01 | 
`
Violation  | 
  . Operational Band ` مستقالً عن 250 عند ` Regulatory Warning اختبر أيضًا
 
  الحداثة  18.3 ###
 
  | السيناريو | المتوقع |
 
|---|--- |  وفق السياسة الشاملة |   Fresh ` | 4:00.000 | عمر الحقل 
` |   Stale ` | 4:00.001 | عمر الحقل 
`
 | PM10    | حديث، رياح قديمة PM10 Fresh  والرياح Stale  |  حديثًا |  PM10 قديم | ال يصبح  PM10 | حرارة حديثة،
  بسببها |  Allow | رؤية آمنة قديمة | ال 
  | رؤية خطرة قديمة | ال تثبت خطرًا حيًا، وال تسمح باستئناف سابق |
 
 | timestamp   + مستقبلي | غير مؤهل Clock Error  | 
 
 ### 18.4 ThingsBoard 
 - PM10   عشر  - : يحتفظ كل حقل بوقته. 10:00 وحرارة عند   08:00 عند Polls   تغير الحرارة فقط: ال ينشأ أكثر من صف PM10  .نقطة بال  - واحد Timestamp ` ال تستخدم : now .`)( -   إعادة نفس Event/Payload: Duplicate .نفس  - صحيح EventId   مع Payload .مختلف: رفض صريح 
 
 ### 18.5 DB   والتزامن  جديدة مفتوحة  Function مباشرًا، وال توجد    DML ` ال يملكان anon/authenticated ` - من اإلشارة لتقييم مشروع آخر.  Pointer تمنع  FK - يفشل.  Evidence Table ` على كل UPDATE/DELETE/TRUNCATE ` - : تتراجع معاملة القرار. Outbox فشل إدراج   - مفتوح واحد.  Alert: Alert عامالن ينشئان  - . Lease بعد انتهاء   Job تعطل العامل: تستعاد   - : عامل واحد فقط. Job عامالن يطالبان  - . Orphans ` بال 40001 `: واحد ينجح واآلخر ` expected_revision بنفس `  CAS طلبا   - : ال قرار بعد األرشفة. Persist Decision متزامن مع  Archive - جديد.  Snapshot وال  History : بعد فوز األرشفة ال Ingest متزامن مع  FK . - Archive : يفشل B من  Profile يشير إلى    A في   FK . - Final Decision : يفشل B من  Evaluation يشير إلى  A في  FK . - Current Pointer : يفشل  B من  Shift أو   Device يشير إلى  A في مشروع  Profile - `: ال خلط. activity_group_id مشروعان بنفس ` -
  . Default Privileges ` بسبب PUBLIC لـ` 
 
 
 ### 18.6 Live/Forecast 
 - Open-Meteo  القرار الحي ال يتأخر وال يتغير. 429 معلق أو : - ` fetch ممنوع داخل ` Live .صفر استدعاءات : - Forecast  قديم: التخطيط يظهر Stale .نتيجة  - ، والقرار الحي ال يتأثر Planning   ال تُحفظ كـ Live   .وال تنتج مخالفة مؤكدة 
 
 ### 18.7 SSRF  واألسرار يعمل اإلصداران المصرح بهما، ثم يفشل المفتاح القديم بعد    Rotation أثناء  - . AAD/GCM يفشل بسبب  Auth Tag بين اتصالين أو العبث بـ   Ciphertext نقل  - مرفوضة.   Plaintext Credentials : أي Cutover بعد  - . Path Traversal ` ضد  vendorStationId تحقق من ` - . Timeout أكبر من الحد و Response ارفض   - خاصًا.  AAAA عامًا و A واسمًا يعيد   DNS Rebinding اختبر  - ` أو أي وجهة غير معتمدة. 169.254.169.254 إلى ` Redirect ارفض   - . CGNAT و Link-local خاص أو محجوز، بما فيه  IPv4/IPv6 ارفض كل  - غير المتوقع.  Path/Query والمنفذ غير المعتمد و  Userinfo و  HTTP ارفض   -
  اإللغاء.
 
 
 --- 
 
 ## 19  . CI  وبوابات الجودة 
  `: package.json أضف في 
`
 
``` json 
{ 
  " scripts { :" 
    " typecheck": "tsc --noEmit ," 
    " lint": "eslint . --max-warnings=0 ," 
    " test": "vitest run ," 
    " test:db ,"..." :" 
    " test:e2e ,"..." :" 
    " verify": "npm run typecheck && npm run lint && npm run test && npm 
run test:db && npm run test:e2e && npm run build " 
   } 
} 
 ``` 
  بإلغاء القواعد. األولوية:  ESLint خطأ  230 ال تُخفِ
 
  . DB واستعالمات  RPC payloads ` من القرار، موصالت األجهزة،  any . إزالة ` 
1 . إزالة الوعود أو األخطاء المبتلعة. 
2 التي قد تسبب تقييمًا مكررًا.   Hook dependencies . إصالح 
3 . تنظيف األخطاء الواجهية المتبقية. 
4
  التغطية ال تقاس بالنسبة العامة فقط. ضع حدًا مرتفعًا للفروع الحرجة:
 
 - Final Decision Resolver للفئات. 100 : تغطية فروع % - PM10 Boundary/Continuity: 100%  .للحواف - Freshness classifier: 100%  .للحاالت - Ingest RPC/Outbox/Archive اختبارات : DB  .حقيقية 
 
 --- 
 
 ## 20  . Definition of Done 
  ال يكتب المبرمج «تم اإلصالح» إال إذا قدم األدلة التالية:
 
 
1 . Diff  .واضح لكل ملف تغير ولماذا 
2 . Migration  جديدة، مع نتيجة تشغيلها على قاعدة فارغة وقاعدة Upgrade .  كاملة.  Boundary Matrix . نتائج 
3 بأوقات مختلفة للحقول.   ThingsBoard . اختبار 
4 . Live ` في fetch . اختبار يثبت صفر ` 
5 . Queue وال  CAS متزامن للعزل واألرشفة و PostgreSQL . اختبار 
6. قرار وتنبيه يحمالن  
7
` final_decision_id/evaluation_run_id/input_snapshot_hash .نفسها ` 
8 . Heartbeat فعلي للـ Scheduler   .وقياس التأخر  . Plaintext قديمة   Credentials . إثبات صفر 
9 . Production Build و typecheck`, `lint`, unit, DB, E2E . نجاح ` 
10` في اختبار القبول السابق، مع إضافة االختبارات الجديدة الواردة  9/ 9 . ` 
11 هنا.
 سيناريو ميداني على األقل قبل السماح بقرار آلي   100 . مراجعة بشرية لنتائج 
12 ملزم.
 
 
 --- 
  . توجيه أخير للمبرمج 21 ##
 
 ال تعِد كتابة المحركات الحسابية السليمة بال سبب. ركّز على حدود المسؤوليات 
  والعقود بينها:
  التوقعات تخدم التخطيط فقط.  - التنبيه يستهلك القرار المحفوظ نفسه وال يعيد الحساب.  - في معاملة واحدة.  Outbox قاعدة البيانات تحفظ القرار ودليله و - محرك القرار يختار النتيجة األشد دون تخفيف داللي.  - محرك القواعد يصف القواعد المنطبقة.  - طبقة الجودة تحدد ما يصلح للقرار.  - القياس يحفظ الحقيقة ووقتها.  -
 
 إذا بقي أي مسار قادرًا على إنشاء قرار حي من قيمة قديمة، أو تنبيه بال 
 
` final_decision_id  أو قراءة بعد أرشفة المشروع، فاإلصالح غير جذري حتى لو ،` الحالية.  Unit نجحت اختبارات
 
 