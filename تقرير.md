 dcr-app (7 `) `  — # تقرير إعادة تدقيق منظومة مرقاب 
     2026 أغسطس  4 **التاريخ:**
 **نطاق المراجعة:** مقارنة النسخة السابعة بتوصيات تدقيق النسخة السادسة، 
 وفحص محركات القرار، إدخال األجهزة، الموصالت، الجدولة والتنبيهات، الترحيالت 
  واألمان، ثم تشغيل اختبارات المشروع واختبارات قبول مستقلة.  
  **:SHA-256 **بصمة األرشيف
 
`80e9009bbf5a6db17c7803da60099bc5cde4bf71883e455f5cd32afd33764038   ` **سالمة المصدر:** لم يُعدَّل األرشيف األصلي. أ ُجري الفك واالختبار في نسخة عمل 
  منفصلة.
 
 > هذا تدقيق هندسي وتشغيلي مقابل المتطلبات والمراجع المرفقة، وليس اعتمادا ً 
  قانونيا ً أو تنظيميا ً من الجهة المختصة.
 
  . الحكم التنفيذي 1 ##
 
  ` للنشر التشغيلي أو الستخدام النظام كسجل امتثال رسمي.** NO-GO **القرار: 
`
 وبناء   TypeScript النسخة السابعة أفضل من السادسة بوضوح: أصلحت أخطاء
   CAS ، أصلحت سباق أول إدراج في 619 اإلنتاج، رفعت اختبارات المشروع إلى
 والتشفير والموصل الوهمي.    RLS وختمت عدة فجوات في  Scheduler برمجيا ً، أضافت
 التي    DVI لكن ستة اختبارات قبول أساسية ما زالت تفشل، وأهمها تحويل أوامر 
 `، وعدم تطبيق حداثة مستقلة للرياح  MONITOR تطلب تقييدا ً أو إيقافا ً إلى `
 خالف النطاق المعتمد للمشروع.  200 األخضر حتى  PM10 والرؤية، وبقاء حد
 
 الحزمة األصلية ناجحة ألن بعض اختبارات المشروع تثبّت السلوك الخاطئ نفسه. لذلك 
  ` دليالً منفردا ً على سالمة القرار. 619 / 619 ال يجوز اتخاذ `

  . نتائج التشغيل 2 ##
 
  | الفحص | النتيجة |
 
|---|--- |:  ملفا ً |   29 ناجحة** في  619 / 619 | اختبارات المشروع األصلية | *
* فاشلة** |  9/ 6 ناجحة،  3/9 | اختبارات قبول مستقلة للتوصيات السابقة | *
*
 | TypeScript `tsc --noEmit | **ناجح، صفر أخطاء** | `  صفحة/مسار |  53 لإلنتاج | **ناجح**؛  Next.js | بناء 
 
 | ESLint  :تحذيرا ً** |  18 خطأ و 230 | **فاشل  ** | app` | **21.47% | تغطية األسطر مع تضمين جميع ملفات 
` %** | 21.06 | تغطية العبارات | *
* %** | 22.84 | تغطية الفروع | *
* %** | 14.32 | تغطية الدوال | *
* ** | Scheduler/Alerts/Provider Pull/EvaluateProject | **0% | اختبارات
  | **غير موجود** |  PostgreSQL حقيقي/تزامن  Supabase | اختبار
 
 % مضللة؛ فهي ال تحسب الملفات التي ال  74 التغطية االفتراضية التي تظهر قرابة
 تستوردها االختبارات. عند إدخال جميع ملفات التطبيق تهبط تغطية األسطر إلى 
 
21.47 .% 
  ### مقارنة سريعة بالنسخة السادسة
 
  |  7 | النسخة  6 | البند | النسخة 
 
|---|---|:--- |:  ** | 619 / 619 | ** 587 / 587 | اختبارات المشروع |
 
 | TypeScript | 30  ** | 0 خطأ | **  | بناء اإلنتاج | فاشل | **ناجح** |
 
 | ESLint | 231  ،تحذيرا ً** |  18 خطأ،  230 تحذيرا ً | ** 37 خطأ  ** | 3/9 | ** 3/9 | قبول التوصيات الحرجة |
 
 
 التحسن في قابلية البناء حقيقي، لكن نتيجة القبول الحرجة لم تتحسن. 
  . حالة التوصيات االثنتي عشرة السابقة 3 ##
 
  | الخالصة |  7 | # | التوصية | الحالة في النسخة
 
|---|:---|---| --- | | **جزئي جيد** |   CI إلى   Typecheck والبناء وإضافة   TypeScript | إصالح  1|
  . | CI workflow وال  typecheck` script ناجحان؛ ال يوجد ` TypeScript البناء و

 |2  | migrations    تصحيحية جديدة وفريدة | **مكتمل بنيويا ً** | األرقام فريدةوالترتيب القديم أ ُصلح؛ ال يوجد اختبار قاعدة فارغة، وتوجد حالة وسيطة غير 
  `. | 020006 ` و` 020004 قابلة للتشغيل بين `
| تصحيح خرائط القرار النهائي | **غير منفذ** | خمس خرائط حرجة ما زالت   3|
  `. | MONITOR تنتهي `
   Push ` | **جزئي** | إلزام جيد في eventId/sequence/observedAt | إلزام ` 4|
  تسمح بالقيم الفارغة. |   SQL و  Provider Pull فقط؛ الكاتب الداخلي 
ودقائق | **غير منفذ قراريا ً** | الطوابع   4 | حداثة مستقلة لكل حقل خالل   5|
  دقيقة. |  20 مخزنة، لكن القيم القديمة تدخل القرار؛ النافذة العامة
 وكتابة اختبار تزامن حقيقي | **جزئي جيد** | الكود أصلح سباق   CAS | إصالح  6|
  متزامن فعلي. |   PostgreSQL أول إدراج؛ ال يوجد اختبار
 
 |7  | Scheduler   | **شامل كل دقيقتين مع قفل | **جزئي endpoint  و bucket   . | lease/retry/cleanup موجودان؛ ال يوجد إعداد نشر يشغله، والقفل بال
 | تنبيه من القرار نفسه وفي نفس المعاملة/اللقطة | **جزئي** | االستدعاء   8|
  صار فوريا ً، لكنه منفصل وغير ذري ويعيد حساب طقس جديدا ً. |
 ؛ المسار  fallback | **جزئي** | يوجد  Open‑Meteo | فصل القرار الحي عن  9|
  ثانية. |  20 الحي ما زال ينتظر الشبكة وقد يتأخر أكثر من
 والتشفير والوحدات | **جزئي جيد** |   ThingsBoard وتأمين  Mock | تعطيل  10 |
 
Mock  محجوب في اإلنتاج والتشفير جيد للجديد؛ SSRF   عبر redirect/DNS rebinding   والبيانات القديمة باقية. |
 ورفع إصدار القواعد | **غير منفذ وظيفيا ً** | اإلصدار   PM10 | حسم حدود  11 |
  . | 340–321 وال توجد طبقة   200 `، لكن الحد األخضر ما زال  2026.2 رُفع إلى `
| منع أي تشغيل لمشروع مؤرشف | **جزئي** | الجدولة والتقييم يستبعدانه،   12 |
  ما زاال يكتبان قراءات، وسباق القرار ممكن. |  Push/RPC لكن
 
  غير منفذة.   3 جزئية، و 8 **اإلجمالي:** توصية واحدة مكتملة بنيويا ً، 
 
  . اختبار القبول المستقل 4 ##
 
  ### الحاالت الناجحة
 
  | الحالة | النتيجة |
 
|---|--- | 
 | PM10 = 150 | `ALLOW | ` 
 | PM10 = 250 | `PRECAUTION | ` 
 | PM10 = 251 | `ALLOW_WITH_CONTROLS | ` 
  ### الحاالت الفاشلة
 
  | المدخل | الحالي | المطلوب |
 
|---|---| --- | 
 | DVI `RESTRICT` | `MONITOR` | `RESTRICT | ` 
 | DVI `RESTRICT_SEVERE` | `MONITOR` | `RESTRICT | ` 
` | STOP_DUST_GENERATING_ACTIVITIES` | `MONITOR` | `PROTECTIVE_STOP | ` 
` | STOP_VISIBILITY_DEPENDENT_ACTIVITIES` | `MONITOR` | `PROTECTIVE_STOP  `
| 
 | Compliance `FIELD_VERIFICATION_REQUIRED` | `MONITOR` | 
`HOLD_FOR_VERIFICATION | ` 
 | PM10 = 151 ` ال قاعدة؛ يبقى | ALLOW` | `PRECAUTION | ` 
 
`  app/utils/final-decision-engine/engine.test.ts االختبارات الحالية في ` `. يجب تغيير  RESTRICT → MONITOR ` و` STOP_* → MONITOR تتوقع صراحة ً بعض خرائط `
 االختبارات مع التنفيذ، ال االحتفاظ بها كمرجع.

  . موانع اإلطالق الحرجة 5 ##
 
  خرائط القرار النهائي تسمح بالعمل عند طلب اإليقاف  5.1 ###
 
  218 `، تقريبا ً األسطر  app/utils/final-decision-engine/engine.ts الموضع: 
`
244 . 
 _*` مع حاالت  STOP *` و` RESTRICT ` و` FIELD_VERIFICATION_REQUIRED تجميع `
 `، ومعناها التشغيلي السماح  MONITOR المراقبة في فرع واحد يجعل النتيجة `
بالعمل مع المراقبة. قواعد اإليقاف اإللزامية وقرارات اإليقاف الصريحة يجب أن 
  تتغلب دائما ً على الدرجة.
 
  االستبدال األدنى الصحيح:
 
 
``` ts 
if (mandatoryStop)  { 
  operationalDecision = 'MANDATORY_STOP ;' 
 } else if (pendingAffectedStop { ) 
  operationalDecision = 'PROTECTIVE_STOP ;' 
 } else if (evidenceUnavailable { ) 
  operationalDecision = 'HOLD_FOR_VERIFICATION ;' 
 } else if (suppressDviMonitoring { ) 
  operationalDecision = 'ALLOW ;' 
 } else if (compliance?.decisionCategory === 
'FIELD_VERIFICATION_REQUIRED { )' 
  operationalDecision = 'HOLD_FOR_VERIFICATION ;' 
 } else if  ( 
  dvi.decisionCategory === 'STOP_DUST_GENERATING_ACTIVITIES || ' 
  dvi.decisionCategory === 'STOP_VISIBILITY_DEPENDENT_ACTIVITIES ' 
 { ) 
  operationalDecision = 'PROTECTIVE_STOP ;' 
 } else if  ( 
  compliance?.decisionCategory === 'RESTRICT_ACTIVITY || ' 
  dvi.decisionCategory === 'RESTRICT || ' 
  dvi.decisionCategory === 'RESTRICT_SEVERE ' 
 { ) 
  operationalDecision = 'RESTRICT ;' 
 } else if  ( 
  compliance?.decisionCategory === 'ALLOW_WITH_CONTROLS || ' 
  compliance?.decisionCategory === 'PRECAUTION || ' 
  dvi.decisionCategory === 'ALLOW_WITH_MONITORING ' 
 { ) 
  operationalDecision = 'MONITOR ;' 
 } else   { 
  operationalDecision = 'ALLOW ;' 
} 
 ``` 
 ` يبقى  MANDATORY_STOP يجب إضافة اختبار جدولي للقيم الخمس، واختبار يثبت أن 
` األخرى.  DVI/Compliance أعلى أولوية من كل فروع
 
  ما زالت تخالف نطاقات المشروع المعتمدة  PM10 حدود  5.2 ###
 
  `: rulebook.ts التنفيذ الحالي في 
`
 `: إيقاف احترازي ثم إلزامي بعد استمرار أكثر من دقيقتين. 340 `> - `: سماح مع ضوابط أيضا ً. 340– 301 ` - `: سماح مع ضوابط. 300– 251 ` - `: احتراز. 250– 201 ` - `: سماح. 200–0` - 
  النطاقات المعتمدة للمشروع في المحادثة السابقة:
 `: أسود؛ إيقاف احترازي فوري، ثم إلزامي بعد استمرار أكثر من  340 `> - `: أحمر، تقييد ومحاولة إيقاف النشاط المتأثر. 340– 321 ` - `: ضوابط مشددة. 320– 251 ` - `: برتقالي/احتراز. 250– 151 ` - `: أخضر. 150–0` -
  دقيقتين.
 
 ألكثر   340 ، وكون المخالفة عند **تجاوز** 250 المرفق التنظيمي يؤيد حد التحذير
 فهو سياسة تشغيلية    150 من دقيقتين، ودورية تسجيل ال تتجاوز دقيقة. أما حد
 معتمدة للمشروع، وليس ادعاءً بأنه نص تنظيمي مباشر. ومستند الخوارزميات 
 لتقدير الخطر، فال يجوز استخدامه لتبرير نقل حد السماح   DVI يستخدم نطاقات
  . 200 التشغيلي إلى
 
  التصحيح األدنى:
 
 
``` ts 
const PM10_PRECAUTION_UG_M3 = 151 ; 
const PM10_CONTROLS_UG_M3 = 251 ; 
const PM10_RED_RESTRICT_UG_M3 = 321 ; 
const PM10_VIOLATION_STOP_UG_M3 = 340 ; 
 ``` 
  ثم ترتيب القواعد من األشد إلى األخف:
 
 
1 ` . pm10 > 340`: `STOP_AFFECTED_ACTIVITY ` فورا ً؛ ` MANDATORY_STOP  بعد أكثر ` ثانية.   120 من
 
2 ` . 321  =< pm10 <= 340`: `RESTRICT_ACTIVITY  أو قرار أحمر مستقل متفق ` . 320–251 عليه، وال يُدمج مع 
 
3 ` . 251  =< pm10 <= 320`: `ALLOW_WITH_CONTROLS .` 
4 ` . 151  =< pm10 <= 250`: `PRECAUTION .` 
5 ` . pm10 <= 150  ال قاعدة :` PM10 . 
 »، فلتبدأ السلسلة عند  250 دقيقة. إذا كان المقصود «تجاوز  30 يجب توحيد قاعدة
 ثم ال تُصدر القرار إال عند   250 ` أيضا ً؛ ال تبدأ داخليا ً من 251 ` وتُطبَّق عند ` 251 `

251 . 
  حداثة الحقول مستقلة في قاعدة البيانات فقط  5.3 ###
 
 `  app/utils/dust-engine/types.ts ` و` app/lib/dustEvaluation.ts الموضع: 
` `. app/utils/dust-engine/engine.ts و`

 ` وغيرها موجودة، لكن  last_wind_speed_at`, `last_visibility_at أوقات `
 
` buildDustInput  يمرر وقت ` PM10    والوقت العام فقط. الرياح أو الرؤية القديمة` مع أن الحقل المسبب للقرار  OK حديث، فيصبح الدليل ` PM10 قد تدخل القرار مع
  منتهي الصالحية.
 
  اإلصالح المطلوب:
 
 
``` ts 
const FIELD_FRESHNESS_MS = 4 * 60_000 ; 
 
function freshOrNull<T>(value: T | null, observedAt: string | null, 
nowMs: number): T | null  { 
  if (value === null || !observedAt) return null ; 
  const observedMs = Date.parse(observedAt) ; 
  if (!Number.isFinite(observedMs)) return null ; 
  const age = nowMs - observedMs ; 
  if (age < 0 || age > FIELD_FRESHNESS_MS) return null ; 
  return value ; 
} 
 ``` 
 `، ثم إسقاط القيمة المنتهية قبل  DustEngineInput يجب تمرير وقت كل حقل داخل 
`
` mergeDustReading  وال يجوز استخدام وقت .` PM10   إلثبات حداثة الرياح أو الرؤية.
 
 دقائق، ورؤية    4:01 كم/س عمرها   40 حديث، رياح  PM10 اختبار القبول الحاسم: 
 دقائق؛ يجب أال تُنتجا إيقافا ً حيا ً، ويجب أن تظهر جودة الدليل   4:01 م عمرها 300
 ناقصة للحقلين.
 
 
 ### 5.4 ThingsBoard  ينسب أحدث timestamp   إلى جميع الحقول 
  `. app/lib/providers/thingsboardConnector.ts الموضع: 
`
 والرياح وغيرها، ثم يضعه في   PM10 بين الحرارة و  timestamp الموصل يجمع أكبر 
 
` observedAtIso واحد للحمولة كلها. حرارة حديثة يمكن أن تجعل ` PM10   عمرهساعة يبدو حديثا ً، وتُنشئ سلسلة استمرار زائفة أو تُسقط تحديثا ً حقيقيا ً على أنه 
 
duplicate . 
  يلزم تغيير عقد الموصل من وقت واحد للحمولة إلى وقت مستقل لكل قياس، مثال:
 
 
``` ts 
type TimedValue = { value: number; observedAtIso: string } ; 
 
interface NormalizedReading  { 
  pm10?: TimedValue ; 
  pm25?: TimedValue ; 
  windSpeedKmh?: TimedValue ; 
  windGustKmh?: TimedValue ; 
  windDirectionDeg?: TimedValue ; 
  visibilityM?: TimedValue ; 
  temperatureC?: TimedValue ; 
  relativeHumidityPercent?: TimedValue ; 
} 
 ``` 
 ثابت لكل مجموعة قياسات   hash ` من معرف حدث المصدر أو eventId ويُبنى `
 فقط. يجب أال يُستخدم وقت الخادم بدالً من وقت   timestamp وطوابعها، ال من أكبر
 ؛ تعامل معها كبيانات غير صالحة  timestamps الرصد عندما يقدّم المصدر قيما ً بال
  للقرار الحي.
 
 
 ### 5.5 Scheduler  ً موجود في الكود لكنه غير مثبت تشغيليا 
  `. vercel.json ` و` app/api/cron/scheduler-tick/route.ts الموضع: 
`
 ،  secret fail-closed شامل للمشاريع غير المؤرشفة،  endpoint التحسن الحقيقي:
  )`. project_id,time_bucket كل دقيقتين مع مفتاح `( bucket و

  المتبقي:
 ` يشغل  vercel.json ال يوجد إعداد نشر أو سكربت داخل المشروع يستدعيه؛ ` -
 التالي بالتداخل إذا ظل السابق   bucket أو حالة نجاح/فشل، ويسمح  lease ال  - نفسه.   bucket داخل  retry صف القفل يُنشأ قبل التقييم؛ إذا فشل التقييم فال  - توليد التنبيهات يوميا ً فقط.
 ، ما يعرض الوظيفة النتهاء المهلة  pagination كل المشاريع تُعالج تسلسليا ً بال   - سنويا ً.   262,800 صف قفل يوميا ً لكل مشروع، أي   cleanup: 720 ال  - يعمل.
  عند التوسع.
 
 يثبت آخر   health check قبل االعتماد يجب وجود إعداد فعلي للنداء كل دقيقتين،
 
tick  ،ناجح lease   ،قابل لالسترداد retry ، cleanup و ، metric   لزمن التأخر وعدد المشاريع الفاشلة.
 
  القرار والتنبيه ليسا لقطة ذرية واحدة  5.6 ###
 
 
` evaluateProject ` يحفظ القرار ثم يستدعي ` checkDustActivities  في عملية ` latest منفصلة. فشل التنبيه يُبتلع، والمنشئ يعيد حساب طقس جديدا ً ويمزجه مع
 
stored decisions ` ال يوجد . final_decision_id في سجل التنبيه، وال ` unique 
idempotency   .يمنع التكرار 
  داخل معاملة حفظ القرار:  Outbox الحل الموصى به هو
 
 
``` sql 
insert into public.decision_alert_outbox 
  ( final_decision_id, project_id, activity_group_id, event_type, 
idempotency_key ) 
values 
  ( v_final_decision_id, p_project_id, p_activity_group_id , 
   ' FINAL_DECISION_PERSISTED', p_project_id::text || ':' || 
v_final_decision_id::text ) 
on conflict (idempotency_key) do nothing ; 
 ``` 
 ` نفسه،  final_decision_id ، ويبني التنبيه من ` outbox صف  worker ثم يعالج
 مفتاح فريد   alert `. يجب أن يكون على  processed_at/error/attempt_count ويضع `
 يمنع التكرار.
 
  Scheduler/Alert ` في كل profile.onsite_pm10 كما يجب إيقاف إعادة تسجيل 
`
pass  ً حاليا ً قد تُسجّل القيمة اليدوية الثابتة مرتين في الدورة الواحدة، مولدة . صفا ً يوميا ً لكل نشاط دون حدث رصد جديد.  1,440–720

  العزل بين المشاريع غير مكتمل بعد المفتاح المركب  5.7 ###
 
 )`، وهذا جيد. لكن عدة  project_id, activity_group_id المفتاح الحالي أصبح `(
 `  activity_group_id ` ما زالت تفلتر ` app/lib/dustEvaluation.ts قراءات في `
 وحده:
 
 - previous compliance decisions  914–911 قرب األسطر . - ` fetchLatestFinalDecisions 1894–1879 ` قرب . - ` fetchLatestStoredCompliance  1959–1930 ` قرب . 
 ` من العميل، يمكن تكرار القيمة بين مشروعين  activity_group_id يقبل ` API وألن
  . A في شاشة أو تنبيه مشروع  B وخلط قرار مشروع
 
  النمط الصحيح:
 
 
``` ts 
const { data } = await supabase 
  . from('final_decisions') 
  . select )'*'( 
  . eq('project_id', projectId) 
  . in('activity_group_id', activityGroupIds) 
  . order('created_at', { ascending: false }) ; 
 
const key = `${row.project_id}:${row.activity_group_id} ;` 
 ``` 
   evaluation من اإلشارة إلى  current pointer مركبة تمنع  FKs يجب أيضا ً إضافة
  project_id, activity_group_id, created_at تابع لمشروع آخر، وفهرس `
(
desc .`) 
  والقرارات المتسابقة  Push المشروع المؤرشف ما زال يقبل   5.8 ###
 
 
` requireDeviceApiKey `و ` ingest_device_reading_atomic  ،يفحصان نشاط الجهاز `بعد   history/snapshot `. لذلك تستمر كتابة projects.archived_at وال يفحصان `
 `. persisted:0 األرشفة، رغم أن التقييم الالحق يعيد `

  نفسها:  RPC يلزم داخل
 
 
``` sql 
select p.archived_at 
into v_project_archived_at 
from public.project_devices d 
join public.projects p on p.id = d.project_id 
where d.id = p_device_id 
  and d.project_id = p_project_id 
  and d.is_active = true 
for update of d, p ; 
 
if not found or v_project_archived_at is not null then 
  raise exception 'PROJECT_ARCHIVED_OR_DEVICE_INACTIVE' using errcode = 
'P0001 ;' 
end if ; 
 ``` 
 ` من عدم أرشفة المشروع  persist_activity_decision_atomic ويجب أن يتحقق 
`ذرية تستخدم   RPC والنشاط تحت القفل نفسه. أرشفة النشاط/المشروع يجب أن تكون 
  القفل ذاته وتعطل األجهزة واالتصاالت ذات الصلة.
 
 
 ### 5.9 append-only  غير مكتمل 
 
Triggers ` الحالية تمنع UPDATE/DELETE ` فقط، وال تمنع ` TRUNCATE  كما تسمح .`` وفصل الدليل عن  dust_profile_id ` بتصفير ` forbid_evidence_mutation دالة `
 النشاط.
 
  ,`final_decisions المطلوب على جميع جداول األدلة، ومنها 
`
`dust_evaluations`, compliance, PM10, device history, alert events   `: admin_audit_log و
`
 
``` sql 
create trigger forbid_final_decisions_truncate 
before truncate on public.final_decisions 
for each statement execute function public.forbid_any_mutation ;)( 
 
revoke update, delete, truncate on public.final_decisions 
from anon, authenticated, service_role ; 
 
grant select, insert on public.final_decisions to service_role ; 
 ``` 
 `.  schema `public في  default privileges منفصالً لكل جدول، واسحب   Trigger أضف
 `، واحذف استثناء  ON DELETE RESTRICT غيّر روابط أدلة النشاط إلى `

` dust_profile_id = NULL وال تعتمد على .` RLS ` لحماية TRUNCATE  ألنه ال يمر ` بها.
 
  Push عقد الحدث كامل فقط في   5.10 ###
 
 
Push  يتحقق من eventId  و sequence الصحيح غير السالب و observedAt  لكنه يرفض ، Provider دقيقة بدالً من حفظها كدليل تاريخي غير حي. أما  40 القراءة األقدم من 
 
Pull  والكاتب الداخلي و SQL    فما زالت تقبل null   وال تتحقق من sequence . 
  التنفيذ الصحيح على مرحلتين:
 
 
1 . backfill    للقيم القديمة، وتحديد عقد واضح لكل provider . 
2 ` . NOT NULL `و ` CHECK (sequence_no >= 0) والتحقق داخل ،` RPC مع حفظ ، late 
event  في history    ومنعه فقط من تحديث snapshot / .قرار حي 
 يجب رفض الوقت المستقبلي، وعدم استبدال وقت الرصد بوقت االستالم، واستخدام 
 
` Number.isSafeInteger  في ` TypeScript . 
 
 ### 5.11 Open‑Meteo  ما زال في المسار الحي 
 ، لكن الطلب الحي  Open‑Meteo من الجهاز عند فشل  fallback النسخة تستطيع بناء
   20 يمكن أن يتجاوز التأخير   backoff والـ   retries ينتظر محاوالت الشبكة أوالً. مع
  ` نفسه. evaluateProject ينتظر `  Push ثانية قبل حفظ القرار، ومسار
 
 قبل    Open‑Meteo ` مع جهاز مكتمل وحديث، ال تستدعِ  LIVE_OPERATIONAL في الوضع 
`القرار. احفظ قرار الجهاز فورا ً، ثم جلب التوقعات في مسار تخطيط/إثراء منفصل 
  ال يستطيع إسقاط القرار الحي أو تأخيره.
 
  غير مكتملة  SSRF من  ThingsBoard حماية  5.12 ###
 
 
` safeUrl.ts ` يفحص العناوين الخاصة، لكنه يسمح ` http ` ويتبع ،`: fetch  `  redirect `؛ لذلك تبقى fetch مرة للفحص ثم يعيده `  DNS التحويالت تلقائيا ً، ويحل
  ممكنة.   DNS rebinding و  metadata IP إلى
 
  قبل اإلنتاج:
 :`؛ الحفظ  enc:v1 شغّل سكربت تشفير السجالت القديمة وأثبت أن كل قيمة أصبحت `  - . HTTP ال ترسل اسم مستخدم/كلمة مرور عبر  - يثبت الوجهة.  outbound proxy لنطاقات المزودين أو  allowlist استخدم  - ` قبل أي متابعة. Location '`، وافحص كل ` redirect: 'manual استخدم ` - :` فقط. https اسمح ` -
  الجديد وحده ال يكفي.
 
  . إصالحات صحيحة تستحق اإلبقاء 6 ##
   rollback ` و ROW_COUNT وفحص ` advisory lock ألول إدراج عبر  CAS إصالح   - قبل استخدامه.  history فريدة، وإنشاء  migrations أرقام  - وبناء اإلنتاج.   TypeScript اختفاء أخطاء  -
  للمعاملة الخاسرة.
 
  Outbox استدعاء التنبيه مباشرة بعد الحفظ قلص الفجوة الزمنية، لكنه يحتاج   - والتقييم المعتاد.  Provider Pull و Scheduler استبعاد المشاريع المؤرشفة من   - . 15 ، والهدم/قطع األحجار تحت 25 تصحيح استئناف الرياح: العام تحت  - في اإلنتاج.   Mock provider تعطيل  - للسجالت الجديدة.  AES‑256‑GCM تشفير  - إغالق معظم الكتابات المباشرة على جداول القرار.  - أحدث.   snapshot من استبدال  late event ذرية الستقبال الجهاز ومنع   RPC - المفتاح المركب للجداول الحالية.  
- ليصبح موثوقا ً.
 
  . ترتيب التنفيذ اإللزامي 7 ##
 
 
 ### P0  —  قبل أي تجربة ميدانية ملزمة 
  الخمس واختبارات الجدول.  Final Decision . خرائط 
1 دقيقة.   30 وتوحيد قاعدة  340–321 المعتمدة وفصل  PM10 . إعادة حدود 
2 . ThingsBoard per-field timestamps دقائق لكل حقل وإصالح   4 . حداثة 
3  FKs ` وإضافة project_id + activity_group_id . عزل كل قراءة قرار بـ` 
4 المركبة.
  نفسها.   RPCs . منع المشروع/النشاط المؤرشف داخل 
5
6 . Outbox  `ذرية تربط التنبيه بـ final_decision_id .وتمنع التكرار `  . lease/retry/cleanup كل دقيقتين مع  Scheduler . تشغيل فعلي ومراقب للـ 
7 عن المسار الحي.   Open‑Meteo . فصل  
8 تاريخيا ً فقط.  late events ، وحفظ SQL/Provider Pull . إكمال عقد الحدث في  
9 . default privileges وتصفير روابط األدلة، وإغالق   TRUNCATE . منع 
10
 
 ### P1  —  قبل Production 
  القديمة.   credentials وترحيل  HTTPS وفرض  redirect/DNS rebinding . تأمين 
1 . CI ` إلى typecheck`, `lint`, `test`, `test:db`, `test:e2e . إضافة ` 
2 إلى آخرها.  migration . اختبار قاعدة فارغة من أول 
3 CAS ، ingest idempotency ، archive/evaluate متزامن لـ PostgreSQL . اختبار 
4
race  .والعزل بين مشروعين  . final decisions/locks اليدوي وضبط نمو   PM10 . إزالة تكرار تسجيل 
5 ، ال محركات الحساب فقط. API/DB ورفع تغطية  ESLint خطأ   230 . تنظيف 
6
  . بوابات قبول اإلصدار التالي 8 ##
 
  حتى تتحقق جميع الشروط التالية:  GO ال يتغير الحكم إلى
 ** من اختبار القبول  9/9 ` أو أكثر من اختبارات المشروع، مع ** 619 / 619 ` -
 `،  340.01 / 340 / 321 / 320 / 251 / 250 / 201 / 200 / 151 / 150 عند ` PM10 مصفوفة  - المستقل.
 مستقل وعدم   timestamp قديم، مع  PM10 : حرارة حديثة +  ThingsBoard اختبار  - . PM10 ` دقائق لكل من الرياح والرؤية و 4:01 اختبار حقل منتهي عند ` - `، واستئناف مستقر عشر دقائق. 25.001 /25 /15 / 14.999 اختبار رياح عند ` - ` ثانية. 121 / 120 / 119 و`

duplicate  .اختبار  - كاذب Open‑Meteo  .اختبار مشروعين بنفس ` - معطل: القرار الحي من الجهاز يعود بال أي انتظار شبكة activity_group_id  بال خلط في الشاشة أو القرار أو `` لآلخر وال أثر تاريخي للمعاملة  40001 متزامن: ناجح واحد فقط و` CAS اختبار  - التنبيه.
 ` على جداول األدلة يفشل حتى بصالحية الدور  UPDATE/DELETE/TRUNCATE ` - : ال قراءة وال قرار بعد األرشفة. ingest/evaluate اختبار أرشفة متزامن مع  - الخاسرة.
  مثبتة.  idempotency ` واحدا ً و final_decision_id القرار والتنبيه يحمالن ` - التشغيلي.
 
عند التأخر، ال مجرد وجود    alert و  tick فعلي عبر سجل آخر  Scheduler إثبات  
-
endpoint . - TypeScript  والبناء و ESLint و CI  .كلها ناجحة 
  . الخالصة للمبرمج 9 ##
 
 ال تعِد كتابة محركات الحساب التي اجتازت االختبارات. ركز على طبقة الدمج التي 
 تحول مخرجاتها إلى قرار تشغيلي موثوق: ترتيب أولوية القرارات، حداثة كل 
 قياس، هوية الحدث، العزل بين المشاريع، المعاملة التي تحفظ القرار والتنبيه، 
  لتزامن. وضمانات قاعدة البيانات عند األرشفة وا
 
 النسخة السابعة تصلح كبيئة تطوير وتجربة داخلية غير ملزمة مع إشراف بشري 
 وبيانات غير حساسة. **ال تصلح بعد التخاذ إيقاف/استمرار ميداني آلي أو كسجل 
  امتثال رسمي.**
 
 