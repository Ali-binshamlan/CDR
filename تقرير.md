 ` للمبرمج dcr-app(5).rar # دليل تصحيح `
     2026 أغسطس  2 تاريخ المراجعة:
  ` وحده، دون مقارنة بوثائق أو سورس آخر. dcr-app(5).rar النطاق: 
`
  ) الخالصة التي يجب أن يبدأ منها فريق التطوير 1 ##
 
 ` مطابق بايتًا ببايت للنسخة التي  dcr-app(5).rar أن `   SHA-256 تم التحقق بال
ـ`). لذلك ال توجد فروق كودية جديدة تغيّر  dcr-app(4).rar جرى تحليلها سابق ًا (`
 نتيجة التدقيق.
 
 والرياح قوية، وال أنصح بإعادة كتابة حدودها   PM10 النواة الحسابية الخاصة ب
ـاآلن. موانع اإلطالق موجودة أساسًا في طبقات **الزمن، التخزين، التزامن، األمن، 
  الجدولة، وتجميع القرار النهائي**.
 
  ترتيب التنفيذ اإللزامي:
 
  | األولوية | اإلصالح | شرط اإلغالق |
 
|---|---| --- | 
 | P0   إصالح | migrations  | إنشاء قاعدة فارغة ينجح بالكامل | 
 | P0  منع الكتابة المباشرة عبر | Supabase | `authenticated  ال يملك ` DML   على جداول القرار |
 
 | P0 إدخال الجهاز بمعاملة واحدة و | Idempotency    إلزامية | التكرار والتزامن ال ينتجان أكثر من حدث واحد |
 
 | P0 ` اعتماد | observedAt دقائق في   4 ` وحداثة كل حقل | ال تدخل قيمة أقدم من القرار الحي |
 
 | P0   إصالح | CAS  وحفظ القرار النهائي | تعارض CAS ` وال يترك أي  40001 يرجع ` صف جزئي |
 
 | P0   فصل القرار الحي عن | Open‑Meteo   فشل المزود ال يوقف قرار الجهاز الحي |
| 
 | P0  | تقييم خادمي كل دقيقتين | يستمر القرار والتنبيه والمتصفح مغلق | 
 | P0  إصالح تجميع | DVI  ` | والقرار النهائي STOP ` ال يتحول إلى `*_ MONITOR | ` 
 | P1 >` 25 ` تعرض «أقل من 25 | إصالح شرط استئناف الرياح | بوابة | » 
 | P1  | األرشفة بدل الحذف | ال تُحذف سلسلة األدلة أو تتعطل القيود | 
 | P1   تحقق | Zod شامل للـ API ` ال تصل | NaN أو ` enums  | مجهولة إلى المحركات 
 | P2 | ESLint  | والتغطية CI ≥ 80 يمنع دينًا جديدًا وتغطية المسارات الحرجة | % 
   Supabase كاملة باختبارات تكامل على قاعدة  P0 ال يُسمح بإطالق النظام قبل إغالق
  فعلية مؤقتة.
 
 
 --- 
  migrations ) إصالح ترتيب  2 ##
 
  ### الخطأ
 
  الملف:
 
 
` supabase/migrations/202607290005_device_event_contract.sql ` 
 `، لكن الجدول ال يُنشأ إال  ALTER TABLE public.device_readings_history ينفذ 
` في:
 
 
` supabase/migrations/202607290006_close_full_supabase_gap.sql ` 
  لذلك يفشل إنشاء قاعدة جديدة قبل اكتمال النظام.
 
  ### التصحيح
 
 في أي بيئة مشتركة، بدّل رقمي الملفين ليصبح   migrations إذا لم تُطبّق هذه
  الترتيب:
 
 
``` text 
202607290001 _ baseline_final.sql 
202607290002 _ security_hardening.sql 
202607290003 _ sensor_events.sql 
202607290004 _ append_only_audit.sql 
202607290005 _ close_full_supabase_gap.sql 
202607290006 _ device_event_contract.sql 
202607290007 _ atomic_decision_persist.sql 
202607290008 _ database_integrity.sql 
 ``` 
 ، **ال تعِد تسمية ملفات  production أو  staging إذا طُبّقت األرقام الحالية في
 `  008 تصالحيًا ` migration جديدًا للقواعد الجديدة أو  baseline مطبقة**. أنشئ
 ` ثم يضيف األعمدة والفهارس والقيود  CREATE TABLE IF NOT EXISTS يستخدم `
 الناقصة.
 
  ### اختبار القبول
 
 
``` bash 
supabase db reset 
supabase db lint 
 ``` 
   staging مع بيئة  schema diff يجب أن ينجح األمران على قاعدة فارغة، ثم يكون
  المعتمدة صفرا ً.
 
 
 --- 
  ACL و  RLS ) إغالق الكتابة المباشرة عبر 3 ##
 
  ### الخطأ
 
 `  project_shifts ` و` projects ` الحالية على ` FOR ALL سياسات 
`` تسمح لمالك المشروع  decision_records ` و` alerts ` و` project_dust_profiles و`
والكتابة مباشرة. هذا يلغي قيمة التحقق الخادمي؛ يمكن تعديل مدخل   API بتجاوز 
  يؤثر في قرار االمتثال أو إدخال سجل قرار مزور.
 
 
RLS  وحدها ليست كافية. يجب إزالة سياسات الكتابة وسحب امتيازات DML   من
` anon `و ` authenticated .` 
  الصحيح  migration ### كود
 
 
``` sql 
begin ; 
 
drop policy if exists "projects_owner_all" on public.projects ; 
drop policy if exists "project_shifts_owner_all" on 
public.project_shifts ; 
drop policy if exists "project_dust_profiles_owner_all" on 
public.project_dust_profiles ; 
drop policy if exists "alerts_owner_all" on public.alerts ; 
drop policy if exists "decision_records_owner_all" on 
public.decision_records ; 
 
create policy "projects_owner_select " 
on public.projects for select to authenticated 
using (user_id = auth.uid()) ; 
 
create policy "project_shifts_owner_select " 
on public.project_shifts for select to authenticated 
using (exists  ( 
  select 1 from public.projects p 
  where p.id = project_shifts.project_id 
    and p.user_id = auth.uid )( 
 ;)) 
 
create policy "project_dust_profiles_owner_select " 
on public.project_dust_profiles for select to authenticated 
using (exists  ( 
  select 1 from public.projects p 
  where p.id = project_dust_profiles.project_id 
    and p.user_id = auth.uid )( 
 ;)) 
 
create policy "alerts_owner_select " 
on public.alerts for select to authenticated 
using (exists  ( 
  select 1 from public.projects p 
  where p.id = alerts.project_id 
    and p.user_id = auth.uid )( 
 ;)) 
 
create policy "decision_records_owner_select " 
on public.decision_records for select to authenticated 
using (exists  ( 
  select 1 from public.projects p 
  where p.id = decision_records.project_id 
    and p.user_id = auth.uid )( 
 ;)) 
 
revoke insert, update, delete, truncate, references, trigger 
on table 
  public.projects , 
  public.project_shifts , 
  public.project_devices , 
  public.project_dust_profiles , 
  public.dust_evaluations , 
  public.current_dust_decisions , 
  public.dust_compliance_evaluations , 
  public.current_dust_compliance_decisions , 
  public.pm10_readings_history , 
  public.weather_forecasts , 
  public.sensitive_receptors , 
  public.alerts , 
  public.decision_records , 
  public.admin_audit_log , 
  public.user_authorizations , 
  public.alert_state_events , 
  public.device_readings_history , 
  public.final_decisions 
from anon, authenticated ; 
 
revoke insert, update, delete, truncate, references, trigger 
on table public.profiles 
from anon, authenticated ; 
 
alter default privileges for role postgres in schema public 
revoke insert, update, delete, truncate, references, trigger 
on tables from anon, authenticated ; 
 
commit ; 
 ``` 
 ` وحده ال يمنع التعديل إذا بقي امتياز  UPDATE(state) مالحظة مهمة: سحب `
 
` UPDATE .على مستوى الجدول ` 
  ### اختبار القبول
 
 
``` sql 
select grantee, table_name, privilege_type 
from information_schema.role_table_grants 
where table_schema = 'public ' 
  and grantee in ('anon', 'authenticated') 
  and privilege_type in  ( 
    ' INSERT', 'UPDATE', 'DELETE ,' 
    ' TRUNCATE', 'REFERENCES', 'TRIGGER ' 
   ;) 
 ``` 
 بتوكن مستخدم حقيقي: الكتابة   REST النتيجة المطلوبة: صفر صفوف. وبعدها اختبر
  الخادمي تنجح.  API المباشرة مرفوضة، والكتابة عبر
 
 
 --- 
  ) عقد إدخال الجهاز: إلزام الحدث والزمن والتحقق 4 ##
 
  ### الخطأ
 
 `  sequence ` و` eventId ` الحقول ` app/api/devices/ingest/route.ts في 
`` يقبل كسورًا أو أرقامًا سالبة،  sequence ` اختيارية. كما أن ` observedAt و`
 وأزمنة الحقول تُحدّث بوقت الوصول ال وقت القياس.
 
  يجب الفصل بين:
 
 - ` observedAt .وقت القياس الحقيقي؛ هو أساس الحداثة والقرار :` - ` receivedAt .وقت وصول الطلب للخادم؛ لمراقبة االتصال والتأخير فقط :` - ` created_at .وقت إنشاء صف التدقيق :` 
  الصحيح  Zod ### مخطط
 
 
``` ts 
import { z } from 'zod ;' 
 
const MEASUREMENT_FIELDS  [ = 
  ' windSpeedKmh ,' 
  ' windGustKmh ,' 
  ' windDirectionDeg ,' 
  ' pm10 ,' 
  ' pm25 ,' 
  ' visibilityM ,' 
  ' relativeHumidityPercent ,' 
  ' temperatureC ,' 
 ] as const ; 
 
export const DeviceIngestSchema = z 
  . object {( 
    eventId: z.string().trim().min(1).max ( 128 ,) 
    sequence: 
z.number().int().nonnegative().max(Number.MAX_SAFE_INTEGER) , 
    observedAt: z.string().datetime({ offset: true }) , 
    windSpeedKmh: z.number().finite().min(0).max(300).optional ,)( 
    windGustKmh: z.number().finite().min(0).max(400).optional ,)( 
    windDirectionDeg: z.number().finite().min(0).max(360).optional ,)( 
    pm10: z.number().finite().min(0).max(100_000).optional ,)( 
    pm25: z.number().finite().min(0).max(100_000).optional ,)( 
    visibilityM: z.number().finite().min(0).max(100_000).optional ,)( 
    relativeHumidityPercent: 
z.number().finite().min(0).max(100).optional ,)( 
    temperatureC: z.number().finite().min(-20).max(70).optional ,)( 
   )} 
  . strict )( 
  . refine ( 
    (body) => MEASUREMENT_FIELDS.some((field) => body[field] !== 
undefined) , 
     { message } 'يجب إرسال قراءة قياس واحدة على األقل' : 
   ;) 
 ``` 
  بعد التحقق  route ### كود
 
 
``` ts 
const json: unknown = await request.json().catch(() => null) ; 
const parsed = DeviceIngestSchema.safeParse(json) ; 
 
if (!parsed.success)  { 
  return NextResponse.json ( 
     { error ,'حمولة الجهاز غير صالحة' : details: parsed.error.issues  ,} 
     { status: 400  } 
   ;) 
} 
 
const receivedAt = new Date ;)( 
const observedAt = new Date(parsed.data.observedAt) ; 
 
if (observedAt.getTime() > receivedAt.getTime() + 2 * 60_000)   { 
  return NextResponse.json ( 
     { error: 'observedAt  ,} 'في المستقبل؛ تحقق من ساعة الجهاز 
     { status: 400  } 
   ;) 
} 
 
const  { 
  eventId , 
  sequence , 
  observedAt: _ignored , 
  ... measurements 
 = } parsed.data ; 
 
const { data, error } = await supabaseAdmin.rpc ( 
  ' ingest_device_reading_atomic ,' 
   { 
    p_project_id: auth.projectId , 
    p_device_id: auth.deviceId , 
    p_external_event_id: eventId , 
    p_sequence_no: sequence , 
    p_observed_at: observedAt.toISOString ,)( 
    p_received_at: receivedAt.toISOString ,)( 
    p_measurements: measurements , 
   } 
;) 
 
if (error)  { 
  return NextResponse.json ( 
     { error: safeErrorResponse(error, 'atomic device ingest failed')  ,} 
     { status: error.code === '42501' ? 403 : 500  } 
   ;) 
} 
 
return NextResponse.json {( 
  success: true , 
  duplicate: Boolean(data?.[0]?.is_duplicate) , 
  eventId , 
  observedAt: observedAt.toISOString ,)( 
  receivedAt: receivedAt.toISOString ,)( 
 ;)} 
 ``` 
 ` في مسار االمتثال. إن كان دعم  receivedAt ` المفقود بـ` observedAt ال تستبدل 
`قديمًا يسجل الحدث مع   endpoint أجهزة قديمة ضروريًا، خصص
 
` evidence_eligible=false .` 
 
 --- 
  ) إدخال الجهاز يجب أن يكون معاملة واحدة 5 ##
 
  ### الخطأ
 
 `، ثم إدخال سجل  project_devices المسار الحالي يقوم بفحص التكرار، ثم تحديث 
`، في عمليات مستقلة. يمكن أن يفشل سجل الدليل وتبقى  PM10 الجهاز، ثم سجل
  اللقطة محدثة، ثم تعود استجابة نجاح.
 
  ### تغييرات المخطط
 
 
``` sql 
alter table public.project_devices 
  add column if not exists last_received_at timestamptz , 
  add column if not exists last_pm25_at timestamptz ; 
 
update public.project_devices 
set last_received_at = last_reading_at 
where last_received_at is null ; 
 
alter table public.device_readings_history 
  add column if not exists received_at timestamptz ; 
 
alter table public.pm10_readings_history 
  add column if not exists received_at timestamptz ; 
 
create unique index if not exists device_event_idempotency_idx 
on public.device_readings_history(device_id, external_event_id) 
where external_event_id is not null ; 
 ``` 
  الصحيح  RPC ### هيكل
 
 الدالة التالية توضح العقد المعامالتي الملزم. يجب إبقاء تحديث جميع الحقول 
  الثمانية بالنمط نفسه المبين أدناه.
 
 
``` sql 
create or replace function public.ingest_device_reading_atomic ( 
  p_project_id uuid , 
  p_device_id uuid , 
  p_external_event_id text , 
  p_sequence_no bigint , 
  p_observed_at timestamptz , 
  p_received_at timestamptz , 
  p_measurements jsonb 
) 
returns table (is_duplicate boolean, event_row_id uuid) 
language plpgsql 
security invoker 
set search_path = pg_catalog, public 
as  $$ 
declare 
  v_event_row_id uuid ; 
  v_actual_project_id uuid ; 
  v_is_active boolean ; 
begin 
  if p_external_event_id is null 
     or length(btrim(p_external_event_id)) not between 1 and 128 then 
    raise exception using errcode = '22023 ,' 
      message = 'eventId is required and must contain 1..128 characters ;' 
  end if ; 
 
  if p_sequence_no is null or p_sequence_no < 0 then 
    raise exception using errcode = '22023 ,' 
      message = 'sequence must be a non-negative bigint ;' 
  end if ; 
 
  if p_observed_at is null or p_received_at is null then 
    raise exception using errcode = '22023 ,' 
      message = 'observed_at and received_at are required ;' 
  end if ; 
 
  if p_observed_at > p_received_at + interval '2 minutes' then 
    raise exception using errcode = '22007 ,' 
      message = 'observed_at is in the future ;' 
  end if ; 
 
  if p_measurements is null 
     or jsonb_typeof(p_measurements) <> 'object ' 
     or not (p_measurements ?| array [ 
        ' windSpeedKmh', 'windGustKmh', 'windDirectionDeg ,' 
        ' pm10', 'pm25', 'visibilityM ,' 
        ' relativeHumidityPercent', 'temperatureC ' 
      )] then 
    raise exception using errcode = '22023 ,' 
      message = 'at least one measurement is required ;' 
  end if ; 
 
  select d.project_id, d.is_active 
  into v_actual_project_id, v_is_active 
  from public.project_devices d 
  where d.id = p_device_id 
  for update ; 
 
  if not found 
     or v_actual_project_id <> p_project_id 
     or not v_is_active then 
    raise exception using errcode = '42501 ,' 
      message = 'device not found, revoked, or project mismatch ;' 
  end if ; 
 
  insert into public.device_readings_history  ( 
    project_id, device_id , 
    wind_speed_kmh, wind_gust_kmh, wind_direction_deg , 
    pm10_ug_m3, pm25_ug_m3, visibility_m , 
    relative_humidity_percent, temperature_c , 
    recorded_at, observed_at, received_at , 
    external_event_id, sequence_no 
   ) values  ( 
    p_project_id, p_device_id , 
    (p_measurements->>'windSpeedKmh')::numeric , 
    (p_measurements->>'windGustKmh')::numeric , 
    (p_measurements->>'windDirectionDeg')::numeric , 
    (p_measurements->>'pm10')::numeric , 
    (p_measurements->>'pm25')::numeric , 
    (p_measurements->>'visibilityM')::numeric , 
    (p_measurements->>'relativeHumidityPercent')::numeric , 
    (p_measurements->>'temperatureC')::numeric , 
    p_observed_at, p_observed_at, p_received_at , 
    btrim(p_external_event_id), p_sequence_no 
   ) 
  on conflict (device_id, external_event_id) 
    where external_event_id is not null 
  do nothing 
  returning id into v_event_row_id ; 
 
  if v_event_row_id is null then 
    select h.id into v_event_row_id 
    from public.device_readings_history h 
    where h.device_id = p_device_id 
      and h.external_event_id = btrim(p_external_event_id) ; 
 
    return query select true, v_event_row_id ; 
    return ; 
  end if ; 
 
  if p_measurements ? 'pm10' then 
    insert into public.pm10_readings_history  ( 
      project_id, device_id, pm10_ug_m3, source , 
      recorded_at, observed_at, received_at , 
      external_event_id, sequence_no 
     ) values  ( 
      p_project_id, p_device_id , 
      (p_measurements->>'pm10')::numeric, 'device ,' 
      p_observed_at, p_observed_at, p_received_at , 
      btrim(p_external_event_id), p_sequence_no 
     ;) 
  end if ; 
 
  update public.project_devices d 
  set 
    last_received_at = greatest ( 
      coalesce(d.last_received_at, p_received_at), p_received_at 
     ,) 
    last_reading_at = greatest ( 
      coalesce(d.last_reading_at, p_received_at), p_received_at 
     ,) 
    last_wind_speed_kmh = case 
      when p_measurements ? 'windSpeedKmh ' 
        and (d.last_wind_speed_at is null or p_observed_at >= 
d.last_wind_speed_at) 
      then (p_measurements->>'windSpeedKmh')::numeric 
      else d.last_wind_speed_kmh end , 
    last_wind_speed_at = case 
      when p_measurements ? 'windSpeedKmh ' 
        and (d.last_wind_speed_at is null or p_observed_at >= 
d.last_wind_speed_at) 
      then p_observed_at else d.last_wind_speed_at end , 
    last_wind_gust_kmh = case 
      when p_measurements ? 'windGustKmh ' 
        and (d.last_wind_gust_at is null or p_observed_at >= 
d.last_wind_gust_at) 
      then (p_measurements->>'windGustKmh')::numeric 
      else d.last_wind_gust_kmh end , 
    last_wind_gust_at = case 
      when p_measurements ? 'windGustKmh ' 
        and (d.last_wind_gust_at is null or p_observed_at >= 
d.last_wind_gust_at) 
      then p_observed_at else d.last_wind_gust_at end , 
    last_wind_direction_deg = case 
      when p_measurements ? 'windDirectionDeg ' 
        and (d.last_wind_direction_at is null or p_observed_at >= 
d.last_wind_direction_at) 
      then (p_measurements->>'windDirectionDeg')::numeric 
      else d.last_wind_direction_deg end , 
    last_wind_direction_at = case 
      when p_measurements ? 'windDirectionDeg ' 
        and (d.last_wind_direction_at is null or p_observed_at >= 
d.last_wind_direction_at) 
      then p_observed_at else d.last_wind_direction_at end , 
    last_pm10 = case 
      when p_measurements ? 'pm10 ' 
        and (d.last_pm10_at is null or p_observed_at >= d.last_pm10_at) 
      then (p_measurements->>'pm10')::numeric else d.last_pm10 end , 
    last_pm10_at = case 
      when p_measurements ? 'pm10 ' 
        and (d.last_pm10_at is null or p_observed_at >= d.last_pm10_at) 
      then p_observed_at else d.last_pm10_at end , 
    last_pm25 = case 
      when p_measurements ? 'pm25 ' 
        and (d.last_pm25_at is null or p_observed_at >= d.last_pm25_at) 
      then (p_measurements->>'pm25')::numeric else d.last_pm25 end , 
    last_pm25_at = case 
      when p_measurements ? 'pm25 ' 
        and (d.last_pm25_at is null or p_observed_at >= d.last_pm25_at) 
      then p_observed_at else d.last_pm25_at end , 
    last_visibility_m = case 
      when p_measurements ? 'visibilityM ' 
        and (d.last_visibility_at is null or p_observed_at >= 
d.last_visibility_at) 
      then (p_measurements->>'visibilityM')::numeric 
      else d.last_visibility_m end , 
    last_visibility_at = case 
      when p_measurements ? 'visibilityM ' 
        and (d.last_visibility_at is null or p_observed_at >= 
d.last_visibility_at) 
      then p_observed_at else d.last_visibility_at end , 
    last_relative_humidity_percent = case 
      when p_measurements ? 'relativeHumidityPercent ' 
        and (d.last_relative_humidity_at is null or p_observed_at >= 
d.last_relative_humidity_at) 
      then (p_measurements->>'relativeHumidityPercent')::numeric 
      else d.last_relative_humidity_percent end , 
    last_relative_humidity_at = case 
      when p_measurements ? 'relativeHumidityPercent ' 
        and (d.last_relative_humidity_at is null or p_observed_at >= 
d.last_relative_humidity_at) 
      then p_observed_at else d.last_relative_humidity_at end , 
    last_temperature_c = case 
      when p_measurements ? 'temperatureC ' 
        and (d.last_temperature_at is null or p_observed_at >= 
d.last_temperature_at) 
      then (p_measurements->>'temperatureC')::numeric 
      else d.last_temperature_c end , 
    last_temperature_at = case 
      when p_measurements ? 'temperatureC ' 
        and (d.last_temperature_at is null or p_observed_at >= 
d.last_temperature_at) 
      then p_observed_at else d.last_temperature_at end 
  where d.id = p_device_id ; 
 
  return query select false, v_event_row_id ; 
end ; 
 ;$$ 
 
revoke execute on function public.ingest_device_reading_atomic ( 
  uuid, uuid, text, bigint, timestamptz, timestamptz, jsonb 
 ) from public, anon, authenticated ; 
 
grant execute on function public.ingest_device_reading_atomic ( 
  uuid, uuid, text, bigint, timestamptz, timestamptz, jsonb 
 ) to service_role ; 
 ``` 
 الحدث المتأخر يُحفظ تاريخيًا، لكنه ال يستبدل اللقطة األحدث ألن كل حقل يُحدّث فقط 
  الحقل نفسه.  timestamp ` أحدث من p_observed_at إذا كان `

 
 --- 
  ) حداثة كل حقل قبل دخوله إلى المحرك 6 ##
 
  ### الخطأ
 
 
` FreshDeviceReading  يحتوي أزمنة مستقلة للرياح والرؤية، لكن التعليق `والكود يعتبرانها «للعرض فقط». بذلك يمكن لقيمة رياح أو رؤية قديمة أن تدخل 
 ` يثبت اتصال الجهاز فقط وال يثبت حداثة أي  last_reading_at قرارًا حيًا. `
  مستشعر.
 
  ` app/lib/deviceTelemetry.ts ### ملف مركزي جديد: 
`
 
``` ts 
export const DEVICE_REPORTING_INTERVAL_MS = 2 * 60_000 ; 
export const LIVE_FIELD_MAX_AGE_MS = 2 * DEVICE_REPORTING_INTERVAL_MS ; 
export const FUTURE_CLOCK_TOLERANCE_MS = 2 * 60_000 ; 
 
export type MetricFreshness  = 
  ' | FRESH ' 
  ' | STALE ' 
  ' | MISSING ' 
  ' | INVALID_TIME ' 
  ' | FUTURE_TIME ;' 
 
export interface CheckedMetric   { 
  decisionValue: number | null ; 
  observedAt: string | null ; 
  freshness: MetricFreshness ; 
  validUntilMs: number | null ; 
} 
 
export function checkMetric ( 
  value: number | null , 
  observedAt: string | null , 
  nowMs: number = Date.now )( 
 :) CheckedMetric   { 
  if (value === null || observedAt === null)  { 
    return { decisionValue: null, observedAt, freshness: 'MISSING', 
validUntilMs: null } ; 
   } 
 
  const observedMs = Date.parse(observedAt) ; 
  if (!Number.isFinite(observedMs))   { 
    return { decisionValue: null, observedAt, freshness: 'INVALID_TIME', 
validUntilMs: null } ; 
   } 
 
  if (observedMs > nowMs + FUTURE_CLOCK_TOLERANCE_MS)  { 
    return { decisionValue: null, observedAt, freshness: 'FUTURE_TIME', 
validUntilMs: null } ; 
   } 
 
  const validUntilMs = observedMs + LIVE_FIELD_MAX_AGE_MS ; 
  if (nowMs > validUntilMs)  { 
    return { decisionValue: null, observedAt, freshness: 'STALE', 
validUntilMs } ; 
   } 
 
  return { decisionValue: value, observedAt, freshness: 'FRESH', 
validUntilMs } ; 
} 
 ``` 
  ` افحص كل قيمة بوقتها: buildDustInput ثم في `
 
 
``` ts 
const windSpeed = checkMetric ( 
  freshDevice?.last_wind_speed_kmh ?? null , 
  freshDevice?.last_wind_speed_at ?? null , 
  nowMs 
;) 
const windGust = checkMetric ( 
  freshDevice?.last_wind_gust_kmh ?? null , 
  freshDevice?.last_wind_gust_at ?? null , 
  nowMs 
;) 
const windDirection = checkMetric ( 
  freshDevice?.last_wind_direction_deg ?? null , 
  freshDevice?.last_wind_direction_at ?? null , 
  nowMs 
;) 
const pm10 = checkMetric ( 
  freshDevice?.last_pm10 ?? null , 
  freshDevice?.last_pm10_at ?? null , 
  nowMs 
;) 
const pm25 = checkMetric ( 
  freshDevice?.last_pm25 ?? null , 
  freshDevice?.last_pm25_at ?? null , 
  nowMs 
;) 
const visibility = checkMetric ( 
  freshDevice?.last_visibility_m ?? null , 
  freshDevice?.last_visibility_at ?? null , 
  nowMs 
;) 
 
return   {  // الحقول الحالية األخرى تبقى كما هي.  
 
  hasDeviceLink: Boolean(row.device_id) , 
  deviceLastReadingAt: freshDevice?.last_reading_at ?? null , 
  devicePm10LastReadingAt: pm10.observedAt , 
  deviceWindSpeedKmh: windSpeed.decisionValue , 
  deviceWindGustKmh: windGust.decisionValue , 
  deviceWindDirectionDeg: windDirection.decisionValue , 
  devicePm10: pm10.decisionValue , 
  devicePm25: pm25.decisionValue , 
  deviceVisibilityM: visibility.decisionValue , 
;} 
 ``` 
 ` واالستعالمين في  FreshDeviceReading ` إلى ` last_pm25_at يجب إضافة 
`
` dustEvaluation.ts أبقِ آخر قيمة قديمة في .` DTO    عرض منفصل؛ ال تمررها إلى *` المستخدمة في القرار. device حقول `

 دقيقة إلى   20 ` من final-decision-engine/adapters.ts غيّر حد جودة الدليل في 
`فقط. إذا فقد حقل حرج أو أصبح قديمًا،   PM10 دقائق، وافحص الرياح والرؤية ال  4
 `. ALLOW `، ال ` HOLD_FOR_VERIFICATION فالنتيجة الحية تكون `

 
 --- 
  Open‑Meteo ) فصل القرار الحي للجهاز عن 7 ##
 
  ### الخطأ
 
 
` evaluateDustVisibilityWindow  يجلب ` Open‑Meteo ` قبل بناء worst  من `` النشاط  computeDustResults الجهاز. إذا فشل المزود، يُرمى خطأ، ثم قد يُسقط `
 ` رغم وجود جهاز كامل. persisted: 0 ويعيد مسار التقييم `

  ### التصحيح المعماري
 
  افصل مسارين:
 
 - ` evaluateDustVisibilityOperational  متزامن، يعتمد على الجهاز فقط، بال :`للتخطيط وأفضل/أسوأ   Open‑Meteo `: يستخدم evaluateDustVisibilityForecast ` - شبكة.
  نافذة فقط.
 
  ### كود القرار الحي
 
 
``` ts 
function buildDeviceContextSample(input: DustEngineInput): 
DustWeatherSample  { 
  return  { 
    visibilityM: input.deviceVisibilityM ?? null , 
    weatherCode: null , 
    weatherSymbol: 'UNKNOWN ,' 
    windSpeedKmh: input.deviceWindSpeedKmh ?? null , 
    windGustKmh: input.deviceWindGustKmh ?? null , 
    windDirectionDeg: input.deviceWindDirectionDeg ?? null , 
    relativeHumidityPercent: input.deviceRelativeHumidityPercent ?? null , 
    temperatureC: input.deviceTemperatureC ?? null , 
    rainfallLast24hMm: null , 
    pm10: input.devicePm10 ?? null , 
    pm25: input.devicePm25 ?? null , 
    dustConcentration: null , 
    dataSource: 'none ,' 
    isForecastStale: false , 
   ;} 
} 
 
export function evaluateDustVisibilityOperational ( 
  input: DustEngineInput , 
  windowStartIso: string , 
  durationHours: number , 
  nowMs: number = Date.now )( 
 :) DustWindowEvaluation  { 
  const safeDuration = Math.max(1, Math.round(durationHours)) ; 
  const startMs = Date.parse(windowStartIso) ; 
  const endMs = startMs + safeDuration * 3_600_000 ; 
  const liveMarginMs = 30 * 60_000 ; 
 
  if (!input.hasDeviceLink)  { 
    return buildAwaitingEvaluationWindow(windowStartIso, endMs, 
safeDuration) ; 
   } 
 
  if (nowMs < startMs - liveMarginMs || nowMs > endMs)  { 
    throw new Error('OPERATIONAL_EVALUATION_OUTSIDE_ACTIVITY_WINDOW') ; 
   } 
 
  const sample = buildDeviceContextSample(input) ; 
  const evaluatedAt = new Date(nowMs).toISOString ;)( 
  const dvi = computeDviResult(input, sample, undefined, false) ; 
 
  const worst: DviHourlyEvaluation  { = 
     ... dvi , 
    time: evaluatedAt , 
    rawWeatherSample: sample , 
    mergedReading: mergeDustReading(input, sample, undefined, false) , 
   ;} 
 
  return  { 
    worst , 
    hourly ,][ : 
    windowStartIso: new Date(startMs).toISOString ,)( 
    windowEndIso: new Date(endMs).toISOString ,)( 
    durationHours: safeDuration , 
    bestWindowStartIso: null , 
    bestWindowWorst: null , 
    avoidWindowStartIso: null , 
    avoidWindowWorst: null , 
   ;} 
} 
 ``` 
 في نقطة الكتابة الرسمية استخدم المسار التشغيلي فقط. اجلب التوقعات في 
 
cadence   دقيقة، وال تجعل  60–30 منفصل كل Cron  الدقيقتين يطلب Open‑Meteo   لكل نشاط.
 
 ` للقرار الحي. اجعله أصغر وقت  validUntil = now + 60 minutes كذلك ال تجعل 
` `. observedAt انتهاء للحقول الحرجة وبحد أقصى أربع دقائق من `

 
 --- 
  ) تشغيل التقييم خادميًا كل دقيقتين 8 ##
 
  ### الخطأ
 
 `، وتوليد  dashboard/Projects/[id]/page.tsx إعادة التقييم كل دقيقتين في 
``. كالهما يتوقف عند إغالق التبويب.  dashboard/layout.tsx تنبيهات المستخدم في `

` vercel.json يشغّل ` Cron   مرة يوميًا فقط، ومسار التنبيهات ال يعيد حفظ القرار التشغيلي الكامل.
 
  ### الخدمة المشتركة
 
  الحالي إلى:  route انقل منطق
 
 
` app/services/projectEvaluation.ts ` 
 
``` ts 
export async function evaluateAndPersistProject ( 
  projectId: string , 
  triggeredBy: 'user_refresh' | 'scheduler_2m ' 
 { ) 
  const [projectRes, profilesRes, shiftsRes, receptorsRes] = await 
Promise.all [( 
    supabaseAdmin.from('projects').select('*').eq('id', 
projectId).maybeSingle ,)( 
     
supabaseAdmin.from('project_dust_profiles').select('*').eq('project_id', 
projectId) , 
    supabaseAdmin.from('project_shifts').select('*').eq('project_id', 
projectId) 
       . order('sort_order', { ascending: true }) , 
    supabaseAdmin.from('sensitive_receptors') 
       . select('id, name, receptor_type, lat, lng') , 
   ;)] 
 
  for (const result of [projectRes, profilesRes, shiftsRes, 
receptorsRes])  { 
    if (result.error) throw result.error ; 
   } 
  if (!projectRes.data) throw new Error('PROJECT_NOT_FOUND') ; 
 
  const project = { ...projectRes.data, shifts: shiftsRes.data  ][ ?? } ; 
  const profiles = profilesRes.data  ;][ ?? 
 
  const dustResults = await computeDustResults ( 
    profiles , 
    project , 
    supabaseAdmin , 
     { operationalOnly: true, includeForecasts: false  } 
   ;) 
 
  const receptors = (receptorsRes.data ?? 
[]).map(buildSensitiveReceptor) ; 
  const compliance = await computeDustComplianceResults ( 
    profiles, project, dustResults, receptors, supabaseAdmin, true 
   ;) 
 
  const persisted = await persistActivityDecisionsAtomic ( 
    supabaseAdmin , 
    projectId , 
    dustResults , 
    compliance , 
    triggeredBy , 
    triggeredBy 
   ;) 
 
  const failedActivityIds = persisted 
    . filter((row) => row.failed) 
    . map((row) => row.activityId) ; 
 
  return  { 
    projectId , 
    evaluated: dustResults.length , 
    persisted: dustResults.length - failedActivityIds.length , 
    failedActivityIds , 
   ;} 
} 
 ``` 
   Cron مسار المستخدم يفحص الجلسة والملكية ثم يستدعي هذه الخدمة. مسار 
  يستدعيها بصالحية الخادم.
 
 
 ### Route  المجدول 
 
``` ts 
import { timingSafeEqual } from 'crypto ;' 
import { NextResponse } from 'next/server ;' 
import { evaluateAndPersistProject } from 
'@/app/services/projectEvaluation ;' 
import { supabaseAdmin } from '@/app/lib/supabaseAdmin ;' 
 
export const runtime = 'nodejs ;' 
export const maxDuration = 60 ; 
 
function authorized(request: Request): boolean   { 
  const secret = process.env.CRON_SECRET ; 
  if (!secret) return false ; 
  const actual = Buffer.from(request.headers.get('authorization') ?? '') ; 
  const expected = Buffer.from(`Bearer ${secret}`) ; 
  return actual.length === expected.length && timingSafeEqual(actual, 
expected) ; 
} 
 
export async function GET(request: Request)  { 
  if (!process.env.CRON_SECRET)  { 
    return NextResponse.json  {( ok: false, error: 'CRON_SECRET    'غير معرّف
 ,} { status: 503 } ;) 
   } 
  if (!authorized(request))  { 
    return NextResponse.json({ ok: false, error: 'unauthorized' }, { 
status: 401 }) ; 
   } 
 
  const { data: projects, error } = await supabaseAdmin 
    . from('projects').select('id') ; 
  if (error)  { 
    return NextResponse.json({ ok: false, error: error.message }, { 
status: 500 }) ; 
   } 
 
  const ids = (projects ?? []).map((project) => project.id) ; 
  const outcomes: Array<{ projectId: string; ok: boolean; error?: string 
} ;][ = > 
  const concurrency = 4 ; 
 
  await Promise.all(Array.from ( 
     { length: Math.min(concurrency, ids.length)  ,} 
    async (_, worker)   { >= 
      for (let index = worker; index < ids.length; index += concurrency)  
{ 
        const projectId = ids[index] ; 
        try   { 
          await evaluateAndPersistProject(projectId, 'scheduler_2m') ; 
          outcomes.push({ projectId, ok: true }) ; 
         } catch (caught { ) 
          outcomes.push {( 
             projectId , 
             ok: false , 
             error: caught instanceof Error ? caught.message : 
String(caught) , 
           ;)} 
         } 
       } 
     } 
   ;)) 
 
  const failures = outcomes.filter((item) => !item.ok) ; 
  return NextResponse.json ( 
     { ok: failures.length === 0, checkedAt: new Date().toISOString(), 
outcomes  ,} 
     { status: failures.length === 0 ? 200 : 207  } 
   ;) 
} 
 ``` 
 ` بمفتاح  scheduled_evaluation_runs لدقيقتين أو جدول ` bucket يجب إضافة قفل
 
(` project_id, bucket_at  لمنع تنفيذ متزامن مكرر. توليد التنبيهات يحدث `) **بعد نجاح حفظ القرار**.
 
 
``` json 
{ 
  " crons [ :" 
     " { path": "/api/cron/evaluate", "schedule": "*/2  } "* * * * 
   ] 
} 
 ``` 
 هذا الجدول ال يعمل إال على منصة/خطة تدعم دقيقتين فعليًا. إن كانت الخطة ال 
 خارجي موثوق. المتصفح يصبح للعرض فقط،   scheduler أو  worker تدعم ذلك، استخدم 
  ال مصدر تشغيل.
 
 
 --- 
  والحفظ الذري للقرار  CAS ) إصالح 9 ##
 
  ### الخطأ
 
 
` persist_activity_decision_atomic ` ينفذ ` UPDATE ... WHERE updated_at = 
expected لكنه ال يفحص عدد الصفوف المتأثرة. بعد تعارض ،` CAS   يمكنه إدخال
` final_decisions .ثم إعالن نجاح الحفظ ` 
  الملزم   SQL ### كود
 
  `: DECLARE أضف إلى 
`
 
``` sql 
v_affected_rows integer := 0 ; 
 ``` 
  وفي أول الدالة:
 
 
``` sql 
perform pg_advisory_xact_lock ( 
  pg_catalog.hashtextextended ( 
    p_project_id::text || ':' || p_activity_group_id , 
    0 
   ) 
;) 
 
perform 1 
from public.project_dust_profiles p 
where p.id = p_activity_id::uuid 
  and p.project_id = p_project_id 
  and coalesce(p.activity_group_id, 'dust-' || p.id::text) = 
p_activity_group_id ; 
 
if not found then 
  raise exception using errcode = '23503 ,' 
    message = 'activity/project/activity_group mismatch ;' 
end if ; 
 ``` 
  ` للقرار الحالي: INSERT ... ON CONFLICT DO NOTHING ` أو ` UPDATE بعد كل 
`
 
``` sql 
get diagnostics v_affected_rows = row_count ; 
 
if v_affected_rows <> 1 then 
  raise exception using 
    errcode = '40001 ,' 
    message = format ( 
       ' CAS conflict for project=%s activity_group=%s ,' 
      p_project_id , 
      p_activity_group_id 
     ;) 
end if ; 
 ``` 
  RAISE . ألن ` Compliance و DVI في  CAS ` إال بعد نجاح final_decisions ال تُدخل 
` يتيم.  evaluation ` داخل الدالة يتراجع بالمعاملة كلها، ال يبقى 
40001
 ` فقط  anon, authenticated ` أيضًا؛ سحب الصالحية من ` PUBLIC عن ` EXECUTE وأغلق
  افتراضيًا:  PUBLIC لـ EXECUTE يمنح الدوال  PostgreSQL ال يكفي ألن
 
 
``` sql 
revoke execute on function public.persist_activity_decision_atomic ( 
  uuid, text, text, jsonb, text, timestamptz, jsonb, text , 
  text, timestamptz, uuid, timestamptz, timestamptz, jsonb, timestamptz 
 ) from public, anon, authenticated ; 
 
grant execute on function public.persist_activity_decision_atomic ( 
  uuid, text, text, jsonb, text, timestamptz, jsonb, text , 
  text, timestamptz, uuid, timestamptz, timestamptz, jsonb, timestamptz 
 ) to service_role ; 
 ``` 
  TypeScript ### معالجة
 
 
``` ts 
const { data, error } = await supabaseAdmin.rpc ( 
  ' persist_activity_decision_atomic ,' 
  rpcPayload 
;) 
 
if (error?.code === '40001')  { 
  return { ...base, failed: true, conflict: true } ; 
} 
 
if (error || !data?.[0])  { 
  return { ...base, failed: true, conflict: false } ; 
} 
 ``` 
 جديد؛ أعد قراءة الحالة   timestamp ` ال تعِد إرسال نفس النتيجة مع 40001 عند 
` دورة الحساب كاملة.  caller ليعيد  HTTP 409 وإعادة الحساب، أو أرجع
 
 
 --- 
  ) إصالح نص استئناف الرياح 10 ##
 
  ### الخطأ
 
 
` app/utils/dust-compliance-engine/engine.ts  يجمع بوابة الرياح العامة ` ». 15 `، ثم يعرض للجميع «أقل من  15 ` مع الهدم وقطع األحجار عند `>= 25 `>

  ### كود االستبدال
 
 
``` ts 
const GENERAL_WIND_STOP_RULE_CODES = new Set [( 
  ' GATE-WIND-ABOVE-25-004 ,' 
  ' GATE-WIND-ABOVE-25-RESUME-HOLD ,' 
 ;)] 
 
const ACTIVITY_WIND_15_STOP_RULE_CODES = new Set [( 
  ' DEMO-WIND-STOP-001 ,' 
  ' STONECUT-WIND-STOP-003 ,' 
 ;)] 
 
const requiresWindBelow15 = topHits.some((hit >= ) 
  ACTIVITY_WIND_15_STOP_RULE_CODES.has(hit.code) 
;) 
const requiresWindBelow25 = topHits.some((hit >= ) 
  GENERAL_WIND_STOP_RULE_CODES.has(hit.code) 
;) 
 
const restartConditions: string ;][ = ][ 
if (mandatoryStop || decisionCategory === 'STOP_AFFECTED_ACTIVITY')  { 
  if (requiresWindBelow15)  { 
    restartConditions.push كم/س');  15 ('انخفاض سرعة الرياح إلى ما دون 
   } else if (requiresWindBelow25 { ) 
    restartConditions.push كم/س');  25 ('انخفاض سرعة الرياح إلى ما دون 
   } 
  والتثبيط والرؤية واالستقرار الموجودة حاليًا هنا.  DMP // أبقِ بقية شروط  
 
} 
 ``` 
  . 15 إذا اجتمعت القاعدتان، يفوز الحد األشد: أقل من
 
 
 --- 
  ) إصالح القرار النهائي 11 ##
 
  ### الخطأ
 
 
` app/utils/final-decision-engine/engine.ts ` يحول ` RESTRICT  ``. هذا يعني السماح بالعمل  MONITOR _*` إلى ` STOP ` وقراري ` RESTRICT_SEVERE و`
 مع المراقبة رغم أن القرار الفيزيائي يطلب تقييدًا أو توقفًا.
 
  ### جدول التحويل الصحيح
 
 
 | DVI | OperationalDecision  | 
|---|--- | 
` | ALLOW` | `ALLOW | ` 
` | ALLOW_WITH_MONITORING` | `MONITOR | ` 
` | RESTRICT` | `RESTRICT | ` 
` | RESTRICT_SEVERE` | `RESTRICT | ` 
` | STOP_DUST_GENERATING_ACTIVITIES` | `PROTECTIVE_STOP | ` 
` | STOP_VISIBILITY_DEPENDENT_ACTIVITIES` | `PROTECTIVE_STOP | ` 
` | MANDATORY_STOP ` أو ` mandatoryStop=true` | `MANDATORY_STOP | ` 
 
` FIELD_VERIFICATION_REQUIRED ` يجب أن يصبح ` HOLD_FOR_VERIFICATION  ` `. regulatoryFinding=NOT_DETERMINABLE و`

  ### كود التحويل
 
 
``` ts 
function mapDviOperationalDecision ( 
  dvi: DviEvaluationResult , 
  suppressPm10Mandatory: boolean 
 :) OperationalDecision  { 
  if  ( 
    ! suppressPm10Mandatory  && 
    ( dvi.mandatoryStop || dvi.decisionCategory === 'MANDATORY_STOP )' 
   { ) 
    return 'MANDATORY_STOP ;' 
   } 
 
  switch (dvi.decisionCategory)  { 
    case 'ALLOW :' 
      return 'ALLOW ;' 
    case 'ALLOW_WITH_MONITORING :' 
      return 'MONITOR ;' 
    case 'RESTRICT :' 
    case 'RESTRICT_SEVERE :' 
      return 'RESTRICT ;' 
    case 'STOP_DUST_GENERATING_ACTIVITIES :' 
    case 'STOP_VISIBILITY_DEPENDENT_ACTIVITIES :' 
      return 'PROTECTIVE_STOP ;' 
    case 'MANDATORY_STOP :' 
      return 'MANDATORY_STOP ;' 
   } 
} 
 
function mapComplianceOperationalDecision ( 
  compliance: DustComplianceResult | null 
 :) OperationalDecision  { 
  if (!compliance) return 'ALLOW ;' 
 
  const blocks  = 
    compliance.decisionCategory === 'MANDATORY_STOP || ' 
    compliance.decisionCategory === 'STOP_AFFECTED_ACTIVITY ;' 
 
  if (blocks && !compliance.pendingConfirmation) return 'MANDATORY_STOP ;' 
  if (blocks && compliance.pendingConfirmation) return 'PROTECTIVE_STOP ;' 
 
  switch (compliance.decisionCategory)  { 
    case 'FIELD_VERIFICATION_REQUIRED :' 
      return 'HOLD_FOR_VERIFICATION ;' 
    case 'RESTRICT_ACTIVITY :' 
      return 'RESTRICT ;' 
    case 'PRECAUTION :' 
    case 'ALLOW_WITH_CONTROLS :' 
      return 'MONITOR ;' 
    default : 
      return 'ALLOW ;' 
   } 
} 
 
const OPERATIONAL_PRIORITY: Record<OperationalDecision, number { = > 
  ALLOW: 0 , 
  MONITOR: 1 , 
  RESTRICT: 2 , 
  HOLD_FOR_VERIFICATION: 3 , 
  PROTECTIVE_STOP: 4 , 
  MANDATORY_STOP: 5 , 
;} 
 
function stricter ( 
  left: OperationalDecision , 
  right: OperationalDecision 
 :) OperationalDecision  { 
  return OPERATIONAL_PRIORITY[left] >= OPERATIONAL_PRIORITY[right] 
     ? left 
     : right ; 
} 
 ``` 
 ` سببه رؤية أو  dvi.mandatoryStop المعل ّق أن يلغي ` PM10 مهم: ال تسمح لتوقف
   PM10 اإللزامي ناتجًا حصريًا عن قاعدة  DVI رياح. القمع مسموح فقط إذا كان
  المعل ّقة.
 
 سبب القرار والملصق يجب أن يأتيا من المرشح الذي فاز بالشدة، ال من مصدر أخف. 
 ` بسبب  MANDATORY_STOP » بينما القرار الفعلي ` PM10 وإال يمكن عرض «احتراز
  الرؤية.
 
  في مولّد التنبيهات:
 
 
``` ts 
const isOperationalStop  = 
  finalDecision.operationalDecision === 'MANDATORY_STOP || ' 
  finalDecision.operationalDecision === 'PROTECTIVE_STOP ;' 
 ``` 
  ` فقط. mandatoryStop استخدمه إلطالق تنبيه السالمة؛ ال تحصر التنبيه في 
`
 
 --- 
  ) األرشفة بدل الحذف 12 ##
 
 . لذلك حذف نشاط أو جهاز بعد وجود أدلة سيصطدم بقيود  append-only سجل االمتثال 
 
FK و triggers  .أو سيحاول محو سلسلة اإلثبات 
  ### الجهاز
 
  الفعلي في:  DELETE استبدل 
 
 
` app/api/projects/[projectId]/devices/[deviceId]/route.ts ` 
  بإلغاء:
 
 
``` ts 
const { error } = await supabaseAdmin 
  . from('project_devices') 
  . update {( 
    is_active: false , 
    revoked_at: new Date().toISOString ,)( 
   )} 
  . eq('id', deviceId) 
  . eq('project_id', projectId) ; 
 ``` 
  ### النشاط
 
  أضف:
 
 
``` sql 
alter table public.project_dust_profiles 
  add column if not exists archived_at timestamptz , 
  add column if not exists archived_by uuid ; 
 ``` 
 ` من  archived_at is not null لألرشفة. استبعد ` UPDATE إلى  DELETE ثم بدّل 
  التقييمات الجديدة، وأبقِ السجالت التاريخية قابلة للقراءة.
 
 
 --- 
  ) التحقق الدفاعي من المدخالت 13 ##
 
 `  app/api/dust-profiles/route.ts ()` كامالً في ` z.object(...).strict استخدم 
` لحقلين ثم إدخال كائن العميل. يجب أن يشمل:  denylist بدل
 
 - enums  `فعلية لـ activity_type `و ` regulatory_activity .` - ` lat  و`  90 و  90-` بين lng  األرقام  - . 180 و  180-` بين finite  .مدة موجبة بحد أقصى منطقي.  - وغير سالبة حيث يلزم - ` device_id .منع قبول `  - ` تابع للمشروع نفسه aei_score `و ` aei_status .من العميل؛ يحسبهما الخادم ` 
  : rulebook وأضف دفاعًا ثانيًا في
 
 
``` ts 
export function classifyWind ( 
  windSpeedKmh: number | null 
 :) DustWindBand  { 
  if  ( 
    windSpeedKmh === null   || 
    ! Number.isFinite(windSpeedKmh)  || 
    windSpeedKmh < 0 
   { ) 
    return 'UNKNOWN ;' 
   } 
 
  if (windSpeedKmh < 15) return 'BELOW_15 ;' 
  if (windSpeedKmh <= 25) return 'FROM_15_TO_25 ;' 
  return 'ABOVE_25 ;' 
} 
 ``` 
  . DVI أو ينتشر داخل   25 ` أن يصنف كرياح أعلى من NaN ال تسمح لـ
`
 
 --- 
  ) خطة اختبارات القبول 14 ##
 
  ### قاعدة البيانات واألمن
 
 
1 ` . supabase db reset .ينجح على قاعدة فارغة ` 
2 ` . authenticated ال يستطيع ` INSERT/UPDATE/DELETE  .مباشرة على جداول القرار  الموثق تنجح.  API . الكتابة عبر 
3
4 . RPC  ` الخاصة بالحفظ واإلدخال غير قابلة للتنفيذ بواسطة PUBLIC  أو `
` authenticated .` 
  ### اإلدخال والزمن
 
  ويحدث اللقطة.  PM10 . الحدث األول ينشئ صفًا واحدًا في سجلي الجهاز و 
1 ` تترك صفًا واحدًا فقط. eventId طلبًا متزامنًا بنفس ` 20 . 
2 . timestamp . الحدث المكرر ال يغير أي  
3 . حدث قديم يصل بعد أحدث: يظهر في التاريخ وال يستبدل اللقطة. 
4 `. last_pm10_at . حرارة فقط ال تغير ` 
5
6 . PM10  .فقط ال يغير أزمنة الرياح والرؤية  ` قديم. 4:00.001 ` مقبول عند الحد، ` 4:00 ` حديث، ` 3:59 . عمر ` 
7
8 . timestamp  .مستقبلي خارج التسامح ال يدخل القرار  ال تدخل القرار.  timestamp . قيمة بال 
9 يتراجع معه سجل الجهاز وتحديث اللقطة.  PM10 . فشل إدخال 
10
  ### المحركات والقرار النهائي
 
  . 25 ` لنشاط عام: توقف وشرط استئناف أقل من  25.01 . ` 
1 . 15 `: توقف وشرط أقل من 15 . الهدم وقطع األحجار عند ` 
2
3 ` . DVI RESTRICT `و ` RESTRICT_SEVERE` => `RESTRICT .` 
4 ` . DVI STOP ` >= غير اإللزامي `*_ PROTECTIVE_STOP .` 
5 ` . FIELD_VERIFICATION_REQUIRED` => `HOLD_FOR_VERIFICATION .`  المعل ّق.  PM10 . توقف رؤية إلزامي ال يلغيه 
6 . سبب القرار يأتي من المصدر الفائز بالشدة. 
7
  ### التزامن والجدولة
 
 
 `، وال صفوف يتيمة. 40001 . طلبان ألول قرار: واحد ينجح واآلخر ` 1 : واحد فقط ينجح. timestamp . طلبان بنفس 
2 ` وال يكتب شيئًا. 23503 . النشاط من مشروع آخر يرجع ` 
3 . المتصفحات مغلقة، ومع ذلك يتحدث القرار خالل دورة الدقيقتين. 
4 . فشل مشروع ال يمنع بقية المشاريع. 
5 الدقيقتين نفسه: واحد فقط ينفذ.  bucket . استدعاءان في 
6 . توليد التنبيه يبدأ بعد حفظ القرار، ال قبله. 
7 `. evaluated_at ال يمنع قرار جهاز مكتمل وال يمنع تقدم `  Open‑Meteo . فشل  
8
  حاسم  E2E ### سيناريو 
 
 أعلى من الحد مع   PM10 أغلق المتصفح، أرسل قراءتين أو أكثر من جهاز فعلي ب
ـ
` observedAt ثانية. يجب أن يتأكد القرار في دورة   121 ` حقيقي، وانتظر تجاوزمتعطالً. بعد ذلك أرسل حرارة فقط؛ يجب    Open‑Meteo المجدول التالية حتى لو كان
 أقدم متأخرًا؛ يجب أن يُحفظ تاريخيًا من    PM10 . ثم أرسل حدث  PM10 أال يتغير عمر
  دون استبدال أحدث قراءة أو إعادة سلسلة االستمرار.
 
 
 --- 
 
 ## 15  ) ESLint   والتغطية دون تشتيت إصالحات P0 
 no-explicit تحذيرًا؛ معظمها ` 33 خطأ و 326 للمشروع نفسه تقارب  lint نتيجة
 
any  ثم متغيرات غير مستخدمة ومشكالت ،` Hooks .ال تعطّل القواعد عالميًا . 
  خطة صحيحة:
 
 في الملفات التي ستُلمس ضمن   lint و  Hooks ومشكالت  parser errors . أصلح فورًا 
1
P0 .   domain ` تدريجيًا بأنواع  any واستبدل ` Supabase `Database` types . ولّد  
2 حقيقية.
  يمنع أي خطأ جديد حتى قبل تصفير الدين القديم.  CI . اجعل 
3الذرية/الزمن؛ حجم التغيير سيرفع    PR ` في any حالة ` 282 . ال تخلط إصالح 
4 مخاطرة االنحدار.
 
  : thresholds نفسه، ثم  Vitest أضف حزمة التغطية الموجودة في إصدار
 
 
``` ts 
// vitest.config.ts 
test { : 
  include: ['app/**/*.test.{ts,tsx}'] , 
  environment: 'node ,' 
  coverage { : 
    provider: 'v8 ,' 
    reporter: ['text', 'json-summary', 'lcov'] , 
    include: ['app/**/*.{ts,tsx}'] , 
    exclude: ['app/**/*.test.{ts,tsx}', 'app/**/types.ts'] , 
    thresholds { : 
      lines: 80 , 
      statements: 80 , 
      functions: 80 , 
      branches: 75 , 
     ,} 
   ,} 
} 
 ``` 
 
 API األولوية في التغطية ليست رفع الرقم العام شكليًا؛ ابدأ بـ
ingest/evaluate  ، RLS  ، Supabase RPC  التزامن، والسيناريو من الجهاز حتى ، القرار والتنبيه.
 
 
 --- 
  قبل النشر  Done ) تعريف 16 ##
 
  ال يعتبر العمل منتهيًا إال إذا تحققت كلها:
 إلثبات االختبارات؛ بيئة مؤقتة معزولة   staging/production ال تُستخدم بيانات  - . CI جميع اختبارات القبول السابقة مفعلة في   - _*` ال يتحول إلى مراقبة. STOP ` - ظاهر ومُعالج، وليس نجاحًا زائفًا.  CAS تعارض  - التقييم والتنبيه يستمران والمتصفح مغلق. - . Open‑Meteo القرار الحي يعمل دون  - ال تدخل قراءة قديمة أو متأخرة في قرار حي على أنها حديثة.  - اإلدخال وحفظ القرار كل منهما معاملة واحدة قابلة للتراجع الكامل.  - ` إلزامية. eventId + sequence + observedAt كل حدث جهاز له ` - ` إلى جداول القرار أو األدلة. anon/authenticated ال كتابة مباشرة من ` - قاعدة جديدة تُنشأ من الصفر دون تدخل يدوي. -
  فقط.
 
  ` إلى مراجعة إطالق نهائية. NO-GO بعد ذلك فقط يمكن االنتقال من 
`
 