-- =====================================================================
-- DCR — 202607290008_require_device_coordinates.sql
-- =====================================================================
-- إحداثيات الجهاز (project_devices.lat/lng) تصير إجبارية (طلب صريح من
-- المستخدم): الربط التلقائي بأقرب جهاز عند إنشاء نشاط تنظيمي
-- (resolveNearestActiveDeviceId في app/api/dust-profiles/route.ts) يحتاج
-- موقعاً فعلياً معروفاً لكل جهاز، وإلا يُستبعد الجهاز صامتاً من الحساب —
-- إجبارية الإدخال منذ الإنشاء (الواجهة + API) تمنع هذا الاستبعاد الصامت
-- من الأساس، والقيد هنا على مستوى القاعدة يمنع أيضاً أي إدراج مباشر
-- مستقبلي (SQL Editor، سكربت هجرة بيانات، إلخ) يتجاوز طبقة الـAPI.
--
-- المستخدم أكّد صراحةً مسؤوليته عن حذف أي أجهزة قديمة محفوظة بلا إحداثيات
-- قبل تطبيق هذا الملف — ALTER TABLE ... SET NOT NULL يفشل لو بقي أي صف
-- بـlat أو lng = null وقت التنفيذ.
-- =====================================================================

alter table public.project_devices
  alter column lat set not null,
  alter column lng set not null;
